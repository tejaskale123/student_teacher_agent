================================================================================
PROJECT EXTRACTION REPORT
================================================================================

PROJECT STRUCTURE:
--------------------------------------------------------------------------------

student_teacher_agent/
├── README.md
├── app.py
├── chat_history.json
├── chunks.pkl
├── config.py
├── export_project.py
├── faiss_index.bin
├── main.py
├── project_extraction.txt
├── requirements.txt
├── sample.pdf
├── test_nvidia.py
├── test_nvidia_key.py
├── test_openai.py
├── agents/
│   ├── __init__.py
│   ├── answer_generator_agent.py
│   ├── combiner_agent.py
│   ├── confidence_agent.py
│   ├── critic_agent.py
│   ├── execution_monitor_agent.py
│   ├── formatter_agent.py
│   ├── intent_agent.py
│   ├── math_agent.py
│   ├── memory_agent.py
│   ├── planner_agent.py
│   ├── query_rewriter_agent.py
│   ├── rag_agent.py
│   ├── reflection_agent.py
│   ├── registry.py
│   ├── research_agent.py
│   ├── router.py
│   ├── search_agent.py
│   ├── search_summarizer_agent.py
│   ├── student_agent.py
│   ├── supervisor_agent.py
│   └── teacher_agent.py
├── data/
│   └── chat_history.json
├── evaluation/
│   └── evaluate.py
├── llm/
│   ├── __init__.py
│   └── nvidia_client.py
├── logs/
│   ├── agent_runs.jsonl
│   └── app.log
├── memory/
│   ├── __init__.py
│   └── memory.py
├── rag/
│   ├── chunker.py
│   ├── embedding_model.py
│   ├── pdf_reader.py
│   ├── rag_chat.py
│   ├── retriever.py
│   ├── test_chunks.py
│   ├── test_embeddings.py
│   ├── test_faiss.py
│   ├── test_pdf.py
│   ├── test_rag.py
│   ├── test_retriever.py
│   └── vector_store.py
└── tools/
    ├── __init__.py
    ├── calculator_tool.py
    └── search_tool.py


================================================================================
FILE CONTENTS
================================================================================


================================================================================
FILE: .history\agents\__init___20260604234133.py
================================================================================



================================================================================
FILE: .history\agents\__init___20260604234134.py
================================================================================



================================================================================
FILE: .history\agents\__init___20260605234133.py
================================================================================

self.llm = NvidiaClient(NVIDIA_API_KEY)

================================================================================
FILE: .history\agents\__init___20260605234136.py
================================================================================

self.llm = NvidiaClient(NVIDIA_API_KEY)

================================================================================
FILE: .history\agents\__init___20260605234426.py
================================================================================



================================================================================
FILE: .history\agents\combiner_agent_20260608004701.py
================================================================================



================================================================================
FILE: .history\agents\combiner_agent_20260608004936.py
================================================================================

class CombinerAgent:

    def combine(self, results):

        output = []

        for name, result in results.items():

            output.append(
                f"{name.upper()} RESULT:\n{result}"
            )

        return "\n\n".join(output)

================================================================================
FILE: .history\agents\combiner_agent_20260608004937.py
================================================================================

class CombinerAgent:

    def combine(self, results):

        output = []

        for name, result in results.items():

            output.append(
                f"{name.upper()} RESULT:\n{result}"
            )

        return "\n\n".join(output)

================================================================================
FILE: .history\agents\critic_agent_20260608005600.py
================================================================================



================================================================================
FILE: .history\agents\critic_agent_20260608005956.py
================================================================================

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class CriticAgent:

    def __init__(self):

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def critique(self, answer):

        prompt = f"""
You are a critic agent.

Review this answer.

Check:

1. Accuracy
2. Completeness
3. Clarity

If needed improve it.

Answer:

{answer}
"""

        return self.llm.ask(prompt)

================================================================================
FILE: .history\agents\formatter_agent_20260608152748.py
================================================================================



================================================================================
FILE: .history\agents\formatter_agent_20260608153009.py
================================================================================

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class FormatterAgent:

    def __init__(self):

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def format(self, answer):

        prompt = f"""
You are an expert AI teacher and content formatter.

Your job is to convert raw AI answers into
professional, easy-to-read responses.

Rules:

1. Always create a title.

2. Add:
   - Summary
   - Main Explanation
   - Key Points
   - Example (if applicable)
   - Conclusion

3. Use bullet points.

4. Preserve URLs exactly.

5. If answer contains search results:

Format:

# Search Results

## Top Sources

1. Title
   URL

2. Title
   URL

## Key Insights

...

## Recommendation

...

6. If answer contains comparison:

Format:

# Comparison Report

## PDF Findings

...

## Search Findings

...

## Similarities

...

## Differences

...

## Conclusion

7. If answer contains calculations:

Format:

# Calculation Result

Expression:
...

Answer:
...

8. Make answer look like ChatGPT.

Raw Answer:

{answer}
"""

        return self.llm.ask(prompt)

================================================================================
FILE: .history\agents\formatter_agent_20260608153010.py
================================================================================

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class FormatterAgent:

    def __init__(self):

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def format(self, answer):

        prompt = f"""
You are an expert AI teacher and content formatter.

Your job is to convert raw AI answers into
professional, easy-to-read responses.

Rules:

1. Always create a title.

2. Add:
   - Summary
   - Main Explanation
   - Key Points
   - Example (if applicable)
   - Conclusion

3. Use bullet points.

4. Preserve URLs exactly.

5. If answer contains search results:

Format:

# Search Results

## Top Sources

1. Title
   URL

2. Title
   URL

## Key Insights

...

## Recommendation

...

6. If answer contains comparison:

Format:

# Comparison Report

## PDF Findings

...

## Search Findings

...

## Similarities

...

## Differences

...

## Conclusion

7. If answer contains calculations:

Format:

# Calculation Result

Expression:
...

Answer:
...

8. Make answer look like ChatGPT.

Raw Answer:

{answer}
"""

        return self.llm.ask(prompt)

================================================================================
FILE: .history\agents\intent_agent_20260608161110.py
================================================================================

class IntentAgent:

    def detect(self, question):

        question = (
            question
            .lower()
            .strip()
        )

        if (
            "exam" in question
            or "2 marks" in question
            or "5 marks" in question
            or "unit" in question
        ):
            return "exam"

        elif (
            "interview" in question
            or "job" in question
        ):
            return "interview"

        elif (
            "compare" in question
        ):
            return "comparison"

        elif (
            "research" in question
        ):
            return "research"

        return "general"


================================================================================
FILE: .history\agents\intent_agent_20260608170901.py
================================================================================

class IntentAgent:

    def detect(self, question):
question = (
    question
    .lower()
    .strip()
)

if (
    "full form" in question
):
    return "short_answer"

if (
    "exam" in question
    or "2 marks" in question
    or "5 marks" in question
    or "unit" in question
):
    return "exam"

        elif (
            "interview" in question
            or "job" in question
        ):
            return "interview"

        elif (
            "compare" in question
        ):
            return "comparison"

        elif (
            "research" in question
        ):
            return "research"

        return "general"


================================================================================
FILE: .history\agents\intent_agent_20260608170906.py
================================================================================

class IntentAgent:

    def detect(self, question):
        question = (
            question
            .lower()
            .strip()
        )

        if (
            "full form" in question
        ):
            return "short_answer"

        if (
            "exam" in question
            or "2 marks" in question
            or "5 marks" in question
            or "unit" in question
        ):
    return "exam"

        elif (
            "interview" in question
            or "job" in question
        ):
            return "interview"

        elif (
            "compare" in question
        ):
            return "comparison"

        elif (
            "research" in question
        ):
            return "research"

        return "general"


================================================================================
FILE: .history\agents\intent_agent_20260608170908.py
================================================================================

class IntentAgent:

    def detect(self, question):
        question = (
            question
            .lower()
            .strip()
        )

        if (
            "full form" in question
        ):
            return "short_answer"

        if (
            "exam" in question
            or "2 marks" in question
            or "5 marks" in question
            or "unit" in question
        ):
    return "exam"

        elif (
            "interview" in question
            or "job" in question
        ):
            return "interview"

        elif (
            "compare" in question
        ):
            return "comparison"

        elif (
            "research" in question
        ):
            return "research"

        return "general"


================================================================================
FILE: .history\agents\intent_agent_20260608170911.py
================================================================================

class IntentAgent:

    def detect(self, question):
        question = (
            question
            .lower()
            .strip()
        )

        if (
            "full form" in question
        ):
            return "short_answer"

        if (
            "exam" in question
            or "2 marks" in question
            or "5 marks" in question
            or "unit" in question
        ):
         return "exam"

        elif (
            "interview" in question
            or "job" in question
        ):
            return "interview"

        elif (
            "compare" in question
        ):
            return "comparison"

        elif (
            "research" in question
        ):
            return "research"

        return "general"


================================================================================
FILE: .history\agents\intent_agent_20260608170912.py
================================================================================

class IntentAgent:

    def detect(self, question):
        question = (
            question
            .lower()
            .strip()
        )

        if (
            "full form" in question
        ):
            return "short_answer"

        if (
            "exam" in question
            or "2 marks" in question
            or "5 marks" in question
            or "unit" in question
        ):
         return "exam"

        elif (
            "interview" in question
            or "job" in question
        ):
            return "interview"

        elif (
            "compare" in question
        ):
            return "comparison"

        elif (
            "research" in question
        ):
            return "research"

        return "general"


================================================================================
FILE: .history\agents\intent_agent_20260608170918.py
================================================================================

class IntentAgent:

    def detect(self, question):
        question = (
            question
            .lower()
            .strip()
        )

        if (
            "full form" in question
        ):
            return "short_answer"

        if (
            "exam" in question
            or "2 marks" in question
            or "5 marks" in question
            or "unit" in question
        ):
         return "exam"

        elif (
            "interview" in question
            or "job" in question
        ):
            return "interview"

        elif (
            "compare" in question
        ):
            return "comparison"

        elif (
            "research" in question
        ):
            return "research"

        return "general"


================================================================================
FILE: .history\agents\math_agent_20260608002756.py
================================================================================



================================================================================
FILE: .history\agents\math_agent_20260608003758.py
================================================================================

from tools.calculator_tool import CalculatorTool


class MathAgent:

    def __init__(self):
        self.calculator = CalculatorTool()

    def run(self, question):
        return self.calculator.calculate(question)

================================================================================
FILE: .history\agents\memory_agent_20260608005547.py
================================================================================



================================================================================
FILE: .history\agents\memory_agent_20260608010008.py
================================================================================

from memory.memory import Memory


class MemoryAgent:

    def __init__(self):

        self.memory = Memory()

    def get_context(self):

        history = self.memory.get_history()

        context = ""

        for item in history[-10:]:

            context += (
                f"{item['role']}: "
                f"{item['message']}\n"
            )

        return context

================================================================================
FILE: .history\agents\planner_agent_20260608004625.py
================================================================================



================================================================================
FILE: .history\agents\planner_agent_20260608005006.py
================================================================================

class PlannerAgent:

    def plan(self, question):

        question = question.lower()

        tasks = []

        if any(
            word in question
            for word in [
                "compare",
                "comparison",
                "difference"
            ]
        ):

            tasks.append("search")
            tasks.append("rag")

            return tasks

        return []

================================================================================
FILE: .history\agents\planner_agent_20260608005007.py
================================================================================

class PlannerAgent:

    def plan(self, question):

        question = question.lower()

        tasks = []

        if any(
            word in question
            for word in [
                "compare",
                "comparison",
                "difference"
            ]
        ):

            tasks.append("search")
            tasks.append("rag")

            return tasks

        return []

================================================================================
FILE: .history\agents\rag_agent_20260608002807.py
================================================================================



================================================================================
FILE: .history\agents\rag_agent_20260608003616.py
================================================================================

from rag.rag_chat import RAGChat


class RAGAgent:

    def __init__(self):
        self.rag = RAGChat()

    def run(self, question):
        return self.rag.ask(question)

================================================================================
FILE: .history\agents\rag_agent_20260608003813.py
================================================================================

from rag.rag_chat import RAGChat


class RAGAgent:

    def __init__(self):
        self.rag = RAGChat()

    def run(self, question):
        return self.rag.ask(question)

================================================================================
FILE: .history\agents\reflection_agent_20260608004714.py
================================================================================



================================================================================
FILE: .history\agents\reflection_agent_20260608004952.py
================================================================================

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class ReflectionAgent:

    def __init__(self):

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def review(self, answer):

        prompt = f"""
You are a quality reviewer.

Review the answer below.

If the answer is already good,
return it unchanged.

Otherwise improve it.

Answer:

{answer}
"""

        return self.llm.ask(prompt)

================================================================================
FILE: .history\agents\reflection_agent_20260608004953.py
================================================================================

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class ReflectionAgent:

    def __init__(self):

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def review(self, answer):

        prompt = f"""
You are a quality reviewer.

Review the answer below.

If the answer is already good,
return it unchanged.

Otherwise improve it.

Answer:

{answer}
"""

        return self.llm.ask(prompt)

================================================================================
FILE: .history\agents\reflection_agent_20260608005000.py
================================================================================

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class ReflectionAgent:

    def __init__(self):

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def review(self, answer):

        prompt = f"""
You are a quality reviewer.

Review the answer below.

If the answer is already good,
return it unchanged.

Otherwise improve it.

Answer:

{answer}
"""

        return self.llm.ask(prompt)

================================================================================
FILE: .history\agents\registry_20260608004736.py
================================================================================



================================================================================
FILE: .history\agents\registry_20260608010019.py
================================================================================

from agents.search_agent import SearchAgent
from agents.math_agent import MathAgent
from agents.rag_agent import RAGAgent
from agents.research_agent import ResearchAgent


class AgentRegistry:

    def __init__(self):

        self.agents = {

            "search": SearchAgent(),

            "math": MathAgent(),

            "rag": RAGAgent(),

            "research": ResearchAgent()
        }

    def get_agent(self, name):

        return self.agents.get(name)

================================================================================
FILE: .history\agents\research_agent_20260608005631.py
================================================================================



================================================================================
FILE: .history\agents\research_agent_20260608005949.py
================================================================================

from agents.search_agent import SearchAgent


class ResearchAgent:

    def __init__(self):

        self.search_agent = SearchAgent()

    def research(self, topic):

        result = self.search_agent.run(topic)

        return f"""
RESEARCH REPORT

Topic:
{topic}

Findings:
{result}
"""

================================================================================
FILE: .history\agents\router_20260605002543.py
================================================================================



================================================================================
FILE: .history\agents\router_20260605002601.py
================================================================================

class Router:

    def route(self, question):

        question = question.lower()

        if any(op in question for op in ["+", "-", "*", "/"]):
            return "calculator"

        if "search" in question:
            return "search"

        return "knowledge"

================================================================================
FILE: .history\agents\router_20260605002602.py
================================================================================

class Router:

    def route(self, question):

        question = question.lower()

        if any(op in question for op in ["+", "-", "*", "/"]):
            return "calculator"

        if "search" in question:
            return "search"

        return "knowledge"

================================================================================
FILE: .history\agents\router_20260605002603.py
================================================================================

class Router:

    def route(self, question):

        question = question.lower()

        if any(op in question for op in ["+", "-", "*", "/"]):
            return "calculator"

        if "search" in question:
            return "search"

        return "knowledge"

================================================================================
FILE: .history\agents\router_20260607004421.py
================================================================================

class Router:

    def route(self, question):

        question = question.lower()

        if any(op in question for op in ["+", "-", "*", "/"]):
            return "calculator"

        if "search" in question:
            return "search"

        pdf_keywords = [
            "pdf",
            "document",
            "chapter",
            "notes",
            "summary",
            "summarize"
        ]

        if any(word in question for word in pdf_keywords):
            return "rag"

        return "knowledge"

================================================================================
FILE: .history\agents\router_20260607014656.py
================================================================================

class Router:

    def route(self, question):

        question = question.lower().strip()

        # Calculator
        if any(op in question for op in ["+", "-", "*", "/"]):
            return "calculator"

        # Search
        search_keywords = [
            "search",
            "latest",
            "news",
            "find",
            "lookup"
        ]

        if any(word in question for word in search_keywords):
            return "search"

        # PDF / RAG
        pdf_keywords = [
            "pdf",
            "document",
            "chapter",
            "notes",
            "summary",
            "summarize",
            "what are python",
            "python function",
            "python functions",
            "for loop",
            "while loop",
            "class in python",
            "classes in python"
        ]

        if any(word in question for word in pdf_keywords):
            return "rag"

        return "knowledge"

================================================================================
FILE: .history\agents\router_20260607014657.py
================================================================================

class Router:

    def route(self, question):

        question = question.lower().strip()

        # Calculator
        if any(op in question for op in ["+", "-", "*", "/"]):
            return "calculator"

        # Search
        search_keywords = [
            "search",
            "latest",
            "news",
            "find",
            "lookup"
        ]

        if any(word in question for word in search_keywords):
            return "search"

        # PDF / RAG
        pdf_keywords = [
            "pdf",
            "document",
            "chapter",
            "notes",
            "summary",
            "summarize",
            "what are python",
            "python function",
            "python functions",
            "for loop",
            "while loop",
            "class in python",
            "classes in python"
        ]

        if any(word in question for word in pdf_keywords):
            return "rag"

        return "knowledge"

================================================================================
FILE: .history\agents\router_20260608004017.py
================================================================================

import re


class Router:

    def route(self, question):

        question = question.lower().strip()

        if re.fullmatch(
            r"[0-9+\-*/(). ]+",
            question
        ):
            return "calculator"

        search_keywords = [
            "search",
            "latest",
            "news",
            "find",
            "lookup"
        ]

        if any(
            word in question
            for word in search_keywords
        ):
            return "search"

        pdf_keywords = [
            "pdf",
            "document",
            "chapter",
            "notes",
            "summary",
            "summarize"
        ]

        if any(
            word in question
            for word in pdf_keywords
        ):
            return "rag"

        return "knowledge"

================================================================================
FILE: .history\agents\router_20260608004020.py
================================================================================

import re


class Router:

    def route(self, question):

        question = question.lower().strip()

        if re.fullmatch(
            r"[0-9+\-*/(). ]+",
            question
        ):
            return "calculator"

        search_keywords = [
            "search",
            "latest",
            "news",
            "find",
            "lookup"
        ]

        if any(
            word in question
            for word in search_keywords
        ):
            return "search"

        pdf_keywords = [
            "pdf",
            "document",
            "chapter",
            "notes",
            "summary",
            "summarize"
        ]

        if any(
            word in question
            for word in pdf_keywords
        ):
            return "rag"

        return "knowledge"

================================================================================
FILE: .history\agents\router_20260608005114.py
================================================================================

import re


class Router:

    def route(self, question):

        question = question.lower().strip()

        if re.fullmatch(
            r"[0-9+\-*/(). ]+",
            question
        ):
            return "calculator"

        search_keywords = [
            "search",
            "latest",
            "news",
            "find",
            "lookup"
        ]

        if any(
            word in question
            for word in search_keywords
        ):
            return "search"

        pdf_keywords = [
            "pdf",
            "document",
            "chapter",
            "notes",
            "summary",
            "summarize"
        ]

        if any(
            word in question
            for word in pdf_keywords
        ):
            return "rag"

        comparison_keywords = [
            "compare",
            "comparison",
            "difference"
        ]

        if any(
            word in question
            for word in comparison_keywords
        ):
            return "multi"

        return "knowledge"

================================================================================
FILE: .history\agents\router_20260608005123.py
================================================================================

import re


class Router:

    def route(self, question):

        question = question.lower().strip()

        if re.fullmatch(
            r"[0-9+\-*/(). ]+",
            question
        ):
            return "calculator"

        search_keywords = [
            "search",
            "latest",
            "news",
            "find",
            "lookup"
        ]

        if any(
            word in question
            for word in search_keywords
        ):
            return "search"

        pdf_keywords = [
            "pdf",
            "document",
            "chapter",
            "notes",
            "summary",
            "summarize"
        ]

        if any(
            word in question
            for word in pdf_keywords
        ):
            return "rag"

        comparison_keywords = [
            "compare",
            "comparison",
            "difference"
        ]

        if any(
            word in question
            for word in comparison_keywords
        ):
            return "multi"

        return "knowledge"

================================================================================
FILE: .history\agents\search_agent_20260608002729.py
================================================================================



================================================================================
FILE: .history\agents\search_agent_20260608003804.py
================================================================================

from tools.search_tool import SearchTool


class SearchAgent:

    def __init__(self):
        self.search_tool = SearchTool()

    def run(self, question):
        return self.search_tool.search(question)

================================================================================
FILE: .history\agents\search_agent_20260608003805.py
================================================================================

from tools.search_tool import SearchTool


class SearchAgent:

    def __init__(self):
        self.search_tool = SearchTool()

    def run(self, question):
        return self.search_tool.search(question)

================================================================================
FILE: .history\agents\student_agent_20260604232357.py
================================================================================



================================================================================
FILE: .history\agents\student_agent_20260604234134.py
================================================================================



================================================================================
FILE: .history\agents\student_agent_20260604234623.py
================================================================================

from agents.teacher_agent import TeacherAgent

class StudentAgent:

    def __init__(self):
        self.teacher = TeacherAgent()

    def ask_teacher(self, question):
        return self.teacher.answer(question)

================================================================================
FILE: .history\agents\supervisor_agent_20260608002705.py
================================================================================



================================================================================
FILE: .history\agents\supervisor_agent_20260608002818.py
================================================================================

class SupervisorAgent:

    def route(self, query):

        if is_math(query):
            return math_agent.run(query)

        elif is_search(query):
            return search_agent.run(query)

        elif is_pdf(query):
            return rag_agent.run(query)

        else:
            return teacher_agent.run(query)

================================================================================
FILE: .history\agents\supervisor_agent_20260608002819.py
================================================================================

class SupervisorAgent:

    def route(self, query):

        if is_math(query):
            return math_agent.run(query)

        elif is_search(query):
            return search_agent.run(query)

        elif is_pdf(query):
            return rag_agent.run(query)

        else:
            return teacher_agent.run(query)

================================================================================
FILE: .history\agents\supervisor_agent_20260608003628.py
================================================================================

from agents.rag_agent import RAGAgent


class SupervisorAgent:

    def __init__(self):

        self.rag_agent = RAGAgent()

    def handle(self, route, question):

        if route == "rag":
            return self.rag_agent.run(question)

        return None

================================================================================
FILE: .history\agents\supervisor_agent_20260608003629.py
================================================================================

from agents.rag_agent import RAGAgent


class SupervisorAgent:

    def __init__(self):

        self.rag_agent = RAGAgent()

    def handle(self, route, question):

        if route == "rag":
            return self.rag_agent.run(question)

        return None

================================================================================
FILE: .history\agents\supervisor_agent_20260608003820.py
================================================================================

from agents.math_agent import MathAgent
from agents.search_agent import SearchAgent
from agents.rag_agent import RAGAgent


class SupervisorAgent:

    def __init__(self):

        self.math_agent = MathAgent()
        self.search_agent = SearchAgent()
        self.rag_agent = RAGAgent()

    def handle(self, route, question):

        if route == "calculator":
            return self.math_agent.run(question)

        elif route == "search":
            return self.search_agent.run(question)

        elif route == "rag":
            return self.rag_agent.run(question)

        return None

================================================================================
FILE: .history\agents\supervisor_agent_20260608005015.py
================================================================================

from agents.math_agent import MathAgent
from agents.search_agent import SearchAgent
from agents.rag_agent import RAGAgent

from agents.planner_agent import PlannerAgent
from agents.combiner_agent import CombinerAgent
from agents.reflection_agent import ReflectionAgent


class SupervisorAgent:

    def __init__(self):

        self.math_agent = MathAgent()
        self.search_agent = SearchAgent()
        self.rag_agent = RAGAgent()

        self.planner = PlannerAgent()
        self.combiner = CombinerAgent()
        self.reflection = ReflectionAgent()

    def handle(self, route, question):

        planned = self.planner.plan(
            question
        )

        if planned:

            results = {}

            for task in planned:

                if task == "search":

                    results["search"] = (
                        self.search_agent.run(
                            question
                        )
                    )

                elif task == "rag":

                    results["rag"] = (
                        self.rag_agent.run(
                            question
                        )
                    )

            combined = (
                self.combiner.combine(
                    results
                )
            )

            return self.reflection.review(
                combined
            )

        if route == "calculator":

            return self.math_agent.run(
                question
            )

        elif route == "search":

            return self.search_agent.run(
                question
            )

        elif route == "rag":

            return self.rag_agent.run(
                question
            )

        return None

================================================================================
FILE: .history\agents\supervisor_agent_20260608153038.py
================================================================================

import json
import time
from concurrent.futures import ThreadPoolExecutor
from datetime import datetime
from pathlib import Path

from agents.math_agent import MathAgent
from agents.search_agent import SearchAgent
from agents.rag_agent import RAGAgent

from agents.planner_agent import PlannerAgent
from agents.combiner_agent import CombinerAgent
from agents.reflection_agent import ReflectionAgent
from agents.critic_agent import CriticAgent
from agents.formatter_agent import FormatterAgent

class SupervisorAgent:

    def __init__(self):

        self.math_agent = MathAgent()
        self.search_agent = SearchAgent()
        self.rag_agent = RAGAgent()

        self.planner = PlannerAgent()
        self.combiner = CombinerAgent()
        self.reflection = ReflectionAgent()
        self.critic = CriticAgent()
        self.last_run = {}

    def log_run(self, payload):
        logs_dir = Path("logs")
        logs_dir.mkdir(exist_ok=True)

        with (logs_dir / "agent_runs.jsonl").open("a", encoding="utf-8") as file:
            file.write(
                json.dumps(
                    payload,
                    ensure_ascii=False
                )
            )
            file.write("\n")

    def handle(self, route, question):
        started_at = time.perf_counter()

        planned = self.planner.plan(
            question
        )

        if planned:

            search_query = planned["search"]
            rag_query = planned["rag"]

            with ThreadPoolExecutor() as executor:

                search_future = executor.submit(
                    self.search_agent.run,
                    search_query
                )

                rag_future = executor.submit(
                    self.rag_agent.run,
                    rag_query
                )

                search_result = search_future.result()
                rag_result = rag_future.result()

            results = {
                "search": search_result,
                "rag": rag_result
            }

            combined = (
                self.combiner.combine(
                    results
                )
            )

            reflected = self.reflection.review(
                search_result,
                rag_result
            )

            final_answer = self.critic.critique(
                reflected
            )

            execution_time = round(time.perf_counter() - started_at, 2)
            self.last_run = {
                "query": question,
                "route": "multi",
                "agents": ["SearchAgent", "RAGAgent", "ReflectionAgent", "CriticAgent"],
                "execution_time": execution_time,
                "retrieved_chunks": "PDF result generated"
            }
            self.log_run(
                {
                    **self.last_run,
                    "timestamp": datetime.now().isoformat(timespec="seconds")
                }
            )

            return final_answer

        if route == "calculator":

            result = self.math_agent.run(
                question
            )
            agents = ["MathAgent"]

        elif route == "search":

            result = self.search_agent.run(
                question
            )
            agents = ["SearchAgent"]

        elif route == "rag":

            result = self.rag_agent.run(
                question
            )
            agents = ["RAGAgent"]

        else:
            result = None
            agents = []

        execution_time = round(time.perf_counter() - started_at, 2)
        self.last_run = {
            "query": question,
            "route": route,
            "agents": agents,
            "execution_time": execution_time,
            "retrieved_chunks": "N/A"
        }
        self.log_run(
            {
                **self.last_run,
                "timestamp": datetime.now().isoformat(timespec="seconds")
            }
        )

        return result


================================================================================
FILE: .history\agents\teacher_agent_20260604232344.py
================================================================================



================================================================================
FILE: .history\agents\teacher_agent_20260604234134.py
================================================================================



================================================================================
FILE: .history\agents\teacher_agent_20260604234653.py
================================================================================

class TeacherAgent:

    def answer(self, question):

        question = question.lower()

        if "ai" in question:
            return "AI means Artificial Intelligence."

        elif "python" in question:
            return "Python is a popular programming language."

        elif "agent" in question:
            return "An AI Agent can think, plan and use tools."

        else:
            return "Sorry, I don't know that yet."

================================================================================
FILE: .history\agents\teacher_agent_20260604234654.py
================================================================================

class TeacherAgent:

    def answer(self, question):

        question = question.lower()

        if "ai" in question:
            return "AI means Artificial Intelligence."

        elif "python" in question:
            return "Python is a popular programming language."

        elif "agent" in question:
            return "An AI Agent can think, plan and use tools."

        else:
            return "Sorry, I don't know that yet."

================================================================================
FILE: .history\agents\teacher_agent_20260604235207.py
================================================================================

from memory.memory import Memory

class TeacherAgent:

    def __init__(self):
        self.memory = Memory()

    def answer(self, question):

        self.memory.save("student", question)

        question = question.lower()

        if "ai" in question:
            response = "AI means Artificial Intelligence."

        elif "python" in question:
            response = "Python is a popular programming language."

        elif "agent" in question:
            response = "An AI Agent can think, plan and use tools."

        else:
            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260604235208.py
================================================================================

from memory.memory import Memory

class TeacherAgent:

    def __init__(self):
        self.memory = Memory()

    def answer(self, question):

        self.memory.save("student", question)

        question = question.lower()

        if "ai" in question:
            response = "AI means Artificial Intelligence."

        elif "python" in question:
            response = "Python is a popular programming language."

        elif "agent" in question:
            response = "An AI Agent can think, plan and use tools."

        else:
            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260604235210.py
================================================================================

from memory.memory import Memory

class TeacherAgent:

    def __init__(self):
        self.memory = Memory()

    def answer(self, question):

        self.memory.save("student", question)

        question = question.lower()

        if "ai" in question:
            response = "AI means Artificial Intelligence."

        elif "python" in question:
            response = "Python is a popular programming language."

        elif "agent" in question:
            response = "An AI Agent can think, plan and use tools."

        else:
            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260604235432.py
================================================================================

from memory.memory import Memory

class TeacherAgent:

    def __init__(self):
        self.memory = Memory()

    def answer(self, question):

        self.memory.save("student", question)

        question = question.lower()

        if "ai" in question:
            response = "AI means Artificial Intelligence."

        elif "python" in question:
            response = "Python is a popular programming language."

        elif "agent" in question:
            response = "An AI Agent can think, plan and use tools."

        else:
            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605000440.py
================================================================================

from memory.memory import Memory
from memory.memory import Memory
from tools.calculator_tool import CalculatorTool

class TeacherAgent:

    def __init__(self):
        self.memory = Memory()

    def answer(self, question):

        self.memory.save("student", question)

        question = question.lower()

        if "ai" in question:
            response = "AI means Artificial Intelligence."

        elif "python" in question:
            response = "Python is a popular programming language."

        elif "agent" in question:
            response = "An AI Agent can think, plan and use tools."

        else:
            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605000446.py
================================================================================

from memory.memory import Memory

from tools.calculator_tool import CalculatorTool

class TeacherAgent:

    def __init__(self):
        self.memory = Memory()

    def answer(self, question):

        self.memory.save("student", question)

        question = question.lower()

        if "ai" in question:
            response = "AI means Artificial Intelligence."

        elif "python" in question:
            response = "Python is a popular programming language."

        elif "agent" in question:
            response = "An AI Agent can think, plan and use tools."

        else:
            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605000447.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool

class TeacherAgent:

    def __init__(self):
        self.memory = Memory()

    def answer(self, question):

        self.memory.save("student", question)

        question = question.lower()

        if "ai" in question:
            response = "AI means Artificial Intelligence."

        elif "python" in question:
            response = "Python is a popular programming language."

        elif "agent" in question:
            response = "An AI Agent can think, plan and use tools."

        else:
            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605000503.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool

class TeacherAgent:

    def __init__(self):
    self.memory = Memory()
    self.calculator = CalculatorTool()

    def answer(self, question):

        self.memory.save("student", question)

        question = question.lower()

        if "ai" in question:
            response = "AI means Artificial Intelligence."

        elif "python" in question:
            response = "Python is a popular programming language."

        elif "agent" in question:
            response = "An AI Agent can think, plan and use tools."

        else:
            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605000504.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool

class TeacherAgent:

    def __init__(self):
    self.memory = Memory()
    self.calculator = CalculatorTool()

    def answer(self, question):

        self.memory.save("student", question)

        question = question.lower()

        if "ai" in question:
            response = "AI means Artificial Intelligence."

        elif "python" in question:
            response = "Python is a popular programming language."

        elif "agent" in question:
            response = "An AI Agent can think, plan and use tools."

        else:
            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605000631.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool


class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

    def answer(self, question):

        self.memory.save("student", question)

        # Calculator Tool
        if any(op in question for op in ["+", "-", "*", "/"]):

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605001109.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool

class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

    def answer(self, question):

        self.memory.save("student", question)

        # Calculator Tool
        if any(op in question for op in ["+", "-", "*", "/"]):

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605001110.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool

class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

    def answer(self, question):

        self.memory.save("student", question)

        # Calculator Tool
        if any(op in question for op in ["+", "-", "*", "/"]):

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605001130.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool

class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

    def answer(self, question):

        self.memory.save("student", question)

        # Calculator Tool
        if any(op in question for op in ["+", "-", "*", "/"]):

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605001131.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool

class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

    def answer(self, question):

        self.memory.save("student", question)

        # Calculator Tool
        if any(op in question for op in ["+", "-", "*", "/"]):

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605001152.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool

class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

    def answer(self, question):

        self.memory.save("student", question)

        # Calculator Tool
        if any(op in question for op in ["+", "-", "*", "/"]):

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605001314.py
================================================================================

self.search_tool = SearchTool()

================================================================================
FILE: .history\agents\teacher_agent_20260605001319.py
================================================================================

self.search_tool = SearchTool()from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool


class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

    def answer(self, question):

        # Student question save
        self.memory.save("student", question)

        # -------------------------
        # Calculator Tool
        # -------------------------
        if any(op in question for op in ["+", "-", "*", "/"]):

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # -------------------------
        # Search Tool
        # -------------------------
        if "search" in question.lower():

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # -------------------------
        # Knowledge Responses
        # -------------------------
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        # Save teacher answer
        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605001320.py
================================================================================

self.search_tool = SearchTool()from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool


class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

    def answer(self, question):

        # Student question save
        self.memory.save("student", question)

        # -------------------------
        # Calculator Tool
        # -------------------------
        if any(op in question for op in ["+", "-", "*", "/"]):

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # -------------------------
        # Search Tool
        # -------------------------
        if "search" in question.lower():

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # -------------------------
        # Knowledge Responses
        # -------------------------
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        # Save teacher answer
        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605001535.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool


class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

    def answer(self, question):

        # Save student message
        self.memory.save("student", question)

        # Calculator Tool
        if any(op in question for op in ["+", "-", "*", "/"]):

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Tool
        if "search" in question.lower():

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Responses
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605001536.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool


class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

    def answer(self, question):

        # Save student message
        self.memory.save("student", question)

        # Calculator Tool
        if any(op in question for op in ["+", "-", "*", "/"]):

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Tool
        if "search" in question.lower():

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Responses
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605002623.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router

class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

    def answer(self, question):

        # Save student message
        self.memory.save("student", question)

        # Calculator Tool
        if any(op in question for op in ["+", "-", "*", "/"]):

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Tool
        if "search" in question.lower():

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Responses
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605002754.py
================================================================================

```python
from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router


class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response
```


================================================================================
FILE: .history\agents\teacher_agent_20260605002812.py
================================================================================

```python
from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router


class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response


================================================================================
FILE: .history\agents\teacher_agent_20260605002818.py
================================================================================

```python
from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router


class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response


================================================================================
FILE: .history\agents\teacher_agent_20260605002951.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router


class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234116.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234122.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234123.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234125.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234126.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234200.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

       else:
    response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234204.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

       else:
        
    response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234206.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

       else:

        response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234209.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

        response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234212.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

     response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234215.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234217.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234221.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234455.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234456.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234503.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234548.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

           self.llm = NvidiaClient(NVIDIA_API_KEY)

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234554.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

       self.llm = NvidiaClient(NVIDIA_API_KEY)

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234555.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

        self.llm = NvidiaClient(NVIDIA_API_KEY)

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234556.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

        self.llm = NvidiaClient(NVIDIA_API_KEY)

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605235218.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class TeacherAgent:

    def __init__(self):

        self.memory = Memory()
        self.calculator = CalculatorTool()
        self.search_tool = SearchTool()
        self.router = Router()

        self.llm = NvidiaClient(NVIDIA_API_KEY)

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        elif route == "search":

            result = self.search_tool.search(question)

            self.memory.save("teacher", result)

            return result

        response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605235219.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class TeacherAgent:

    def __init__(self):

        self.memory = Memory()
        self.calculator = CalculatorTool()
        self.search_tool = SearchTool()
        self.router = Router()

        self.llm = NvidiaClient(NVIDIA_API_KEY)

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        elif route == "search":

            result = self.search_tool.search(question)

            self.memory.save("teacher", result)

            return result

        response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260606000701.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

```
def __init__(self):

    self.memory = Memory()
    self.calculator = CalculatorTool()
    self.search_tool = SearchTool()
    self.router = Router()

    self.llm = NvidiaClient(NVIDIA_API_KEY)

def answer(self, question):

    # Save student message
    self.memory.save("student", question)

    route = self.router.route(question)

    # Calculator Tool
    if route == "calculator":

        result = self.calculator.calculate(question)

        response = f"Answer = {result}"

        self.memory.save("teacher", response)

        return response

    # Search Tool
    elif route == "search":

        result = self.search_tool.search(question)

        self.memory.save("teacher", result)

        return result

    # ==========================
    # Memory Context
    # ==========================

    history = self.memory.get_history()

    context = ""

    for item in history[-10:]:

        context += f"{item['role']}: {item['message']}\n"

    full_prompt = f"""
```

Previous Conversation:

{context}

Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

```
    response = self.llm.ask(full_prompt)

    self.memory.save("teacher", response)

    return response
```


================================================================================
FILE: .history\agents\teacher_agent_20260606000951.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

```
dvef __init__(self):

    self.memory = Memory()
    self.calculator = CalculatorTool()
    self.search_tool = SearchTool()
    self.router = Router()

    self.llm = NvidiaClient(NVIDIA_API_KEY)

def answer(self, question):

    # Save student message
    self.memory.save("student", question)

    route = self.router.route(question)

    # Calculator Tool
    if route == "calculator":

        result = self.calculator.calculate(question)

        response = f"Answer = {result}"

        self.memory.save("teacher", response)

        return response

    # Search Tool
    elif route == "search":

        result = self.search_tool.search(question)

        self.memory.save("teacher", result)

        return result

    # ==========================
    # Memory Context
    # ==========================

    history = self.memory.get_history()

    context = ""

    for item in history[-10:]:

        context += f"{item['role']}: {item['message']}\n"

    full_prompt = f"""
```

Previous Conversation:

{context}

Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

```
    response = self.llm.ask(full_prompt)

    self.memory.save("teacher", response)

    return response
```


================================================================================
FILE: .history\agents\teacher_agent_20260606000952.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

```
def __init__(self):

    self.memory = Memory()
    self.calculator = CalculatorTool()
    self.search_tool = SearchTool()
    self.router = Router()

    self.llm = NvidiaClient(NVIDIA_API_KEY)

def answer(self, question):

    # Save student message
    self.memory.save("student", question)

    route = self.router.route(question)

    # Calculator Tool
    if route == "calculator":

        result = self.calculator.calculate(question)

        response = f"Answer = {result}"

        self.memory.save("teacher", response)

        return response

    # Search Tool
    elif route == "search":

        result = self.search_tool.search(question)

        self.memory.save("teacher", result)

        return result

    # ==========================
    # Memory Context
    # ==========================

    history = self.memory.get_history()

    context = ""

    for item in history[-10:]:

        context += f"{item['role']}: {item['message']}\n"

    full_prompt = f"""
```

Previous Conversation:

{context}

Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

```
    response = self.llm.ask(full_prompt)

    self.memory.save("teacher", response)

    return response
```


================================================================================
FILE: .history\agents\teacher_agent_20260606000954.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

``` 
def __init__(self):

    self.memory = Memory()
    self.calculator = CalculatorTool()
    self.search_tool = SearchTool()
    self.router = Router()

    self.llm = NvidiaClient(NVIDIA_API_KEY)

def answer(self, question):

    # Save student message
    self.memory.save("student", question)

    route = self.router.route(question)

    # Calculator Tool
    if route == "calculator":

        result = self.calculator.calculate(question)

        response = f"Answer = {result}"

        self.memory.save("teacher", response)

        return response

    # Search Tool
    elif route == "search":

        result = self.search_tool.search(question)

        self.memory.save("teacher", result)

        return result

    # ==========================
    # Memory Context
    # ==========================

    history = self.memory.get_history()

    context = ""

    for item in history[-10:]:

        context += f"{item['role']}: {item['message']}\n"

    full_prompt = f"""
```

Previous Conversation:

{context}

Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

```
    response = self.llm.ask(full_prompt)

    self.memory.save("teacher", response)

    return response
```


================================================================================
FILE: .history\agents\teacher_agent_20260606000955.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

``` 
  def __init__(self):

    self.memory = Memory()
    self.calculator = CalculatorTool()
    self.search_tool = SearchTool()
    self.router = Router()

    self.llm = NvidiaClient(NVIDIA_API_KEY)

def answer(self, question):

    # Save student message
    self.memory.save("student", question)

    route = self.router.route(question)

    # Calculator Tool
    if route == "calculator":

        result = self.calculator.calculate(question)

        response = f"Answer = {result}"

        self.memory.save("teacher", response)

        return response

    # Search Tool
    elif route == "search":

        result = self.search_tool.search(question)

        self.memory.save("teacher", result)

        return result

    # ==========================
    # Memory Context
    # ==========================

    history = self.memory.get_history()

    context = ""

    for item in history[-10:]:

        context += f"{item['role']}: {item['message']}\n"

    full_prompt = f"""
```

Previous Conversation:

{context}

Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

```
    response = self.llm.ask(full_prompt)

    self.memory.save("teacher", response)

    return response
```


================================================================================
FILE: .history\agents\teacher_agent_20260606000958.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

``` def __init__(self):

    self.memory = Memory()
    self.calculator = CalculatorTool()
    self.search_tool = SearchTool()
    self.router = Router()

    self.llm = NvidiaClient(NVIDIA_API_KEY)

def answer(self, question):

    # Save student message
    self.memory.save("student", question)

    route = self.router.route(question)

    # Calculator Tool
    if route == "calculator":

        result = self.calculator.calculate(question)

        response = f"Answer = {result}"

        self.memory.save("teacher", response)

        return response

    # Search Tool
    elif route == "search":

        result = self.search_tool.search(question)

        self.memory.save("teacher", result)

        return result

    # ==========================
    # Memory Context
    # ==========================

    history = self.memory.get_history()

    context = ""

    for item in history[-10:]:

        context += f"{item['role']}: {item['message']}\n"

    full_prompt = f"""
```

Previous Conversation:

{context}

Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

```
    response = self.llm.ask(full_prompt)

    self.memory.save("teacher", response)

    return response
```


================================================================================
FILE: .history\agents\teacher_agent_20260606001001.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

```
def __init__(self):

    self.memory = Memory()
    self.calculator = CalculatorTool()
    self.search_tool = SearchTool()
    self.router = Router()

    self.llm = NvidiaClient(NVIDIA_API_KEY)

def answer(self, question):

    # Save student message
    self.memory.save("student", question)

    route = self.router.route(question)

    # Calculator Tool
    if route == "calculator":

        result = self.calculator.calculate(question)

        response = f"Answer = {result}"

        self.memory.save("teacher", response)

        return response

    # Search Tool
    elif route == "search":

        result = self.search_tool.search(question)

        self.memory.save("teacher", result)

        return result

    # ==========================
    # Memory Context
    # ==========================

    history = self.memory.get_history()

    context = ""

    for item in history[-10:]:

        context += f"{item['role']}: {item['message']}\n"

    full_prompt = f"""
```

Previous Conversation:

{context}

Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

```
    response = self.llm.ask(full_prompt)

    self.memory.save("teacher", response)

    return response
```


================================================================================
FILE: .history\agents\teacher_agent_20260606001014.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

```
def __init__(self):

    self.memory = Memory()
    self.calculator = CalculatorTool()
    self.search_tool = SearchTool()
    self.router = Router()

    self.llm = NvidiaClient(NVIDIA_API_KEY)

def answer(self, question):

    self.memory.save("student", question)

    route = self.router.route(question)

    if route == "calculator":

        result = self.calculator.calculate(question)

        response = f"Answer = {result}"

        self.memory.save("teacher", response)

        return response

    elif route == "search":

        result = self.search_tool.search(question)

        self.memory.save("teacher", result)

        return result

    history = self.memory.get_history()

    context = ""

    for item in history[-10:]:

        context += f"{item['role']}: {item['message']}\n"

    full_prompt = f"""
```

Previous Conversation:

{context}

Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

```
    response = self.llm.ask(full_prompt)

    self.memory.save("teacher", response)

    return response
```


================================================================================
FILE: .history\agents\teacher_agent_20260606001015.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

```
def __init__(self):

    self.memory = Memory()
    self.calculator = CalculatorTool()
    self.search_tool = SearchTool()
    self.router = Router()

    self.llm = NvidiaClient(NVIDIA_API_KEY)

def answer(self, question):

    self.memory.save("student", question)

    route = self.router.route(question)

    if route == "calculator":

        result = self.calculator.calculate(question)

        response = f"Answer = {result}"

        self.memory.save("teacher", response)

        return response

    elif route == "search":

        result = self.search_tool.search(question)

        self.memory.save("teacher", result)

        return result

    history = self.memory.get_history()

    context = ""

    for item in history[-10:]:

        context += f"{item['role']}: {item['message']}\n"

    full_prompt = f"""
```

Previous Conversation:

{context}

Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

```
    response = self.llm.ask(full_prompt)

    self.memory.save("teacher", response)

    return response
```


================================================================================
FILE: .history\agents\teacher_agent_20260606001110.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class TeacherAgent:

    def __init__(self):
        self.memory = Memory()
        self.calculator = CalculatorTool()
        self.search_tool = SearchTool()
        self.router = Router()
        self.llm = NvidiaClient(NVIDIA_API_KEY)

    def answer(self, question):
        self.memory.save("student", question)

        route = self.router.route(question)

        if route == "calculator":
            result = self.calculator.calculate(question)
            response = f"Answer = {result}"
            self.memory.save("teacher", response)
            return response

        elif route == "search":
            result = self.search_tool.search(question)
            self.memory.save("teacher", result)
            return result

        history = self.memory.get_history()
        context = ""
        for item in history[-10:]:
            context += f"{item['role']}: {item['message']}\n"

        full_prompt = f"""
Previous Conversation:

{context}
Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

        response = self.llm.ask(full_prompt)
        self.memory.save("teacher", response)
        return response


================================================================================
FILE: .history\agents\teacher_agent_20260606001425.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class TeacherAgent:

    def __init__(self):
        self.memory = Memory()
        self.calculator = CalculatorTool()
        self.search_tool = SearchTool()
        self.router = Router()
        self.llm = NvidiaClient(NVIDIA_API_KEY)

    def answer(self, question):
        self.memory.save("student", question)

        route = self.router.route(question)

        if route == "calculator":
            result = self.calculator.calculate(question)
            response = f"Answer = {result}"
            self.memory.save("teacher", response)
            return response

        elif route == "search":
            result = self.search_tool.search(question)
            self.memory.save("teacher", result)
            return result

        history = self.memory.get_history()
        context = ""
        for item in history[-10:]:
            context += f"{item['role']}: {item['message']}\n"

        full_prompt = f"""
Previous Conversation:

{context}
Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

        response = self.llm.ask(full_prompt)
        self.memory.save("teacher", response)
        return response


================================================================================
FILE: .history\agents\teacher_agent_20260607003927.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
from rag.rag_chat import RAGChat

class TeacherAgent:

    def __init__(self):
        self.memory = Memory()
        self.calculator = CalculatorTool()
        self.search_tool = SearchTool()
        self.router = Router()
        self.llm = NvidiaClient(NVIDIA_API_KEY)

    def answer(self, question):
        self.memory.save("student", question)

        route = self.router.route(question)

        if route == "calculator":
            result = self.calculator.calculate(question)
            response = f"Answer = {result}"
            self.memory.save("teacher", response)
            return response

        elif route == "search":
            result = self.search_tool.search(question)
            self.memory.save("teacher", result)
            return result

        history = self.memory.get_history()
        context = ""
        for item in history[-10:]:
            context += f"{item['role']}: {item['message']}\n"

        full_prompt = f"""
Previous Conversation:

{context}
Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

        response = self.llm.ask(full_prompt)
        self.memory.save("teacher", response)
        return response


================================================================================
FILE: .history\agents\teacher_agent_20260607003928.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
from rag.rag_chat import RAGChat

class TeacherAgent:

    def __init__(self):
        self.memory = Memory()
        self.calculator = CalculatorTool()
        self.search_tool = SearchTool()
        self.router = Router()
        self.llm = NvidiaClient(NVIDIA_API_KEY)

    def answer(self, question):
        self.memory.save("student", question)

        route = self.router.route(question)

        if route == "calculator":
            result = self.calculator.calculate(question)
            response = f"Answer = {result}"
            self.memory.save("teacher", response)
            return response

        elif route == "search":
            result = self.search_tool.search(question)
            self.memory.save("teacher", result)
            return result

        history = self.memory.get_history()
        context = ""
        for item in history[-10:]:
            context += f"{item['role']}: {item['message']}\n"

        full_prompt = f"""
Previous Conversation:

{context}
Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

        response = self.llm.ask(full_prompt)
        self.memory.save("teacher", response)
        return response


================================================================================
FILE: .history\agents\teacher_agent_20260608003833.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
from rag.rag_chat import RAGChat
from agents.supervisor_agent import SupervisorAgent

class TeacherAgent:

    def __init__(self):
        self.memory = Memory()
        self.calculator = CalculatorTool()
        self.search_tool = SearchTool()
        self.router = Router()
        self.llm = NvidiaClient(NVIDIA_API_KEY)
        self.rag = RAGChat()

    def answer(self, question):
        self.memory.save("student", question)

        route = self.router.route(question)

        if route == "calculator":
            result = self.calculator.calculate(question)
            response = f"Answer = {result}"
            self.memory.save("teacher", response)
            return response

        elif route == "search":
            result = self.search_tool.search(question)
            self.memory.save("teacher", result)
            return result

        elif route == "rag":
            result = self.rag.ask(question)
            self.memory.save("teacher", result)
            return result

        history = self.memory.get_history()
        context = ""
        for item in history[-10:]:
            context += f"{item['role']}: {item['message']}\n"

        full_prompt = f"""
Previous Conversation:

{context}
Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

        response = self.llm.ask(full_prompt)
        self.memory.save("teacher", response)
        return response


================================================================================
FILE: .history\agents\teacher_agent_20260608003834.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
from rag.rag_chat import RAGChat
from agents.supervisor_agent import SupervisorAgent

class TeacherAgent:

    def __init__(self):
        self.memory = Memory()
        self.calculator = CalculatorTool()
        self.search_tool = SearchTool()
        self.router = Router()
        self.llm = NvidiaClient(NVIDIA_API_KEY)
        self.rag = RAGChat()

    def answer(self, question):
        self.memory.save("student", question)

        route = self.router.route(question)

        if route == "calculator":
            result = self.calculator.calculate(question)
            response = f"Answer = {result}"
            self.memory.save("teacher", response)
            return response

        elif route == "search":
            result = self.search_tool.search(question)
            self.memory.save("teacher", result)
            return result

        elif route == "rag":
            result = self.rag.ask(question)
            self.memory.save("teacher", result)
            return result

        history = self.memory.get_history()
        context = ""
        for item in history[-10:]:
            context += f"{item['role']}: {item['message']}\n"

        full_prompt = f"""
Previous Conversation:

{context}
Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

        response = self.llm.ask(full_prompt)
        self.memory.save("teacher", response)
        return response


================================================================================
FILE: .history\agents\teacher_agent_20260608003846.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
from rag.rag_chat import RAGChat
from agents.supervisor_agent import SupervisorAgent

class TeacherAgent:

    def __init__(self):
        self.memory = Memory()
        self.calculator = CalculatorTool()
        self.search_tool = SearchTool()
        self.router = Router()
        self.llm = NvidiaClient(NVIDIA_API_KEY)
        self.rag = RAGChat()
        self.supervisor = SupervisorAgent()

    def answer(self, question):
        self.memory.save("student", question)

        route = self.router.route(question)

        if route == "calculator":
            result = self.calculator.calculate(question)
            response = f"Answer = {result}"
            self.memory.save("teacher", response)
            return response

        elif route == "search":
            result = self.search_tool.search(question)
            self.memory.save("teacher", result)
            return result

        elif route == "rag":
            result = self.rag.ask(question)
            self.memory.save("teacher", result)
            return result

        history = self.memory.get_history()
        context = ""
        for item in history[-10:]:
            context += f"{item['role']}: {item['message']}\n"

        full_prompt = f"""
Previous Conversation:

{context}
Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

        response = self.llm.ask(full_prompt)
        self.memory.save("teacher", response)
        return response


================================================================================
FILE: .history\agents\teacher_agent_20260608003847.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
from rag.rag_chat import RAGChat
from agents.supervisor_agent import SupervisorAgent

class TeacherAgent:

    def __init__(self):
        self.memory = Memory()
        self.calculator = CalculatorTool()
        self.search_tool = SearchTool()
        self.router = Router()
        self.llm = NvidiaClient(NVIDIA_API_KEY)
        self.rag = RAGChat()
        self.supervisor = SupervisorAgent()

    def answer(self, question):
        self.memory.save("student", question)

        route = self.router.route(question)

        if route == "calculator":
            result = self.calculator.calculate(question)
            response = f"Answer = {result}"
            self.memory.save("teacher", response)
            return response

        elif route == "search":
            result = self.search_tool.search(question)
            self.memory.save("teacher", result)
            return result

        elif route == "rag":
            result = self.rag.ask(question)
            self.memory.save("teacher", result)
            return result

        history = self.memory.get_history()
        context = ""
        for item in history[-10:]:
            context += f"{item['role']}: {item['message']}\n"

        full_prompt = f"""
Previous Conversation:

{context}
Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

        response = self.llm.ask(full_prompt)
        self.memory.save("teacher", response)
        return response


================================================================================
FILE: .history\agents\teacher_agent_20260608004008.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
from rag.rag_chat import RAGChat
from agents.supervisor_agent import SupervisorAgent

class TeacherAgent:

    def __init__(self):
        self.memory = Memory()
        self.calculator = CalculatorTool()
        self.search_tool = SearchTool()
        self.router = Router()
        self.llm = NvidiaClient(NVIDIA_API_KEY)
        self.rag = RAGChat()
        self.supervisor = SupervisorAgent()

    def answer(self, question):
        self.memory.save("student", question)

        route = self.router.route(question)

        if route in ["calculator", "search", "rag"]:
            result = self.supervisor.handle(
                route,
                question
            )

            self.memory.save(
                "teacher",
                str(result)
            )

            return str(result)

        history = self.memory.get_history()
        context = ""
        for item in history[-10:]:
            context += f"{item['role']}: {item['message']}\n"

        full_prompt = f"""
Previous Conversation:

{context}
Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

        response = self.llm.ask(full_prompt)
        self.memory.save("teacher", response)
        return response


================================================================================
FILE: .history\agents\teacher_agent_20260608004013.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
from rag.rag_chat import RAGChat
from agents.supervisor_agent import SupervisorAgent

class TeacherAgent:

    def __init__(self):
        self.memory = Memory()
        self.calculator = CalculatorTool()
        self.search_tool = SearchTool()
        self.router = Router()
        self.llm = NvidiaClient(NVIDIA_API_KEY)
        self.rag = RAGChat()
        self.supervisor = SupervisorAgent()

    def answer(self, question):
        self.memory.save("student", question)

        route = self.router.route(question)

        if route in ["calculator", "search", "rag"]:
            result = self.supervisor.handle(
                route,
                question
            )

            self.memory.save(
                "teacher",
                str(result)
            )

            return str(result)

        history = self.memory.get_history()
        context = ""
        for item in history[-10:]:
            context += f"{item['role']}: {item['message']}\n"

        full_prompt = f"""
Previous Conversation:

{context}
Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

        response = self.llm.ask(full_prompt)
        self.memory.save("teacher", response)
        return response


================================================================================
FILE: .history\agents\teacher_agent_20260608005114.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
from rag.rag_chat import RAGChat
from agents.supervisor_agent import SupervisorAgent

class TeacherAgent:

    def __init__(self):
        self.memory = Memory()
        self.calculator = CalculatorTool()
        self.search_tool = SearchTool()
        self.router = Router()
        self.llm = NvidiaClient(NVIDIA_API_KEY)
        self.rag = RAGChat()
        self.supervisor = SupervisorAgent()

    def answer(self, question):
        self.memory.save("student", question)

        route = self.router.route(question)

        if route in ["calculator", "search", "rag", "multi"]:
            result = self.supervisor.handle(
                route,
                question
            )

            self.memory.save(
                "teacher",
                str(result)
            )

            return str(result)

        history = self.memory.get_history()
        context = ""
        for item in history[-10:]:
            context += f"{item['role']}: {item['message']}\n"

        full_prompt = f"""
Previous Conversation:

{context}
Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

        response = self.llm.ask(full_prompt)
        self.memory.save("teacher", response)
        return response


================================================================================
FILE: .history\agents\teacher_agent_20260608005123.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
from rag.rag_chat import RAGChat
from agents.supervisor_agent import SupervisorAgent

class TeacherAgent:

    def __init__(self):
        self.memory = Memory()
        self.calculator = CalculatorTool()
        self.search_tool = SearchTool()
        self.router = Router()
        self.llm = NvidiaClient(NVIDIA_API_KEY)
        self.rag = RAGChat()
        self.supervisor = SupervisorAgent()

    def answer(self, question):
        self.memory.save("student", question)

        route = self.router.route(question)

        if route in ["calculator", "search", "rag", "multi"]:
            result = self.supervisor.handle(
                route,
                question
            )

            self.memory.save(
                "teacher",
                str(result)
            )

            return str(result)

        history = self.memory.get_history()
        context = ""
        for item in history[-10:]:
            context += f"{item['role']}: {item['message']}\n"

        full_prompt = f"""
Previous Conversation:

{context}
Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

        response = self.llm.ask(full_prompt)
        self.memory.save("teacher", response)
        return response


================================================================================
FILE: .history\app_20260607020321.py
================================================================================



================================================================================
FILE: .history\app_20260607020337.py
================================================================================

import streamlit as st
from agents.teacher_agent import TeacherAgent

st.title("Student Teacher Agent")

if "teacher" not in st.session_state:
    st.session_state.teacher = TeacherAgent()

question = st.text_input("Ask a question")

if st.button("Send"):

    answer = st.session_state.teacher.answer(
        question
    )

    st.write("### Answer")
    st.write(answer)

================================================================================
FILE: .history\app_20260607020338.py
================================================================================

import streamlit as st
from agents.teacher_agent import TeacherAgent

st.title("Student Teacher Agent")

if "teacher" not in st.session_state:
    st.session_state.teacher = TeacherAgent()

question = st.text_input("Ask a question")

if st.button("Send"):

    answer = st.session_state.teacher.answer(
        question
    )

    st.write("### Answer")
    st.write(answer)

================================================================================
FILE: .history\app_20260607174936.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    if st.button("New Chat", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    for chat in sorted_chats:
        title = chat.get("title", "New Chat")

        if chat["id"] == st.session_state.active_chat_id:
            title = f"* {title}"

        if st.button(title, key=f"chat_{chat['id']}", use_container_width=True):
            switch_chat(chat["id"])
            st.rerun()


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


st.markdown(
    "<div class='upload-hint'>Attach a PDF, TXT, or DOCX file</div>",
    unsafe_allow_html=True,
)

uploaded_file = st.file_uploader(
    "Upload file",
    type=["pdf", "txt", "docx"],
    label_visibility="collapsed",
)

if uploaded_file is not None:
    file_signature = f"{uploaded_file.name}:{uploaded_file.size}"

    if file_signature != st.session_state.uploaded_file_signature:
        st.session_state.uploaded_file_signature = file_signature
        handle_uploaded_file(uploaded_file)
        st.rerun()


prompt = st.chat_input("Message Student Teacher Agent")

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607175021.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    if st.button("New Chat", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    for chat in sorted_chats:
        title = chat.get("title", "New Chat")

        if chat["id"] == st.session_state.active_chat_id:
            title = f"* {title}"

        if st.button(title, key=f"chat_{chat['id']}", use_container_width=True):
            switch_chat(chat["id"])
            st.rerun()


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


st.markdown(
    "<div class='upload-hint'>Attach a PDF, TXT, or DOCX file</div>",
    unsafe_allow_html=True,
)

uploaded_file = st.file_uploader(
    "Upload file",
    type=["pdf", "txt", "docx"],
    label_visibility="collapsed",
)

if uploaded_file is not None:
    file_signature = f"{uploaded_file.name}:{uploaded_file.size}"

    if file_signature != st.session_state.uploaded_file_signature:
        st.session_state.uploaded_file_signature = file_signature
        handle_uploaded_file(uploaded_file)
        st.rerun()


prompt = st.chat_input("Message Student Teacher Agent")

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607175146.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    if st.button("New Chat", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    for chat in sorted_chats:
        title = chat.get("title", "New Chat")

        if chat["id"] == st.session_state.active_chat_id:
            title = f"* {title}"

        if st.button(title, key=f"chat_{chat['id']}", use_container_width=True):
            switch_chat(chat["id"])
            st.rerun()


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


st.markdown(
    "<div class='upload-hint'>Attach a PDF, TXT, or DOCX file</div>",
    unsafe_allow_html=True,
)

uploaded_file = st.file_uploader(
    "Upload file",
    type=["pdf", "txt", "docx"],
    label_visibility="collapsed",
)

if uploaded_file is not None:
    file_signature = f"{uploaded_file.name}:{uploaded_file.size}"

    if file_signature != st.session_state.uploaded_file_signature:
        st.session_state.uploaded_file_signature = file_signature
        handle_uploaded_file(uploaded_file)
        st.rerun()


prompt = st.chat_input("Message Student Teacher Agent")

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607175148.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    if st.button("New Chat", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    for chat in sorted_chats:
        title = chat.get("title", "New Chat")

        if chat["id"] == st.session_state.active_chat_id:
            title = f"* {title}"

        if st.button(title, key=f"chat_{chat['id']}", use_container_width=True):
            switch_chat(chat["id"])
            st.rerun()


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


st.markdown(
    "<div class='upload-hint'>Attach a PDF, TXT, or DOCX file</div>",
    unsafe_allow_html=True,
)

uploaded_file = st.file_uploader(
    "Upload file",
    type=["pdf", "txt", "docx"],
    label_visibility="collapsed",
)

if uploaded_file is not None:
    file_signature = f"{uploaded_file.name}:{uploaded_file.size}"

    if file_signature != st.session_state.uploaded_file_signature:
        st.session_state.uploaded_file_signature = file_signature
        handle_uploaded_file(uploaded_file)
        st.rerun()


prompt = st.chat_input("Message Student Teacher Agent")

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607175159.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    if st.button("New Chat", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    for chat in sorted_chats:
        title = chat.get("title", "New Chat")

        if chat["id"] == st.session_state.active_chat_id:
            title = f"* {title}"

        if st.button(title, key=f"chat_{chat['id']}", use_container_width=True):
            switch_chat(chat["id"])
            st.rerun()


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


# Attachment button (paperclip) — clicking adds ?attach=1 to URL
st.markdown("<a class='attach-link' href='?attach=1'>📎</a>", unsafe_allow_html=True)

# If user opened the attachment panel via the paperclip link, show a compact uploader
params = st.experimental_get_query_params()
if params.get("attach"):
    with st.container():
        st.markdown("<div class='attachment-panel'>", unsafe_allow_html=True)
        attachment = st.file_uploader(
            "",
            type=["pdf", "docx", "txt", "png", "jpg", "jpeg", "gif"],
            key="attachment_uploader",
            label_visibility="collapsed",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"

            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                # clear the query param and refresh
                st.experimental_set_query_params()
                st.rerun()

        st.markdown("</div>", unsafe_allow_html=True)


prompt = st.chat_input("Message Student Teacher Agent")

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607175206.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    if st.button("New Chat", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    for chat in sorted_chats:
        title = chat.get("title", "New Chat")

        if chat["id"] == st.session_state.active_chat_id:
            title = f"* {title}"

        if st.button(title, key=f"chat_{chat['id']}", use_container_width=True):
            switch_chat(chat["id"])
            st.rerun()


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


# Attachment button (paperclip) — clicking adds ?attach=1 to URL
st.markdown("<a class='attach-link' href='?attach=1'>📎</a>", unsafe_allow_html=True)

# If user opened the attachment panel via the paperclip link, show a compact uploader
params = st.experimental_get_query_params()
if params.get("attach"):
    with st.container():
        st.markdown("<div class='attachment-panel'>", unsafe_allow_html=True)
        attachment = st.file_uploader(
            "",
            type=["pdf", "docx", "txt", "png", "jpg", "jpeg", "gif"],
            key="attachment_uploader",
            label_visibility="collapsed",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"

            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                # clear the query param and refresh
                st.experimental_set_query_params()
                st.rerun()

        st.markdown("</div>", unsafe_allow_html=True)


prompt = st.chat_input("Message Student Teacher Agent")

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607175923.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    if st.button("New Chat", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    for chat in sorted_chats:
        title = chat.get("title", "New Chat")

        if chat["id"] == st.session_state.active_chat_id:
            title = f"* {title}"

        if st.button(title, key=f"chat_{chat['id']}", use_container_width=True):
            switch_chat(chat["id"])
            st.rerun()


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


# Attachment button (paperclip) — clicking adds ?attach=1 to URL
if "show_attachment" not in st.session_state:
    st.session_state.show_attachment = False

# Render chat input row with attachment button on the left
col_attach, col_input, col_spacer = st.columns([0.06, 0.82, 0.12])

with col_attach:
    if st.button("📎", key="attach_btn"):
        st.session_state.show_attachment = not st.session_state.show_attachment

with col_input:
    prompt = st.chat_input("Message Student Teacher Agent")

with col_spacer:
    st.write("")

# Show compact uploader above the input when toggled
if st.session_state.show_attachment:
    with st.container():
        st.markdown("<div class='attachment-panel'>", unsafe_allow_html=True)
        attachment = st.file_uploader(
            "Attach file",
            type=["pdf", "docx", "txt", "png", "jpg", "jpeg", "gif"],
            key="attachment_uploader",
            label_visibility="collapsed",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"

            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.show_attachment = False
                st.rerun()

        st.markdown("</div>", unsafe_allow_html=True)


prompt = st.chat_input("Message Student Teacher Agent")

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607175936.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    if st.button("New Chat", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    for chat in sorted_chats:
        title = chat.get("title", "New Chat")

        if chat["id"] == st.session_state.active_chat_id:
            title = f"* {title}"

        if st.button(title, key=f"chat_{chat['id']}", use_container_width=True):
            switch_chat(chat["id"])
            st.rerun()


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


# Attachment button (paperclip) — clicking adds ?attach=1 to URL
if "show_attachment" not in st.session_state:
    st.session_state.show_attachment = False

# Render chat input row with attachment button on the left
col_attach, col_input, col_spacer = st.columns([0.06, 0.82, 0.12])

with col_attach:
    if st.button("📎", key="attach_btn"):
        st.session_state.show_attachment = not st.session_state.show_attachment

with col_input:
    prompt = st.chat_input("Message Student Teacher Agent")

with col_spacer:
    st.write("")

# Show compact uploader above the input when toggled
if st.session_state.show_attachment:
    with st.container():
        st.markdown("<div class='attachment-panel'>", unsafe_allow_html=True)
        attachment = st.file_uploader(
            "Attach file",
            type=["pdf", "docx", "txt", "png", "jpg", "jpeg", "gif"],
            key="attachment_uploader",
            label_visibility="collapsed",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"

            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.show_attachment = False
                st.rerun()

        st.markdown("</div>", unsafe_allow_html=True)

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607180011.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    if st.button("New Chat", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    for chat in sorted_chats:
        title = chat.get("title", "New Chat")

        if chat["id"] == st.session_state.active_chat_id:
            title = f"* {title}"

        if st.button(title, key=f"chat_{chat['id']}", use_container_width=True):
            switch_chat(chat["id"])
            st.rerun()


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


# Attachment button (paperclip) — clicking adds ?attach=1 to URL
if "show_attachment" not in st.session_state:
    st.session_state.show_attachment = False

# Render chat input row with attachment button on the left
col_attach, col_input, col_spacer = st.columns([0.06, 0.82, 0.12])

with col_attach:
    if st.button("📎", key="attach_btn"):
        st.session_state.show_attachment = not st.session_state.show_attachment

with col_input:
    prompt = st.chat_input("Message Student Teacher Agent")

with col_spacer:
    st.write("")

# Show compact uploader above the input when toggled
if st.session_state.show_attachment:
    with st.container():
        st.markdown("<div class='attachment-panel'>", unsafe_allow_html=True)
        attachment = st.file_uploader(
            "Attach file",
            type=["pdf", "docx", "txt", "png", "jpg", "jpeg", "gif"],
            key="attachment_uploader",
            label_visibility="collapsed",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"

            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.show_attachment = False
                st.rerun()

        st.markdown("</div>", unsafe_allow_html=True)

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607181015.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # New Chat button (always visible at top)
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    # Scrollable chat list container (keeps chat history intact)
    st.markdown("<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>", unsafe_allow_html=True)

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    # Render each chat as a compact row: icon | title | menu
    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        col_icon, col_title, col_menu = st.columns([0.08, 0.78, 0.14])

        with col_icon:
            st.markdown("💬")

        display_title = truncate(title, 28)
        select_key = f"chat_select_{chat_id}"
        label_text = f"{display_title}"
        if is_active:
            label_text = f"● {display_title}"

        with col_title:
            if st.button(label_text, key=select_key, help=title):
                switch_chat(chat_id)
                st.rerun()

        with col_menu:
            menu_key = f"menu_{chat_id}"
            if st.button("⋮", key=menu_key):
                st.session_state._menu_for = chat_id

        # Inline actions (Rename / Delete) when menu is open for this chat
        if st.session_state.get("_menu_for") == chat_id:
            a_col, b_col = st.columns([0.5, 0.5])

            with a_col:
                if st.button("Rename", key=f"rename_btn_{chat_id}"):
                    def do_rename():
                        new_name = st.session_state.get(f"rename_input_{chat_id}")
                        if new_name:
                            for c in st.session_state.chats:
                                if c["id"] == chat_id:
                                    c["title"] = new_name
                                    break
                            save_chats_to_disk()
                            st.session_state._menu_for = None
                            st.rerun()

                    with st.modal("Rename Chat"):
                        st.text_input("New name", key=f"rename_input_{chat_id}", value=title)
                        st.button("Save", on_click=do_rename)

            with b_col:
                if st.button("Delete", key=f"delete_btn_{chat_id}"):
                    def do_delete():
                        st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                        if st.session_state.get("active_chat_id") == chat_id:
                            st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.rerun()

                    with st.modal("Delete Chat"):
                        st.markdown(f"Are you sure you want to delete this chat: **{title}**?")
                        st.button("Confirm Delete", on_click=do_delete)

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


# Attachment button (paperclip) — clicking adds ?attach=1 to URL
if "show_attachment" not in st.session_state:
    st.session_state.show_attachment = False

# Render chat input row with attachment button on the left
col_attach, col_input, col_spacer = st.columns([0.06, 0.82, 0.12])

with col_attach:
    if st.button("📎", key="attach_btn"):
        st.session_state.show_attachment = not st.session_state.show_attachment

with col_input:
    prompt = st.chat_input("Message Student Teacher Agent")

with col_spacer:
    st.write("")

# Show compact uploader above the input when toggled
if st.session_state.show_attachment:
    with st.container():
        st.markdown("<div class='attachment-panel'>", unsafe_allow_html=True)
        attachment = st.file_uploader(
            "Attach file",
            type=["pdf", "docx", "txt", "png", "jpg", "jpeg", "gif"],
            key="attachment_uploader",
            label_visibility="collapsed",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"

            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.show_attachment = False
                st.rerun()

        st.markdown("</div>", unsafe_allow_html=True)

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607181038.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # New Chat button (always visible at top)
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    # Scrollable chat list container (keeps chat history intact)
    st.markdown("<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>", unsafe_allow_html=True)

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    # Render each chat as a compact row: icon | title | menu
    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        col_icon, col_title, col_menu = st.columns([0.08, 0.78, 0.14])

        with col_icon:
            st.markdown("💬")

        display_title = truncate(title, 28)
        select_key = f"chat_select_{chat_id}"
        label_text = f"{display_title}"
        if is_active:
            label_text = f"● {display_title}"

        with col_title:
            if st.button(label_text, key=select_key, help=title):
                switch_chat(chat_id)
                st.rerun()

        with col_menu:
            menu_key = f"menu_{chat_id}"
            if st.button("⋮", key=menu_key):
                st.session_state._menu_for = chat_id

        # Inline actions (Rename / Delete) when menu is open for this chat
        if st.session_state.get("_menu_for") == chat_id:
            a_col, b_col = st.columns([0.5, 0.5])

            with a_col:
                if st.button("Rename", key=f"rename_btn_{chat_id}"):
                    def do_rename():
                        new_name = st.session_state.get(f"rename_input_{chat_id}")
                        if new_name:
                            for c in st.session_state.chats:
                                if c["id"] == chat_id:
                                    c["title"] = new_name
                                    break
                            save_chats_to_disk()
                            st.session_state._menu_for = None
                            st.rerun()

                    with st.modal("Rename Chat"):
                        st.text_input("New name", key=f"rename_input_{chat_id}", value=title)
                        st.button("Save", on_click=do_rename)

            with b_col:
                if st.button("Delete", key=f"delete_btn_{chat_id}"):
                    def do_delete():
                        st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                        if st.session_state.get("active_chat_id") == chat_id:
                            st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.rerun()

                    with st.modal("Delete Chat"):
                        st.markdown(f"Are you sure you want to delete this chat: **{title}**?")
                        st.button("Confirm Delete", on_click=do_delete)

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


# Attachment button (paperclip) — clicking adds ?attach=1 to URL
if "show_attachment" not in st.session_state:
    st.session_state.show_attachment = False

# Render chat input row with attachment button on the left
col_attach, col_input, col_spacer = st.columns([0.06, 0.82, 0.12])

with col_attach:
    if st.button("📎", key="attach_btn"):
        st.session_state.show_attachment = not st.session_state.show_attachment

with col_input:
    prompt = st.chat_input("Message Student Teacher Agent")

with col_spacer:
    st.write("")

# Show compact uploader above the input when toggled
if st.session_state.show_attachment:
    with st.container():
        st.markdown("<div class='attachment-panel'>", unsafe_allow_html=True)
        attachment = st.file_uploader(
            "Attach file",
            type=["pdf", "docx", "txt", "png", "jpg", "jpeg", "gif"],
            key="attachment_uploader",
            label_visibility="collapsed",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"

            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.show_attachment = False
                st.rerun()

        st.markdown("</div>", unsafe_allow_html=True)

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607181156.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # New Chat button (always visible at top)
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    # Scrollable chat list container (keeps chat history intact)
    st.markdown("<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>", unsafe_allow_html=True)

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    # Render each chat as a compact row: icon | title | menu
    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        col_icon, col_title, col_menu = st.columns([0.08, 0.78, 0.14])

        with col_icon:
            st.markdown("💬")

        display_title = truncate(title, 28)
        select_key = f"chat_select_{chat_id}"
        label_text = f"{display_title}"
        if is_active:
            label_text = f"● {display_title}"

        with col_title:
            if st.button(label_text, key=select_key, help=title):
                switch_chat(chat_id)
                st.rerun()

        with col_menu:
            menu_key = f"menu_{chat_id}"
            if st.button("⋮", key=menu_key):
                st.session_state._menu_for = chat_id

        # Inline actions (Rename / Delete) when menu is open for this chat
        if st.session_state.get("_menu_for") == chat_id:
            a_col, b_col = st.columns([0.5, 0.5])

            with a_col:
                if st.button("Rename", key=f"rename_btn_{chat_id}"):
                    def do_rename():
                        new_name = st.session_state.get(f"rename_input_{chat_id}")
                        if new_name:
                            for c in st.session_state.chats:
                                if c["id"] == chat_id:
                                    c["title"] = new_name
                                    break
                            save_chats_to_disk()
                            st.session_state._menu_for = None
                            st.rerun()

                    with st.modal("Rename Chat"):
                        st.text_input("New name", key=f"rename_input_{chat_id}", value=title)
                        st.button("Save", on_click=do_rename)

            with b_col:
                if st.button("Delete", key=f"delete_btn_{chat_id}"):
                    def do_delete():
                        st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                        if st.session_state.get("active_chat_id") == chat_id:
                            st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.rerun()

                    with st.modal("Delete Chat"):
                        st.markdown(f"Are you sure you want to delete this chat: **{title}**?")
                        st.button("Confirm Delete", on_click=do_delete)

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


# Attachment button (paperclip) — clicking adds ?attach=1 to URL
if "show_attachment" not in st.session_state:
    st.session_state.show_attachment = False

# Render chat input row with attachment button on the left
col_attach, col_input, col_spacer = st.columns([0.06, 0.82, 0.12])

with col_attach:
    if st.button("📎", key="attach_btn"):
        st.session_state.show_attachment = not st.session_state.show_attachment

with col_input:
    prompt = st.chat_input("Message Student Teacher Agent")

with col_spacer:
    st.write("")

# Show compact uploader above the input when toggled
if st.session_state.show_attachment:
    with st.container():
        st.markdown("<div class='attachment-panel'>", unsafe_allow_html=True)
        attachment = st.file_uploader(
            "Attach file",
            type=["pdf", "docx", "txt", "png", "jpg", "jpeg", "gif"],
            key="attachment_uploader",
            label_visibility="collapsed",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"

            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.show_attachment = False
                st.rerun()

        st.markdown("</div>", unsafe_allow_html=True)

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607181158.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # New Chat button (always visible at top)
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    # Scrollable chat list container (keeps chat history intact)
    st.markdown("<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>", unsafe_allow_html=True)

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    # Render each chat as a compact row: icon | title | menu
    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        col_icon, col_title, col_menu = st.columns([0.08, 0.78, 0.14])

        with col_icon:
            st.markdown("💬")

        display_title = truncate(title, 28)
        select_key = f"chat_select_{chat_id}"
        label_text = f"{display_title}"
        if is_active:
            label_text = f"● {display_title}"

        with col_title:
            if st.button(label_text, key=select_key, help=title):
                switch_chat(chat_id)
                st.rerun()

        with col_menu:
            menu_key = f"menu_{chat_id}"
            if st.button("⋮", key=menu_key):
                st.session_state._menu_for = chat_id

        # Inline actions (Rename / Delete) when menu is open for this chat
        if st.session_state.get("_menu_for") == chat_id:
            a_col, b_col = st.columns([0.5, 0.5])

            with a_col:
                if st.button("Rename", key=f"rename_btn_{chat_id}"):
                    def do_rename():
                        new_name = st.session_state.get(f"rename_input_{chat_id}")
                        if new_name:
                            for c in st.session_state.chats:
                                if c["id"] == chat_id:
                                    c["title"] = new_name
                                    break
                            save_chats_to_disk()
                            st.session_state._menu_for = None
                            st.rerun()

                    with st.modal("Rename Chat"):
                        st.text_input("New name", key=f"rename_input_{chat_id}", value=title)
                        st.button("Save", on_click=do_rename)

            with b_col:
                if st.button("Delete", key=f"delete_btn_{chat_id}"):
                    def do_delete():
                        st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                        if st.session_state.get("active_chat_id") == chat_id:
                            st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.rerun()

                    with st.modal("Delete Chat"):
                        st.markdown(f"Are you sure you want to delete this chat: **{title}**?")
                        st.button("Confirm Delete", on_click=do_delete)

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


# Attachment button (paperclip) — clicking adds ?attach=1 to URL
if "show_attachment" not in st.session_state:
    st.session_state.show_attachment = False

# Render chat input row with attachment button on the left
col_attach, col_input, col_spacer = st.columns([0.06, 0.82, 0.12])

with col_attach:
    if st.button("📎", key="attach_btn"):
        st.session_state.show_attachment = not st.session_state.show_attachment

with col_input:
    prompt = st.chat_input("Message Student Teacher Agent")

with col_spacer:
    st.write("")

# Show compact uploader above the input when toggled
if st.session_state.show_attachment:
    with st.container():
        st.markdown("<div class='attachment-panel'>", unsafe_allow_html=True)
        attachment = st.file_uploader(
            "Attach file",
            type=["pdf", "docx", "txt", "png", "jpg", "jpeg", "gif"],
            key="attachment_uploader",
            label_visibility="collapsed",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"

            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.show_attachment = False
                st.rerun()

        st.markdown("</div>", unsafe_allow_html=True)

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607233102.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


# Attachment button (paperclip) — clicking adds ?attach=1 to URL
if "show_attachment" not in st.session_state:
    st.session_state.show_attachment = False

# Render chat input row with attachment button on the left
col_attach, col_input, col_spacer = st.columns([0.06, 0.82, 0.12])

with col_attach:
    if st.button("📎", key="attach_btn"):
        st.session_state.show_attachment = not st.session_state.show_attachment

with col_input:
    prompt = st.chat_input("Message Student Teacher Agent")

with col_spacer:
    st.write("")

# Show compact uploader above the input when toggled
if st.session_state.show_attachment:
    with st.container():
        st.markdown("<div class='attachment-panel'>", unsafe_allow_html=True)
        attachment = st.file_uploader(
            "Attach file",
            type=["pdf", "docx", "txt", "png", "jpg", "jpeg", "gif"],
            key="attachment_uploader",
            label_visibility="collapsed",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"

            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.show_attachment = False
                st.rerun()

        st.markdown("</div>", unsafe_allow_html=True)

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607233140.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


# Attachment button (paperclip) — clicking adds ?attach=1 to URL
if "show_attachment" not in st.session_state:
    st.session_state.show_attachment = False

# Render chat input row with attachment button on the left
col_attach, col_input, col_spacer = st.columns([0.06, 0.82, 0.12])

with col_attach:
    if st.button("📎", key="attach_btn"):
        st.session_state.show_attachment = not st.session_state.show_attachment

with col_input:
    prompt = st.chat_input("Message Student Teacher Agent")

with col_spacer:
    st.write("")

# Show compact uploader above the input when toggled
if st.session_state.show_attachment:
    with st.container():
        st.markdown("<div class='attachment-panel'>", unsafe_allow_html=True)
        attachment = st.file_uploader(
            "Attach file",
            type=["pdf", "docx", "txt", "png", "jpg", "jpeg", "gif"],
            key="attachment_uploader",
            label_visibility="collapsed",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"

            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.show_attachment = False
                st.rerun()

        st.markdown("</div>", unsafe_allow_html=True)

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607233723.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        .upload-preview {
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            margin-bottom: 0.65rem;
            width: fit-content;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            color: #8df57f;
            font-size: 0.92rem;
            margin-top: 0.3rem;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>",
        unsafe_allow_html=True,
    )

# Render chat input row with attachment button and chat input
col_attach, col_input = st.columns([0.07, 0.93])

with col_attach:
    if st.button("+", key="attach_btn", help="Upload a file"):
        st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
        st.session_state.attachment_type = None

with col_input:
    prompt = st.chat_input("Message Student Teacher Agent")

if st.session_state.attachment_menu_open:
    with st.expander("Upload file", expanded=True):
        st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
        col_pdf, col_image, col_docx, col_txt, col_cancel = st.columns([1, 1, 1, 1, 1])

        with col_pdf:
            if st.button("📄 Upload PDF", key="upload_pdf"):
                st.session_state.attachment_type = "pdf"
        with col_image:
            if st.button("🖼 Upload Image", key="upload_image"):
                st.session_state.attachment_type = "image"
        with col_docx:
            if st.button("📄 Upload DOCX", key="upload_docx"):
                st.session_state.attachment_type = "docx"
        with col_txt:
            if st.button("📄 Upload TXT", key="upload_txt"):
                st.session_state.attachment_type = "txt"
        with col_cancel:
            if st.button("❌ Cancel", key="upload_cancel"):
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None

        st.markdown("</div>", unsafe_allow_html=True)

        if st.session_state.attachment_type:
            if st.session_state.attachment_type == "pdf":
                allowed_types = ["pdf"]
                label = "Choose a PDF file"
            elif st.session_state.attachment_type == "image":
                allowed_types = ["png", "jpg", "jpeg"]
                label = "Choose an image file"
            elif st.session_state.attachment_type == "docx":
                allowed_types = ["docx"]
                label = "Choose a DOCX file"
            else:
                allowed_types = ["txt"]
                label = "Choose a TXT file"

            attachment = st.file_uploader(
                label,
                type=allowed_types,
                key="attachment_uploader",
                label_visibility="visible",
            )

            if attachment is not None:
                file_signature = f"{attachment.name}:{attachment.size}"
                if file_signature != st.session_state.uploaded_file_signature:
                    st.session_state.uploaded_file_signature = file_signature
                    handle_uploaded_file(attachment)
                    st.session_state.uploaded_preview = attachment.name
                    st.session_state.upload_status = "Ready"
                    st.session_state.attachment_menu_open = False
                    st.session_state.attachment_type = None
                    st.session_state.attachment_uploader = None
                    st.rerun()

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607233729.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        .upload-preview {
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            margin-bottom: 0.65rem;
            width: fit-content;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            color: #8df57f;
            font-size: 0.92rem;
            margin-top: 0.3rem;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>",
        unsafe_allow_html=True,
    )

# Render chat input row with attachment button and chat input
col_attach, col_input = st.columns([0.07, 0.93])

with col_attach:
    if st.button("+", key="attach_btn", help="Upload a file"):
        st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
        st.session_state.attachment_type = None

with col_input:
    prompt = st.chat_input("Message Student Teacher Agent")

if st.session_state.attachment_menu_open:
    with st.expander("Upload file", expanded=True):
        st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
        col_pdf, col_image, col_docx, col_txt, col_cancel = st.columns([1, 1, 1, 1, 1])

        with col_pdf:
            if st.button("📄 Upload PDF", key="upload_pdf"):
                st.session_state.attachment_type = "pdf"
        with col_image:
            if st.button("🖼 Upload Image", key="upload_image"):
                st.session_state.attachment_type = "image"
        with col_docx:
            if st.button("📄 Upload DOCX", key="upload_docx"):
                st.session_state.attachment_type = "docx"
        with col_txt:
            if st.button("📄 Upload TXT", key="upload_txt"):
                st.session_state.attachment_type = "txt"
        with col_cancel:
            if st.button("❌ Cancel", key="upload_cancel"):
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None

        st.markdown("</div>", unsafe_allow_html=True)

        if st.session_state.attachment_type:
            if st.session_state.attachment_type == "pdf":
                allowed_types = ["pdf"]
                label = "Choose a PDF file"
            elif st.session_state.attachment_type == "image":
                allowed_types = ["png", "jpg", "jpeg"]
                label = "Choose an image file"
            elif st.session_state.attachment_type == "docx":
                allowed_types = ["docx"]
                label = "Choose a DOCX file"
            else:
                allowed_types = ["txt"]
                label = "Choose a TXT file"

            attachment = st.file_uploader(
                label,
                type=allowed_types,
                key="attachment_uploader",
                label_visibility="visible",
            )

            if attachment is not None:
                file_signature = f"{attachment.name}:{attachment.size}"
                if file_signature != st.session_state.uploaded_file_signature:
                    st.session_state.uploaded_file_signature = file_signature
                    handle_uploaded_file(attachment)
                    st.session_state.uploaded_preview = attachment.name
                    st.session_state.upload_status = "Ready"
                    st.session_state.attachment_menu_open = False
                    st.session_state.attachment_type = None
                    st.session_state.attachment_uploader = None
                    st.rerun()

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607233755.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        .upload-preview {
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            margin-bottom: 0.65rem;
            width: fit-content;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            color: #8df57f;
            font-size: 0.92rem;
            margin-top: 0.3rem;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>",
        unsafe_allow_html=True,
    )
    st.markdown(
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

# Render chat input row with attachment button and chat input
col_attach, col_input = st.columns([0.07, 0.93])

with col_attach:
    if st.button("+", key="attach_btn", help="Upload a file"):
        st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
        st.session_state.attachment_type = None

with col_input:
    prompt = st.chat_input("Message Student Teacher Agent")

if st.session_state.attachment_menu_open:
    with st.expander("Upload file", expanded=True):
        st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
        col_pdf, col_image, col_docx, col_txt, col_cancel = st.columns([1, 1, 1, 1, 1])

        with col_pdf:
            if st.button("📄 Upload PDF", key="upload_pdf"):
                st.session_state.attachment_type = "pdf"
        with col_image:
            if st.button("🖼 Upload Image", key="upload_image"):
                st.session_state.attachment_type = "image"
        with col_docx:
            if st.button("📄 Upload DOCX", key="upload_docx"):
                st.session_state.attachment_type = "docx"
        with col_txt:
            if st.button("📄 Upload TXT", key="upload_txt"):
                st.session_state.attachment_type = "txt"
        with col_cancel:
            if st.button("❌ Cancel", key="upload_cancel"):
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None

        st.markdown("</div>", unsafe_allow_html=True)

        if st.session_state.attachment_type:
            if st.session_state.attachment_type == "pdf":
                allowed_types = ["pdf"]
                label = "Choose a PDF file"
            elif st.session_state.attachment_type == "image":
                allowed_types = ["png", "jpg", "jpeg"]
                label = "Choose an image file"
            elif st.session_state.attachment_type == "docx":
                allowed_types = ["docx"]
                label = "Choose a DOCX file"
            else:
                allowed_types = ["txt"]
                label = "Choose a TXT file"

            attachment = st.file_uploader(
                label,
                type=allowed_types,
                key="attachment_uploader",
                label_visibility="visible",
            )

            if attachment is not None:
                file_signature = f"{attachment.name}:{attachment.size}"
                if file_signature != st.session_state.uploaded_file_signature:
                    st.session_state.uploaded_file_signature = file_signature
                    handle_uploaded_file(attachment)
                    st.session_state.uploaded_preview = attachment.name
                    st.session_state.upload_status = "Ready"
                    st.session_state.attachment_menu_open = False
                    st.session_state.attachment_type = None
                    st.session_state.attachment_uploader = None
                    st.rerun()

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607233807.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        .upload-preview {
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            margin-bottom: 0.65rem;
            width: fit-content;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            color: #8df57f;
            font-size: 0.92rem;
            margin-top: 0.3rem;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>",
        unsafe_allow_html=True,
    )
    st.markdown(
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

# Render chat input row with attachment button and chat input
col_attach, col_input = st.columns([0.07, 0.93])

with col_attach:
    if st.button("+", key="attach_btn", help="Upload a file"):
        st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
        st.session_state.attachment_type = None

with col_input:
    prompt = st.chat_input("Message Student Teacher Agent")

if st.session_state.attachment_menu_open:
    with st.expander("Upload file", expanded=True):
        st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
        col_pdf, col_image, col_docx, col_txt, col_cancel = st.columns([1, 1, 1, 1, 1])

        with col_pdf:
            if st.button("📄 Upload PDF", key="upload_pdf"):
                st.session_state.attachment_type = "pdf"
        with col_image:
            if st.button("🖼 Upload Image", key="upload_image"):
                st.session_state.attachment_type = "image"
        with col_docx:
            if st.button("📄 Upload DOCX", key="upload_docx"):
                st.session_state.attachment_type = "docx"
        with col_txt:
            if st.button("📄 Upload TXT", key="upload_txt"):
                st.session_state.attachment_type = "txt"
        with col_cancel:
            if st.button("❌ Cancel", key="upload_cancel"):
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None

        st.markdown("</div>", unsafe_allow_html=True)

        if st.session_state.attachment_type:
            if st.session_state.attachment_type == "pdf":
                allowed_types = ["pdf"]
                label = "Choose a PDF file"
            elif st.session_state.attachment_type == "image":
                allowed_types = ["png", "jpg", "jpeg"]
                label = "Choose an image file"
            elif st.session_state.attachment_type == "docx":
                allowed_types = ["docx"]
                label = "Choose a DOCX file"
            else:
                allowed_types = ["txt"]
                label = "Choose a TXT file"

            attachment = st.file_uploader(
                label,
                type=allowed_types,
                key="attachment_uploader",
                label_visibility="visible",
            )

            if attachment is not None:
                file_signature = f"{attachment.name}:{attachment.size}"
                if file_signature != st.session_state.uploaded_file_signature:
                    st.session_state.uploaded_file_signature = file_signature
                    handle_uploaded_file(attachment)
                    st.session_state.uploaded_preview = attachment.name
                    st.session_state.upload_status = "Ready"
                    st.session_state.attachment_menu_open = False
                    st.session_state.attachment_type = None
                    st.session_state.attachment_uploader = None
                    st.rerun()

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607234316.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        .upload-preview {
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            margin-bottom: 0.65rem;
            width: fit-content;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            color: #8df57f;
            font-size: 0.92rem;
            margin-top: 0.3rem;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .chat-input-column [data-testid="stChatInput"] {
            width: 100%;
        }

        .chat-input-column .stTextInput>div {
            border-radius: 24px;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>",
        unsafe_allow_html=True,
    )
    st.markdown(
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

# Render chat input row with attachment button and chat input
col_attach, col_input = st.columns([0.07, 0.93])

with col_attach:
    if st.button("+", key="attach_btn", help="Upload a file"):
        st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
        st.session_state.attachment_type = None

with col_input:
    prompt = st.chat_input("Message Student Teacher Agent")

if st.session_state.attachment_menu_open:
    with st.expander("Upload file", expanded=True):
        st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
        col_pdf, col_image, col_docx, col_txt, col_cancel = st.columns([1, 1, 1, 1, 1])

        with col_pdf:
            if st.button("📄 Upload PDF", key="upload_pdf"):
                st.session_state.attachment_type = "pdf"
        with col_image:
            if st.button("🖼 Upload Image", key="upload_image"):
                st.session_state.attachment_type = "image"
        with col_docx:
            if st.button("📄 Upload DOCX", key="upload_docx"):
                st.session_state.attachment_type = "docx"
        with col_txt:
            if st.button("📄 Upload TXT", key="upload_txt"):
                st.session_state.attachment_type = "txt"
        with col_cancel:
            if st.button("❌ Cancel", key="upload_cancel"):
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None

        st.markdown("</div>", unsafe_allow_html=True)

        if st.session_state.attachment_type:
            if st.session_state.attachment_type == "pdf":
                allowed_types = ["pdf"]
                label = "Choose a PDF file"
            elif st.session_state.attachment_type == "image":
                allowed_types = ["png", "jpg", "jpeg"]
                label = "Choose an image file"
            elif st.session_state.attachment_type == "docx":
                allowed_types = ["docx"]
                label = "Choose a DOCX file"
            else:
                allowed_types = ["txt"]
                label = "Choose a TXT file"

            attachment = st.file_uploader(
                label,
                type=allowed_types,
                key="attachment_uploader",
                label_visibility="visible",
            )

            if attachment is not None:
                file_signature = f"{attachment.name}:{attachment.size}"
                if file_signature != st.session_state.uploaded_file_signature:
                    st.session_state.uploaded_file_signature = file_signature
                    handle_uploaded_file(attachment)
                    st.session_state.uploaded_preview = attachment.name
                    st.session_state.upload_status = "Ready"
                    st.session_state.attachment_menu_open = False
                    st.session_state.attachment_type = None
                    st.session_state.attachment_uploader = None
                    st.rerun()

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607234328.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        .upload-preview {
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            margin-bottom: 0.65rem;
            width: fit-content;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            color: #8df57f;
            font-size: 0.92rem;
            margin-top: 0.3rem;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .chat-input-column [data-testid="stChatInput"] {
            width: 100%;
        }

        .chat-input-column .stTextInput>div {
            border-radius: 24px;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>",
        unsafe_allow_html=True,
    )
    st.markdown(
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

# Render chat input row with attachment button and chat input
col_attach, col_input = st.columns([0.07, 0.93])

with col_attach:
    if st.button("+", key="attach_btn", help="Upload a file"):
        st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
        st.session_state.attachment_type = None

with col_input:
    prompt = st.chat_input("Message Student Teacher Agent")

if st.session_state.attachment_menu_open:
    with st.expander("Upload file", expanded=True):
        st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
        col_pdf, col_image, col_docx, col_txt, col_cancel = st.columns([1, 1, 1, 1, 1])

        with col_pdf:
            if st.button("📄 Upload PDF", key="upload_pdf"):
                st.session_state.attachment_type = "pdf"
        with col_image:
            if st.button("🖼 Upload Image", key="upload_image"):
                st.session_state.attachment_type = "image"
        with col_docx:
            if st.button("📄 Upload DOCX", key="upload_docx"):
                st.session_state.attachment_type = "docx"
        with col_txt:
            if st.button("📄 Upload TXT", key="upload_txt"):
                st.session_state.attachment_type = "txt"
        with col_cancel:
            if st.button("❌ Cancel", key="upload_cancel"):
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None

        st.markdown("</div>", unsafe_allow_html=True)

        if st.session_state.attachment_type:
            if st.session_state.attachment_type == "pdf":
                allowed_types = ["pdf"]
                label = "Choose a PDF file"
            elif st.session_state.attachment_type == "image":
                allowed_types = ["png", "jpg", "jpeg"]
                label = "Choose an image file"
            elif st.session_state.attachment_type == "docx":
                allowed_types = ["docx"]
                label = "Choose a DOCX file"
            else:
                allowed_types = ["txt"]
                label = "Choose a TXT file"

            attachment = st.file_uploader(
                label,
                type=allowed_types,
                key="attachment_uploader",
                label_visibility="visible",
            )

            if attachment is not None:
                file_signature = f"{attachment.name}:{attachment.size}"
                if file_signature != st.session_state.uploaded_file_signature:
                    st.session_state.uploaded_file_signature = file_signature
                    handle_uploaded_file(attachment)
                    st.session_state.uploaded_preview = attachment.name
                    st.session_state.upload_status = "Ready"
                    st.session_state.attachment_menu_open = False
                    st.session_state.attachment_type = None
                    st.session_state.attachment_uploader = None
                    st.rerun()

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607234358.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        .upload-preview {
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            margin-bottom: 0.65rem;
            width: fit-content;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            color: #8df57f;
            font-size: 0.92rem;
            margin-top: 0.3rem;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .chat-input-column [data-testid="stChatInput"] {
            width: 100%;
        }

        .chat-input-column .stTextInput>div {
            border-radius: 24px;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>",
        unsafe_allow_html=True,
    )
    st.markdown(
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

if st.session_state.attachment_menu_open:
    st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
    option_cols = st.columns([1, 1, 1, 1])

    with option_cols[0]:
        if st.button("📄 Upload PDF", key="upload_pdf"):
            st.session_state.attachment_type = "pdf"
    with option_cols[1]:
        if st.button("🖼 Upload Image", key="upload_image"):
            st.session_state.attachment_type = "image"
    with option_cols[2]:
        if st.button("📄 Upload DOCX", key="upload_docx"):
            st.session_state.attachment_type = "docx"
    with option_cols[3]:
        if st.button("📄 Upload TXT", key="upload_txt"):
            st.session_state.attachment_type = "txt"

    st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

with st.container():
    input_cols = st.columns([0.07, 0.93])
    with input_cols[0]:
        if st.button("+", key="attach_btn", help="Upload a file"):
            st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
            st.session_state.attachment_type = None
    with input_cols[1]:
        prompt = st.chat_input("Message Student Teacher Agent")

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607234409.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        .upload-preview {
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            margin-bottom: 0.65rem;
            width: fit-content;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            color: #8df57f;
            font-size: 0.92rem;
            margin-top: 0.3rem;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .chat-input-column [data-testid="stChatInput"] {
            width: 100%;
        }

        .chat-input-column .stTextInput>div {
            border-radius: 24px;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>",
        unsafe_allow_html=True,
    )
    st.markdown(
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

if st.session_state.attachment_menu_open:
    st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
    option_cols = st.columns([1, 1, 1, 1])

    with option_cols[0]:
        if st.button("📄 Upload PDF", key="upload_pdf"):
            st.session_state.attachment_type = "pdf"
    with option_cols[1]:
        if st.button("🖼 Upload Image", key="upload_image"):
            st.session_state.attachment_type = "image"
    with option_cols[2]:
        if st.button("📄 Upload DOCX", key="upload_docx"):
            st.session_state.attachment_type = "docx"
    with option_cols[3]:
        if st.button("📄 Upload TXT", key="upload_txt"):
            st.session_state.attachment_type = "txt"

    st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

with st.container():
    input_cols = st.columns([0.07, 0.93])
    with input_cols[0]:
        if st.button("+", key="attach_btn", help="Upload a file"):
            st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
            st.session_state.attachment_type = None
    with input_cols[1]:
        prompt = st.chat_input("Message Student Teacher Agent")

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607235001.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        .upload-preview {
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            margin-bottom: 0.65rem;
            width: fit-content;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            color: #8df57f;
            font-size: 0.92rem;
            margin-top: 0.3rem;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        button[title="Upload a file"] {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 410px);
            width: 3rem;
            height: 3rem;
            border-radius: 14px;
            background: #2f2f2f;
            border: 1px solid #444;
            color: var(--text);
            font-size: 20px;
            padding: 0;
            z-index: 10010;
        }

        button[title="Upload a file"]:hover {
            background: #383838;
        }

        .chat-input-wrapper {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            display: flex;
            flex-direction: column;
            gap: 0.45rem;
            padding: 0 0.5rem;
            pointer-events: auto;
        }

        .chat-input-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            padding: 0.45rem 0.75rem;
        }

        .chat-input-row .stTextInput {
            flex: 1 1 auto;
        }

        .chat-input-row .stTextInput>div {
            border-radius: 24px;
        }

        .chat-input-row .stTextInput textarea {
            min-height: 2.8rem;
            background: transparent;
            color: var(--text);
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>",
        unsafe_allow_html=True,
    )
    st.markdown(
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

if st.session_state.attachment_menu_open:
    st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
    option_cols = st.columns([1, 1, 1, 1])

    with option_cols[0]:
        if st.button("📄 Upload PDF", key="upload_pdf"):
            st.session_state.attachment_type = "pdf"
    with option_cols[1]:
        if st.button("🖼 Upload Image", key="upload_image"):
            st.session_state.attachment_type = "image"
    with option_cols[2]:
        if st.button("📄 Upload DOCX", key="upload_docx"):
            st.session_state.attachment_type = "docx"
    with option_cols[3]:
        if st.button("📄 Upload TXT", key="upload_txt"):
            st.session_state.attachment_type = "txt"

    st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

with st.container():
    input_cols = st.columns([0.07, 0.93])
    with input_cols[0]:
        if st.button("+", key="attach_btn", help="Upload a file"):
            st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
            st.session_state.attachment_type = None
    with input_cols[1]:
        prompt = st.chat_input("Message Student Teacher Agent")

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607235015.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        .upload-preview {
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            margin-bottom: 0.65rem;
            width: fit-content;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            color: #8df57f;
            font-size: 0.92rem;
            margin-top: 0.3rem;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        button[title="Upload a file"] {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 410px);
            width: 3rem;
            height: 3rem;
            border-radius: 14px;
            background: #2f2f2f;
            border: 1px solid #444;
            color: var(--text);
            font-size: 20px;
            padding: 0;
            z-index: 10010;
        }

        button[title="Upload a file"]:hover {
            background: #383838;
        }

        .chat-input-wrapper {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            display: flex;
            flex-direction: column;
            gap: 0.45rem;
            padding: 0 0.5rem;
            pointer-events: auto;
        }

        .chat-input-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            padding: 0.45rem 0.75rem;
        }

        .chat-input-row .stTextInput {
            flex: 1 1 auto;
        }

        .chat-input-row .stTextInput>div {
            border-radius: 24px;
        }

        .chat-input-row .stTextInput textarea {
            min-height: 2.8rem;
            background: transparent;
            color: var(--text);
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>",
        unsafe_allow_html=True,
    )
    st.markdown(
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

if st.session_state.attachment_menu_open:
    st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
    option_cols = st.columns([1, 1, 1, 1])

    with option_cols[0]:
        if st.button("📄 Upload PDF", key="upload_pdf"):
            st.session_state.attachment_type = "pdf"
    with option_cols[1]:
        if st.button("🖼 Upload Image", key="upload_image"):
            st.session_state.attachment_type = "image"
    with option_cols[2]:
        if st.button("📄 Upload DOCX", key="upload_docx"):
            st.session_state.attachment_type = "docx"
    with option_cols[3]:
        if st.button("📄 Upload TXT", key="upload_txt"):
            st.session_state.attachment_type = "txt"

    st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

with st.container():
    input_cols = st.columns([0.07, 0.93])
    with input_cols[0]:
        if st.button("+", key="attach_btn", help="Upload a file"):
            st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
            st.session_state.attachment_type = None
    with input_cols[1]:
        prompt = st.chat_input("Message Student Teacher Agent")

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607235310.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        .upload-preview {
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            margin-bottom: 0.65rem;
            width: fit-content;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            color: #8df57f;
            font-size: 0.92rem;
            margin-top: 0.3rem;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .chat-input-wrapper {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            display: flex;
            flex-direction: column;
            gap: 0.35rem;
            padding: 0 0.5rem;
            pointer-events: auto;
        }

        .chat-input-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            background: #1c1c1c;
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            padding: 0.45rem 0.75rem;
        }

        .chat-input-row button {
            border: 0;
            padding: 0;
            min-width: 3rem;
            min-height: 3rem;
            border-radius: 14px;
            background: #2f2f2f;
            color: var(--text);
            font-size: 20px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
        }

        .chat-input-row button:hover {
            background: #383838;
        }

        .chat-input-row .stChatInput {
            flex: 1;
        }

        .chat-input-row .stChatInput textarea {
            background: transparent;
            border: none;
            min-height: 3rem;
            color: var(--text);
            padding: 0.6rem 0.9rem;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>",
        unsafe_allow_html=True,
    )
    st.markdown(
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

if st.session_state.attachment_menu_open:
    st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
    option_cols = st.columns([1, 1, 1, 1])

    with option_cols[0]:
        if st.button("📄 Upload PDF", key="upload_pdf"):
            st.session_state.attachment_type = "pdf"
    with option_cols[1]:
        if st.button("🖼 Upload Image", key="upload_image"):
            st.session_state.attachment_type = "image"
    with option_cols[2]:
        if st.button("📄 Upload DOCX", key="upload_docx"):
            st.session_state.attachment_type = "docx"
    with option_cols[3]:
        if st.button("📄 Upload TXT", key="upload_txt"):
            st.session_state.attachment_type = "txt"

    st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

with st.container():
    input_cols = st.columns([0.07, 0.93])
    with input_cols[0]:
        if st.button("+", key="attach_btn", help="Upload a file"):
            st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
            st.session_state.attachment_type = None
    with input_cols[1]:
        prompt = st.chat_input("Message Student Teacher Agent")

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607235317.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        .upload-preview {
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            margin-bottom: 0.65rem;
            width: fit-content;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            color: #8df57f;
            font-size: 0.92rem;
            margin-top: 0.3rem;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .chat-input-wrapper {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            display: flex;
            flex-direction: column;
            gap: 0.35rem;
            padding: 0 0.5rem;
            pointer-events: auto;
        }

        .chat-input-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            background: #1c1c1c;
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            padding: 0.45rem 0.75rem;
        }

        .chat-input-row button {
            border: 0;
            padding: 0;
            min-width: 3rem;
            min-height: 3rem;
            border-radius: 14px;
            background: #2f2f2f;
            color: var(--text);
            font-size: 20px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
        }

        .chat-input-row button:hover {
            background: #383838;
        }

        .chat-input-row .stChatInput {
            flex: 1;
        }

        .chat-input-row .stChatInput textarea {
            background: transparent;
            border: none;
            min-height: 3rem;
            color: var(--text);
            padding: 0.6rem 0.9rem;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>",
        unsafe_allow_html=True,
    )
    st.markdown(
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

if st.session_state.attachment_menu_open:
    st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
    option_cols = st.columns([1, 1, 1, 1])

    with option_cols[0]:
        if st.button("📄 Upload PDF", key="upload_pdf"):
            st.session_state.attachment_type = "pdf"
    with option_cols[1]:
        if st.button("🖼 Upload Image", key="upload_image"):
            st.session_state.attachment_type = "image"
    with option_cols[2]:
        if st.button("📄 Upload DOCX", key="upload_docx"):
            st.session_state.attachment_type = "docx"
    with option_cols[3]:
        if st.button("📄 Upload TXT", key="upload_txt"):
            st.session_state.attachment_type = "txt"

    st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

st.markdown("<div class='chat-input-wrapper'>", unsafe_allow_html=True)
with st.container():
    st.markdown("<div class='chat-input-row'>", unsafe_allow_html=True)
    input_cols = st.columns([0.07, 0.93])
    with input_cols[0]:
        if st.button("+", key="attach_btn", help="Upload a file"):
            st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
            st.session_state.attachment_type = None
    with input_cols[1]:
        prompt = st.chat_input("Message Student Teacher Agent")
    st.markdown("</div>", unsafe_allow_html=True)
st.markdown("</div>", unsafe_allow_html=True)

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607235323.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 8px 0 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 18px;
            color: var(--text);
            min-height: 3.25rem;
            padding: 0.6rem 0.9rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        .upload-preview {
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            margin-bottom: 0.65rem;
            width: fit-content;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            color: #8df57f;
            font-size: 0.92rem;
            margin-top: 0.3rem;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .chat-input-wrapper {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            display: flex;
            flex-direction: column;
            gap: 0.35rem;
            padding: 0 0.5rem;
            pointer-events: auto;
        }

        .chat-input-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            background: #1c1c1c;
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            padding: 0.45rem 0.75rem;
        }

        .chat-input-row button {
            border: 0;
            padding: 0;
            min-width: 3rem;
            min-height: 3rem;
            border-radius: 14px;
            background: #2f2f2f;
            color: var(--text);
            font-size: 20px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
        }

        .chat-input-row button:hover {
            background: #383838;
        }

        .chat-input-row .stChatInput {
            flex: 1;
        }

        .chat-input-row .stChatInput textarea {
            background: transparent;
            border: none;
            min-height: 3rem;
            color: var(--text);
            padding: 0.6rem 0.9rem;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>",
        unsafe_allow_html=True,
    )
    st.markdown(
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

if st.session_state.attachment_menu_open:
    st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
    option_cols = st.columns([1, 1, 1, 1])

    with option_cols[0]:
        if st.button("📄 Upload PDF", key="upload_pdf"):
            st.session_state.attachment_type = "pdf"
    with option_cols[1]:
        if st.button("🖼 Upload Image", key="upload_image"):
            st.session_state.attachment_type = "image"
    with option_cols[2]:
        if st.button("📄 Upload DOCX", key="upload_docx"):
            st.session_state.attachment_type = "docx"
    with option_cols[3]:
        if st.button("📄 Upload TXT", key="upload_txt"):
            st.session_state.attachment_type = "txt"

    st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

st.markdown("<div class='chat-input-wrapper'>", unsafe_allow_html=True)
with st.container():
    st.markdown("<div class='chat-input-row'>", unsafe_allow_html=True)
    input_cols = st.columns([0.07, 0.93])
    with input_cols[0]:
        if st.button("+", key="attach_btn", help="Upload a file"):
            st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
            st.session_state.attachment_type = None
    with input_cols[1]:
        prompt = st.chat_input("Message Student Teacher Agent")
    st.markdown("</div>", unsafe_allow_html=True)
st.markdown("</div>", unsafe_allow_html=True)

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607235834.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            color: var(--text);
            min-height: 3.2rem;
            padding: 0.75rem 1rem 0.75rem 3.5rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        button[title="Upload a file"] {
            position: fixed;
            left: calc(50% - 402px);
            bottom: 20px;
            width: 3rem;
            height: 3rem;
            border-radius: 16px;
            background: #2f2f2f;
            border: 1px solid #444;
            color: var(--text);
            font-size: 22px;
            padding: 0;
            z-index: 10010;
        }

        button[title="Upload a file"]:hover {
            background: #383838;
        }

        .attachment-popup {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 72px;
            width: min(820px, calc(100% - 48px));
            z-index: 10009;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0;
        }

        .upload-preview {
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            margin-bottom: 0.65rem;
            width: fit-content;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            color: #8df57f;
            font-size: 0.92rem;
            margin-top: 0.3rem;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .chat-input-wrapper {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            display: flex;
            flex-direction: column;
            gap: 0.35rem;
            padding: 0 0.5rem;
            pointer-events: auto;
        }

        .chat-input-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            background: #1c1c1c;
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            padding: 0.45rem 0.75rem;
        }

        .chat-input-row button {
            border: 0;
            padding: 0;
            min-width: 3rem;
            min-height: 3rem;
            border-radius: 14px;
            background: #2f2f2f;
            color: var(--text);
            font-size: 20px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
        }

        .chat-input-row button:hover {
            background: #383838;
        }

        .chat-input-row .stChatInput {
            flex: 1;
        }

        .chat-input-row .stChatInput textarea {
            background: transparent;
            border: none;
            min-height: 3rem;
            color: var(--text);
            padding: 0.6rem 0.9rem;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>",
        unsafe_allow_html=True,
    )
    st.markdown(
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

if st.session_state.attachment_menu_open:
    st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
    option_cols = st.columns([1, 1, 1, 1])

    with option_cols[0]:
        if st.button("📄 Upload PDF", key="upload_pdf"):
            st.session_state.attachment_type = "pdf"
    with option_cols[1]:
        if st.button("🖼 Upload Image", key="upload_image"):
            st.session_state.attachment_type = "image"
    with option_cols[2]:
        if st.button("📄 Upload DOCX", key="upload_docx"):
            st.session_state.attachment_type = "docx"
    with option_cols[3]:
        if st.button("📄 Upload TXT", key="upload_txt"):
            st.session_state.attachment_type = "txt"

    st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

st.markdown("<div class='chat-input-wrapper'>", unsafe_allow_html=True)
with st.container():
    st.markdown("<div class='chat-input-row'>", unsafe_allow_html=True)
    input_cols = st.columns([0.07, 0.93])
    with input_cols[0]:
        if st.button("+", key="attach_btn", help="Upload a file"):
            st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
            st.session_state.attachment_type = None
    with input_cols[1]:
        prompt = st.chat_input("Message Student Teacher Agent")
    st.markdown("</div>", unsafe_allow_html=True)
st.markdown("</div>", unsafe_allow_html=True)

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607235836.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            color: var(--text);
            min-height: 3.2rem;
            padding: 0.75rem 1rem 0.75rem 3.5rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        button[title="Upload a file"] {
            position: fixed;
            left: calc(50% - 402px);
            bottom: 20px;
            width: 3rem;
            height: 3rem;
            border-radius: 16px;
            background: #2f2f2f;
            border: 1px solid #444;
            color: var(--text);
            font-size: 22px;
            padding: 0;
            z-index: 10010;
        }

        button[title="Upload a file"]:hover {
            background: #383838;
        }

        .attachment-popup {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 72px;
            width: min(820px, calc(100% - 48px));
            z-index: 10009;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0;
        }

        .upload-preview {
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            margin-bottom: 0.65rem;
            width: fit-content;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            color: #8df57f;
            font-size: 0.92rem;
            margin-top: 0.3rem;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .chat-input-wrapper {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            display: flex;
            flex-direction: column;
            gap: 0.35rem;
            padding: 0 0.5rem;
            pointer-events: auto;
        }

        .chat-input-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            background: #1c1c1c;
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            padding: 0.45rem 0.75rem;
        }

        .chat-input-row button {
            border: 0;
            padding: 0;
            min-width: 3rem;
            min-height: 3rem;
            border-radius: 14px;
            background: #2f2f2f;
            color: var(--text);
            font-size: 20px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
        }

        .chat-input-row button:hover {
            background: #383838;
        }

        .chat-input-row .stChatInput {
            flex: 1;
        }

        .chat-input-row .stChatInput textarea {
            background: transparent;
            border: none;
            min-height: 3rem;
            color: var(--text);
            padding: 0.6rem 0.9rem;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>",
        unsafe_allow_html=True,
    )
    st.markdown(
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

if st.session_state.attachment_menu_open:
    st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
    option_cols = st.columns([1, 1, 1, 1])

    with option_cols[0]:
        if st.button("📄 Upload PDF", key="upload_pdf"):
            st.session_state.attachment_type = "pdf"
    with option_cols[1]:
        if st.button("🖼 Upload Image", key="upload_image"):
            st.session_state.attachment_type = "image"
    with option_cols[2]:
        if st.button("📄 Upload DOCX", key="upload_docx"):
            st.session_state.attachment_type = "docx"
    with option_cols[3]:
        if st.button("📄 Upload TXT", key="upload_txt"):
            st.session_state.attachment_type = "txt"

    st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

st.markdown("<div class='chat-input-wrapper'>", unsafe_allow_html=True)
with st.container():
    st.markdown("<div class='chat-input-row'>", unsafe_allow_html=True)
    input_cols = st.columns([0.07, 0.93])
    with input_cols[0]:
        if st.button("+", key="attach_btn", help="Upload a file"):
            st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
            st.session_state.attachment_type = None
    with input_cols[1]:
        prompt = st.chat_input("Message Student Teacher Agent")
    st.markdown("</div>", unsafe_allow_html=True)
st.markdown("</div>", unsafe_allow_html=True)

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607235842.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            color: var(--text);
            min-height: 3.2rem;
            padding: 0.75rem 1rem 0.75rem 3.5rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        button[title="Upload a file"] {
            position: fixed;
            left: calc(50% - 402px);
            bottom: 20px;
            width: 3rem;
            height: 3rem;
            border-radius: 16px;
            background: #2f2f2f;
            border: 1px solid #444;
            color: var(--text);
            font-size: 22px;
            padding: 0;
            z-index: 10010;
        }

        button[title="Upload a file"]:hover {
            background: #383838;
        }

        .attachment-popup {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 72px;
            width: min(820px, calc(100% - 48px));
            z-index: 10009;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0;
        }

        .upload-preview {
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            margin-bottom: 0.65rem;
            width: fit-content;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            color: #8df57f;
            font-size: 0.92rem;
            margin-top: 0.3rem;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>",
        unsafe_allow_html=True,
    )
    st.markdown(
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

if st.session_state.attachment_menu_open:
    st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
    option_cols = st.columns([1, 1, 1, 1])

    with option_cols[0]:
        if st.button("📄 Upload PDF", key="upload_pdf"):
            st.session_state.attachment_type = "pdf"
    with option_cols[1]:
        if st.button("🖼 Upload Image", key="upload_image"):
            st.session_state.attachment_type = "image"
    with option_cols[2]:
        if st.button("📄 Upload DOCX", key="upload_docx"):
            st.session_state.attachment_type = "docx"
    with option_cols[3]:
        if st.button("📄 Upload TXT", key="upload_txt"):
            st.session_state.attachment_type = "txt"

    st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

st.markdown("<div class='chat-input-wrapper'>", unsafe_allow_html=True)
with st.container():
    st.markdown("<div class='chat-input-row'>", unsafe_allow_html=True)
    input_cols = st.columns([0.07, 0.93])
    with input_cols[0]:
        if st.button("+", key="attach_btn", help="Upload a file"):
            st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
            st.session_state.attachment_type = None
    with input_cols[1]:
        prompt = st.chat_input("Message Student Teacher Agent")
    st.markdown("</div>", unsafe_allow_html=True)
st.markdown("</div>", unsafe_allow_html=True)

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607235853.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            color: var(--text);
            min-height: 3.2rem;
            padding: 0.75rem 1rem 0.75rem 3.5rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        button[title="Upload a file"] {
            position: fixed;
            left: calc(50% - 402px);
            bottom: 20px;
            width: 3rem;
            height: 3rem;
            border-radius: 16px;
            background: #2f2f2f;
            border: 1px solid #444;
            color: var(--text);
            font-size: 22px;
            padding: 0;
            z-index: 10010;
        }

        button[title="Upload a file"]:hover {
            background: #383838;
        }

        .attachment-popup {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 72px;
            width: min(820px, calc(100% - 48px));
            z-index: 10009;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0;
        }

        .upload-preview {
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            margin-bottom: 0.65rem;
            width: fit-content;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            color: #8df57f;
            font-size: 0.92rem;
            margin-top: 0.3rem;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>",
        unsafe_allow_html=True,
    )
    st.markdown(
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

if st.session_state.attachment_menu_open:
    st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
    option_cols = st.columns([1, 1, 1, 1])

    with option_cols[0]:
        if st.button("📄 Upload PDF", key="upload_pdf"):
            st.session_state.attachment_type = "pdf"
    with option_cols[1]:
        if st.button("🖼 Upload Image", key="upload_image"):
            st.session_state.attachment_type = "image"
    with option_cols[2]:
        if st.button("📄 Upload DOCX", key="upload_docx"):
            st.session_state.attachment_type = "docx"
    with option_cols[3]:
        if st.button("📄 Upload TXT", key="upload_txt"):
            st.session_state.attachment_type = "txt"

    st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

st.markdown("<div class='chat-input-wrapper'>", unsafe_allow_html=True)
with st.container():
    st.markdown("<div class='chat-input-row'>", unsafe_allow_html=True)
    input_cols = st.columns([0.07, 0.93])
    with input_cols[0]:
        if st.button("+", key="attach_btn", help="Upload a file"):
            st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
            st.session_state.attachment_type = None
    with input_cols[1]:
        prompt = st.chat_input("Message Student Teacher Agent")
    st.markdown("</div>", unsafe_allow_html=True)
st.markdown("</div>", unsafe_allow_html=True)

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607235906.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            color: var(--text);
            min-height: 3.2rem;
            padding: 0.75rem 1rem 0.75rem 3.5rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        button[title="Upload a file"] {
            position: fixed;
            left: calc(50% - 402px);
            bottom: 20px;
            width: 3rem;
            height: 3rem;
            border-radius: 16px;
            background: #2f2f2f;
            border: 1px solid #444;
            color: var(--text);
            font-size: 22px;
            padding: 0;
            z-index: 10010;
        }

        button[title="Upload a file"]:hover {
            background: #383838;
        }

        .attachment-popup {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 72px;
            width: min(820px, calc(100% - 48px));
            z-index: 10009;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0;
        }

        .upload-preview {
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            margin-bottom: 0.65rem;
            width: fit-content;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            color: #8df57f;
            font-size: 0.92rem;
            margin-top: 0.3rem;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>",
        unsafe_allow_html=True,
    )
    st.markdown(
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

if st.session_state.attachment_menu_open:
    st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
    option_cols = st.columns([1, 1, 1, 1])

    with option_cols[0]:
        if st.button("📄 Upload PDF", key="upload_pdf"):
            st.session_state.attachment_type = "pdf"
    with option_cols[1]:
        if st.button("🖼 Upload Image", key="upload_image"):
            st.session_state.attachment_type = "image"
    with option_cols[2]:
        if st.button("📄 Upload DOCX", key="upload_docx"):
            st.session_state.attachment_type = "docx"
    with option_cols[3]:
        if st.button("📄 Upload TXT", key="upload_txt"):
            st.session_state.attachment_type = "txt"

    st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

if st.button("+", key="attach_btn", help="Upload a file"):
    st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
    st.session_state.attachment_type = None

prompt = st.chat_input("Message Student Teacher Agent")

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607235917.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            color: var(--text);
            min-height: 3.2rem;
            padding: 0.75rem 1rem 0.75rem 3.5rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        button[title="Upload a file"] {
            position: fixed;
            left: calc(50% - 402px);
            bottom: 20px;
            width: 3rem;
            height: 3rem;
            border-radius: 16px;
            background: #2f2f2f;
            border: 1px solid #444;
            color: var(--text);
            font-size: 22px;
            padding: 0;
            z-index: 10010;
        }

        button[title="Upload a file"]:hover {
            background: #383838;
        }

        .attachment-popup {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 72px;
            width: min(820px, calc(100% - 48px));
            z-index: 10009;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0;
        }

        .upload-preview {
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            margin-bottom: 0.65rem;
            width: fit-content;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            color: #8df57f;
            font-size: 0.92rem;
            margin-top: 0.3rem;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>",
        unsafe_allow_html=True,
    )
    st.markdown(
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

if st.session_state.attachment_menu_open:
    st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
    option_cols = st.columns([1, 1, 1, 1])

    with option_cols[0]:
        if st.button("📄 Upload PDF", key="upload_pdf"):
            st.session_state.attachment_type = "pdf"
    with option_cols[1]:
        if st.button("🖼 Upload Image", key="upload_image"):
            st.session_state.attachment_type = "image"
    with option_cols[2]:
        if st.button("📄 Upload DOCX", key="upload_docx"):
            st.session_state.attachment_type = "docx"
    with option_cols[3]:
        if st.button("📄 Upload TXT", key="upload_txt"):
            st.session_state.attachment_type = "txt"

    st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

if st.button("+", key="attach_btn", help="Upload a file"):
    st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
    st.session_state.attachment_type = None

prompt = st.chat_input("Message Student Teacher Agent")

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607235928.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            color: var(--text);
            min-height: 3.2rem;
            padding: 0.75rem 1rem 0.75rem 3.5rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        button[title="Upload a file"] {
            position: fixed;
            left: calc(50% - 402px);
            bottom: 20px;
            width: 3rem;
            height: 3rem;
            border-radius: 16px;
            background: #2f2f2f;
            border: 1px solid #444;
            color: var(--text);
            font-size: 22px;
            padding: 0;
            z-index: 10010;
        }

        button[title="Upload a file"]:hover {
            background: #383838;
        }

        .attachment-popup {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 72px;
            width: min(820px, calc(100% - 48px));
            z-index: 10009;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0;
        }

        .upload-preview {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 108px;
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            z-index: 10009;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            color: #8df57f;
            font-size: 0.92rem;
            margin-top: 0.3rem;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>",
        unsafe_allow_html=True,
    )
    st.markdown(
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

if st.session_state.attachment_menu_open:
    st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
    option_cols = st.columns([1, 1, 1, 1])

    with option_cols[0]:
        if st.button("📄 Upload PDF", key="upload_pdf"):
            st.session_state.attachment_type = "pdf"
    with option_cols[1]:
        if st.button("🖼 Upload Image", key="upload_image"):
            st.session_state.attachment_type = "image"
    with option_cols[2]:
        if st.button("📄 Upload DOCX", key="upload_docx"):
            st.session_state.attachment_type = "docx"
    with option_cols[3]:
        if st.button("📄 Upload TXT", key="upload_txt"):
            st.session_state.attachment_type = "txt"

    st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

if st.button("+", key="attach_btn", help="Upload a file"):
    st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
    st.session_state.attachment_type = None

prompt = st.chat_input("Message Student Teacher Agent")

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607235931.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            color: var(--text);
            min-height: 3.2rem;
            padding: 0.75rem 1rem 0.75rem 3.5rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        button[title="Upload a file"] {
            position: fixed;
            left: calc(50% - 402px);
            bottom: 20px;
            width: 3rem;
            height: 3rem;
            border-radius: 16px;
            background: #2f2f2f;
            border: 1px solid #444;
            color: var(--text);
            font-size: 22px;
            padding: 0;
            z-index: 10010;
        }

        button[title="Upload a file"]:hover {
            background: #383838;
        }

        .attachment-popup {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 72px;
            width: min(820px, calc(100% - 48px));
            z-index: 10009;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0;
        }

        .upload-preview {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 108px;
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            z-index: 10009;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            color: #8df57f;
            font-size: 0.92rem;
            margin-top: 0.3rem;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>",
        unsafe_allow_html=True,
    )
    st.markdown(
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

if st.session_state.attachment_menu_open:
    st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
    option_cols = st.columns([1, 1, 1, 1])

    with option_cols[0]:
        if st.button("📄 Upload PDF", key="upload_pdf"):
            st.session_state.attachment_type = "pdf"
    with option_cols[1]:
        if st.button("🖼 Upload Image", key="upload_image"):
            st.session_state.attachment_type = "image"
    with option_cols[2]:
        if st.button("📄 Upload DOCX", key="upload_docx"):
            st.session_state.attachment_type = "docx"
    with option_cols[3]:
        if st.button("📄 Upload TXT", key="upload_txt"):
            st.session_state.attachment_type = "txt"

    st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

if st.button("+", key="attach_btn", help="Upload a file"):
    st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
    st.session_state.attachment_type = None

prompt = st.chat_input("Message Student Teacher Agent")

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607235944.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            color: var(--text);
            min-height: 3.2rem;
            padding: 0.75rem 1rem 0.75rem 3.5rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        button[title="Upload a file"] {
            position: fixed;
            left: calc(50% - 402px);
            bottom: 20px;
            width: 3rem;
            height: 3rem;
            border-radius: 16px;
            background: #2f2f2f;
            border: 1px solid #444;
            color: var(--text);
            font-size: 22px;
            padding: 0;
            z-index: 10010;
        }

        button[title="Upload a file"]:hover {
            background: #383838;
        }

        .attachment-popup {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 72px;
            width: min(820px, calc(100% - 48px));
            z-index: 10009;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0;
        }

        .upload-preview {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 108px;
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            z-index: 10009;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            color: #8df57f;
            font-size: 0.92rem;
            margin-top: 0.3rem;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>"
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

if st.session_state.attachment_menu_open:
    st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
    option_cols = st.columns([1, 1, 1, 1])

    with option_cols[0]:
        if st.button("📄 Upload PDF", key="upload_pdf"):
            st.session_state.attachment_type = "pdf"
    with option_cols[1]:
        if st.button("🖼 Upload Image", key="upload_image"):
            st.session_state.attachment_type = "image"
    with option_cols[2]:
        if st.button("📄 Upload DOCX", key="upload_docx"):
            st.session_state.attachment_type = "docx"
    with option_cols[3]:
        if st.button("📄 Upload TXT", key="upload_txt"):
            st.session_state.attachment_type = "txt"

    st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

if st.button("+", key="attach_btn", help="Upload a file"):
    st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
    st.session_state.attachment_type = None

prompt = st.chat_input("Message Student Teacher Agent")

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607235947.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            color: var(--text);
            min-height: 3.2rem;
            padding: 0.75rem 1rem 0.75rem 3.5rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        button[title="Upload a file"] {
            position: fixed;
            left: calc(50% - 402px);
            bottom: 20px;
            width: 3rem;
            height: 3rem;
            border-radius: 16px;
            background: #2f2f2f;
            border: 1px solid #444;
            color: var(--text);
            font-size: 22px;
            padding: 0;
            z-index: 10010;
        }

        button[title="Upload a file"]:hover {
            background: #383838;
        }

        .attachment-popup {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 72px;
            width: min(820px, calc(100% - 48px));
            z-index: 10009;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0;
        }

        .upload-preview {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 108px;
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            z-index: 10009;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            color: #8df57f;
            font-size: 0.92rem;
            margin-top: 0.3rem;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>"
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

if st.session_state.attachment_menu_open:
    st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
    option_cols = st.columns([1, 1, 1, 1])

    with option_cols[0]:
        if st.button("📄 Upload PDF", key="upload_pdf"):
            st.session_state.attachment_type = "pdf"
    with option_cols[1]:
        if st.button("🖼 Upload Image", key="upload_image"):
            st.session_state.attachment_type = "image"
    with option_cols[2]:
        if st.button("📄 Upload DOCX", key="upload_docx"):
            st.session_state.attachment_type = "docx"
    with option_cols[3]:
        if st.button("📄 Upload TXT", key="upload_txt"):
            st.session_state.attachment_type = "txt"

    st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

if st.button("+", key="attach_btn", help="Upload a file"):
    st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
    st.session_state.attachment_type = None

prompt = st.chat_input("Message Student Teacher Agent")

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260607235952.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            color: var(--text);
            min-height: 3.2rem;
            padding: 0.75rem 1rem 0.75rem 3.5rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        button[title="Upload a file"] {
            position: fixed;
            left: calc(50% - 402px);
            bottom: 20px;
            width: 3rem;
            height: 3rem;
            border-radius: 16px;
            background: #2f2f2f;
            border: 1px solid #444;
            color: var(--text);
            font-size: 22px;
            padding: 0;
            z-index: 10010;
        }

        button[title="Upload a file"]:hover {
            background: #383838;
        }

        .attachment-popup {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 72px;
            width: min(820px, calc(100% - 48px));
            z-index: 10009;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0;
        }

        .upload-preview {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 108px;
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            z-index: 10009;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 78px;
            color: #8df57f;
            font-size: 0.92rem;
            margin: 0;
            z-index: 10009;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>"
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

if st.session_state.attachment_menu_open:
    st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
    option_cols = st.columns([1, 1, 1, 1])

    with option_cols[0]:
        if st.button("📄 Upload PDF", key="upload_pdf"):
            st.session_state.attachment_type = "pdf"
    with option_cols[1]:
        if st.button("🖼 Upload Image", key="upload_image"):
            st.session_state.attachment_type = "image"
    with option_cols[2]:
        if st.button("📄 Upload DOCX", key="upload_docx"):
            st.session_state.attachment_type = "docx"
    with option_cols[3]:
        if st.button("📄 Upload TXT", key="upload_txt"):
            st.session_state.attachment_type = "txt"

    st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

if st.button("+", key="attach_btn", help="Upload a file"):
    st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
    st.session_state.attachment_type = None

prompt = st.chat_input("Message Student Teacher Agent")

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260608000004.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            color: var(--text);
            min-height: 3.2rem;
            padding: 0.75rem 1rem 0.75rem 3.5rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        button[title="Upload a file"] {
            position: fixed;
            left: calc(50% - 402px);
            bottom: 20px;
            width: 3rem;
            height: 3rem;
            border-radius: 16px;
            background: #2f2f2f;
            border: 1px solid #444;
            color: var(--text);
            font-size: 22px;
            padding: 0;
            z-index: 10010;
        }

        button[title="Upload a file"]:hover {
            background: #383838;
        }

        .attachment-popup {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 72px;
            width: min(820px, calc(100% - 48px));
            z-index: 10009;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0;
        }

        .upload-preview {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 108px;
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            z-index: 10009;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 78px;
            color: #8df57f;
            font-size: 0.92rem;
            margin: 0;
            z-index: 10009;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>"
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

if st.session_state.attachment_menu_open:
    st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
    option_cols = st.columns([1, 1, 1, 1])

    with option_cols[0]:
        if st.button("📄 Upload PDF", key="upload_pdf"):
            st.session_state.attachment_type = "pdf"
    with option_cols[1]:
        if st.button("🖼 Upload Image", key="upload_image"):
            st.session_state.attachment_type = "image"
    with option_cols[2]:
        if st.button("📄 Upload DOCX", key="upload_docx"):
            st.session_state.attachment_type = "docx"
    with option_cols[3]:
        if st.button("📄 Upload TXT", key="upload_txt"):
            st.session_state.attachment_type = "txt"

    st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

if st.button("+", key="attach_btn", help="Upload a file"):
    st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
    st.session_state.attachment_type = None

prompt = st.chat_input("Message Student Teacher Agent")

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260608000005.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            color: var(--text);
            min-height: 3.2rem;
            padding: 0.75rem 1rem 0.75rem 3.5rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        button[title="Upload a file"] {
            position: fixed;
            left: calc((100vw - min(820px, calc(100vw - 48px)))/2 + 12px);
            bottom: 20px;
            width: 3rem;
            height: 3rem;
            border-radius: 16px;
            background: #2f2f2f;
            border: 1px solid #444;
            color: var(--text);
            font-size: 22px;
            padding: 0;
            z-index: 10010;
        }

        button[title="Upload a file"]:hover {
            background: #383838;
        }

        .attachment-popup {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 72px;
            width: min(820px, calc(100% - 48px));
            z-index: 10009;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0;
        }

        .upload-preview {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 108px;
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            z-index: 10009;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 78px;
            color: #8df57f;
            font-size: 0.92rem;
            margin: 0;
            z-index: 10009;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>"
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

if st.session_state.attachment_menu_open:
    st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
    option_cols = st.columns([1, 1, 1, 1])

    with option_cols[0]:
        if st.button("📄 Upload PDF", key="upload_pdf"):
            st.session_state.attachment_type = "pdf"
    with option_cols[1]:
        if st.button("🖼 Upload Image", key="upload_image"):
            st.session_state.attachment_type = "image"
    with option_cols[2]:
        if st.button("📄 Upload DOCX", key="upload_docx"):
            st.session_state.attachment_type = "docx"
    with option_cols[3]:
        if st.button("📄 Upload TXT", key="upload_txt"):
            st.session_state.attachment_type = "txt"

    st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

if st.button("+", key="attach_btn", help="Upload a file"):
    st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
    st.session_state.attachment_type = None

prompt = st.chat_input("Message Student Teacher Agent")

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260608000007.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        [data-testid="stChatInput"] {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            background: transparent;
            padding: 0;
            pointer-events: auto;
        }

        [data-testid="stChatInput"] textarea {
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            color: var(--text);
            min-height: 3.2rem;
            padding: 0.75rem 1rem 0.75rem 3.5rem;
        }

        [data-testid="stChatInput"] textarea:focus {
            border-color: #676767;
            box-shadow: none;
        }

        button[title="Upload a file"] {
            position: fixed;
            left: calc((100vw - min(820px, calc(100vw - 48px)))/2 + 12px);
            bottom: 20px;
            width: 3rem;
            height: 3rem;
            border-radius: 16px;
            background: #2f2f2f;
            border: 1px solid #444;
            color: var(--text);
            font-size: 22px;
            padding: 0;
            z-index: 10010;
        }

        button[title="Upload a file"]:hover {
            background: #383838;
        }

        .attachment-popup {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 72px;
            width: min(820px, calc(100% - 48px));
            z-index: 10009;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0;
        }

        .upload-preview {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 108px;
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            z-index: 10009;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 78px;
            color: #8df57f;
            font-size: 0.92rem;
            margin: 0;
            z-index: 10009;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>"
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

if st.session_state.attachment_menu_open:
    st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
    option_cols = st.columns([1, 1, 1, 1])

    with option_cols[0]:
        if st.button("📄 Upload PDF", key="upload_pdf"):
            st.session_state.attachment_type = "pdf"
    with option_cols[1]:
        if st.button("🖼 Upload Image", key="upload_image"):
            st.session_state.attachment_type = "image"
    with option_cols[2]:
        if st.button("📄 Upload DOCX", key="upload_docx"):
            st.session_state.attachment_type = "docx"
    with option_cols[3]:
        if st.button("📄 Upload TXT", key="upload_txt"):
            st.session_state.attachment_type = "txt"

    st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

if st.button("+", key="attach_btn", help="Upload a file"):
    st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
    st.session_state.attachment_type = None

prompt = st.chat_input("Message Student Teacher Agent")

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260608000329.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        .chatbar-wrapper {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            padding: 0 0.5rem;
            pointer-events: auto;
        }

        .chatbar-row {
            position: relative;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            padding: 0.55rem 0.8rem;
        }

        .chatbar-row .stButton > button {
            min-width: 3rem;
            min-height: 3rem;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #2f2f2f;
            color: var(--text);
            font-size: 20px;
            padding: 0;
        }

        .chatbar-row .stButton > button:hover {
            background: #383838;
        }

        .chatbar-row [data-testid="stChatInput"] {
            flex: 1;
        }

        .chatbar-row [data-testid="stChatInput"] textarea {
            background: transparent;
            border: none;
            min-height: 3rem;
            padding: 0.75rem 1rem;
            color: var(--text);
        }

        .attachment-popup {
            position: absolute;
            bottom: calc(100% + 0.5rem);
            left: 0;
            width: 100%;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.25);
        }

        .attachment-popup {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 72px;
            width: min(820px, calc(100% - 48px));
            z-index: 10009;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0;
        }

        .upload-preview {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 108px;
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            z-index: 10009;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 78px;
            color: #8df57f;
            font-size: 0.92rem;
            margin: 0;
            z-index: 10009;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>"
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

if st.session_state.attachment_menu_open:
    st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
    option_cols = st.columns([1, 1, 1, 1])

    with option_cols[0]:
        if st.button("📄 Upload PDF", key="upload_pdf"):
            st.session_state.attachment_type = "pdf"
    with option_cols[1]:
        if st.button("🖼 Upload Image", key="upload_image"):
            st.session_state.attachment_type = "image"
    with option_cols[2]:
        if st.button("📄 Upload DOCX", key="upload_docx"):
            st.session_state.attachment_type = "docx"
    with option_cols[3]:
        if st.button("📄 Upload TXT", key="upload_txt"):
            st.session_state.attachment_type = "txt"

    st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

if st.button("+", key="attach_btn", help="Upload a file"):
    st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
    st.session_state.attachment_type = None

prompt = st.chat_input("Message Student Teacher Agent")

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260608000339.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        .chatbar-wrapper {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            padding: 0 0.5rem;
            pointer-events: auto;
        }

        .chatbar-row {
            position: relative;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            padding: 0.55rem 0.8rem;
        }

        .chatbar-row .stButton > button {
            min-width: 3rem;
            min-height: 3rem;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #2f2f2f;
            color: var(--text);
            font-size: 20px;
            padding: 0;
        }

        .chatbar-row .stButton > button:hover {
            background: #383838;
        }

        .chatbar-row [data-testid="stChatInput"] {
            flex: 1;
        }

        .chatbar-row [data-testid="stChatInput"] textarea {
            background: transparent;
            border: none;
            min-height: 3rem;
            padding: 0.75rem 1rem;
            color: var(--text);
        }

        .attachment-popup {
            position: absolute;
            bottom: calc(100% + 0.5rem);
            left: 0;
            width: 100%;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.25);
        }

        .attachment-popup {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 72px;
            width: min(820px, calc(100% - 48px));
            z-index: 10009;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0;
        }

        .upload-preview {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 108px;
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            z-index: 10009;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 78px;
            color: #8df57f;
            font-size: 0.92rem;
            margin: 0;
            z-index: 10009;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>"
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

if st.session_state.attachment_menu_open:
    st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
    option_cols = st.columns([1, 1, 1, 1])

    with option_cols[0]:
        if st.button("📄 Upload PDF", key="upload_pdf"):
            st.session_state.attachment_type = "pdf"
    with option_cols[1]:
        if st.button("🖼 Upload Image", key="upload_image"):
            st.session_state.attachment_type = "image"
    with option_cols[2]:
        if st.button("📄 Upload DOCX", key="upload_docx"):
            st.session_state.attachment_type = "docx"
    with option_cols[3]:
        if st.button("📄 Upload TXT", key="upload_txt"):
            st.session_state.attachment_type = "txt"

    st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

if st.button("+", key="attach_btn", help="Upload a file"):
    st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
    st.session_state.attachment_type = None

prompt = st.chat_input("Message Student Teacher Agent")

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260608000429.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        .chatbar-wrapper {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            padding: 0 0.5rem;
            pointer-events: auto;
        }

        .chatbar-row {
            position: relative;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            padding: 0.55rem 0.8rem;
        }

        .chatbar-row .stButton > button {
            min-width: 3rem;
            min-height: 3rem;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #2f2f2f;
            color: var(--text);
            font-size: 20px;
            padding: 0;
        }

        .chatbar-row .stButton > button:hover {
            background: #383838;
        }

        .chatbar-row [data-testid="stChatInput"] {
            flex: 1;
        }

        .chatbar-row [data-testid="stChatInput"] textarea {
            background: transparent;
            border: none;
            min-height: 3rem;
            padding: 0.75rem 1rem;
            color: var(--text);
        }

        .attachment-popup {
            position: absolute;
            bottom: calc(100% + 0.5rem);
            left: 0;
            width: 100%;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.25);
        }

        .attachment-popup {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 72px;
            width: min(820px, calc(100% - 48px));
            z-index: 10009;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0;
        }

        .upload-preview {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 108px;
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            z-index: 10009;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 78px;
            color: #8df57f;
            font-size: 0.92rem;
            margin: 0;
            z-index: 10009;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>"
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

with st.container():
    st.markdown("<div class='chatbar-wrapper'>", unsafe_allow_html=True)

    if st.session_state.attachment_menu_open:
        st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
        st.write("**Attach a file**")
        option_cols = st.columns([1, 1, 1, 1])

        with option_cols[0]:
            if st.button("PDF", key="upload_pdf"):
                st.session_state.attachment_type = "pdf"
        with option_cols[1]:
            if st.button("Image", key="upload_image"):
                st.session_state.attachment_type = "image"
        with option_cols[2]:
            if st.button("DOCX", key="upload_docx"):
                st.session_state.attachment_type = "docx"
        with option_cols[3]:
            if st.button("TXT", key="upload_txt"):
                st.session_state.attachment_type = "txt"

        st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

    st.markdown("<div class='chatbar-row'>", unsafe_allow_html=True)
    cols = st.columns([0.08, 0.92], gap="small")
    with cols[0]:
        if st.button("+", key="attach_btn", help="Upload a file"):
            st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
            st.session_state.attachment_type = None
    with cols[1]:
        prompt = st.chat_input("Message Student Teacher Agent")
    st.markdown("</div>", unsafe_allow_html=True)

    st.markdown("</div>", unsafe_allow_html=True)

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260608000433.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        .chatbar-wrapper {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            padding: 0 0.5rem;
            pointer-events: auto;
        }

        .chatbar-row {
            position: relative;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            padding: 0.55rem 0.8rem;
        }

        .chatbar-row .stButton > button {
            min-width: 3rem;
            min-height: 3rem;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #2f2f2f;
            color: var(--text);
            font-size: 20px;
            padding: 0;
        }

        .chatbar-row .stButton > button:hover {
            background: #383838;
        }

        .chatbar-row [data-testid="stChatInput"] {
            flex: 1;
        }

        .chatbar-row [data-testid="stChatInput"] textarea {
            background: transparent;
            border: none;
            min-height: 3rem;
            padding: 0.75rem 1rem;
            color: var(--text);
        }

        .attachment-popup {
            position: absolute;
            bottom: calc(100% + 0.5rem);
            left: 0;
            width: 100%;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.25);
        }

        .attachment-popup {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 72px;
            width: min(820px, calc(100% - 48px));
            z-index: 10009;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0;
        }

        .upload-preview {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 108px;
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            z-index: 10009;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 78px;
            color: #8df57f;
            font-size: 0.92rem;
            margin: 0;
            z-index: 10009;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>"
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

with st.container():
    st.markdown("<div class='chatbar-wrapper'>", unsafe_allow_html=True)

    if st.session_state.attachment_menu_open:
        st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
        st.write("**Attach a file**")
        option_cols = st.columns([1, 1, 1, 1])

        with option_cols[0]:
            if st.button("PDF", key="upload_pdf"):
                st.session_state.attachment_type = "pdf"
        with option_cols[1]:
            if st.button("Image", key="upload_image"):
                st.session_state.attachment_type = "image"
        with option_cols[2]:
            if st.button("DOCX", key="upload_docx"):
                st.session_state.attachment_type = "docx"
        with option_cols[3]:
            if st.button("TXT", key="upload_txt"):
                st.session_state.attachment_type = "txt"

        st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

    st.markdown("<div class='chatbar-row'>", unsafe_allow_html=True)
    cols = st.columns([0.08, 0.92], gap="small")
    with cols[0]:
        if st.button("+", key="attach_btn", help="Upload a file"):
            st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
            st.session_state.attachment_type = None
    with cols[1]:
        prompt = st.chat_input("Message Student Teacher Agent")
    st.markdown("</div>", unsafe_allow_html=True)

    st.markdown("</div>", unsafe_allow_html=True)

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260608000446.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        .chatbar-wrapper {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            padding: 0 0.5rem;
            pointer-events: auto;
        }

        .chatbar-row {
            position: relative;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            padding: 0.55rem 0.8rem;
        }

        .chatbar-row .stButton > button {
            min-width: 3rem;
            min-height: 3rem;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #2f2f2f;
            color: var(--text);
            font-size: 20px;
            padding: 0;
        }

        .chatbar-row .stButton > button:hover {
            background: #383838;
        }

        .chatbar-row [data-testid="stChatInput"] {
            flex: 1;
        }

        .chatbar-row [data-testid="stChatInput"] textarea {
            background: transparent;
            border: none;
            min-height: 3rem;
            padding: 0.75rem 1rem;
            color: var(--text);
        }

        .attachment-popup {
            position: absolute;
            bottom: calc(100% + 0.5rem);
            left: 0;
            width: 100%;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.25);
        }

        .upload-preview {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 108px;
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            z-index: 10009;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 78px;
            color: #8df57f;
            font-size: 0.92rem;
            margin: 0;
            z-index: 10009;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

        /* Attachment button beside chat input */
        .attach-link {
            position: fixed;
            bottom: 22px;
            left: calc(50% - 420px);
            width: 40px;
            height: 40px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: #2f2f2f;
            border-radius: 10px;
            border: 1px solid #3f3f3f;
            color: var(--text);
            text-decoration: none;
            z-index: 10001;
            font-size: 18px;
        }

        .attachment-panel {
            position: fixed;
            bottom: 72px;
            left: calc(50% - 220px);
            width: 440px;
            z-index: 10001;
            background: transparent;
        }

        .attachment-panel .stFileUploader section {
            display: flex;
            gap: 8px;
            align-items: center;
            padding: 0.5rem;
        }
    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>"
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

with st.container():
    st.markdown("<div class='chatbar-wrapper'>", unsafe_allow_html=True)

    if st.session_state.attachment_menu_open:
        st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
        st.write("**Attach a file**")
        option_cols = st.columns([1, 1, 1, 1])

        with option_cols[0]:
            if st.button("PDF", key="upload_pdf"):
                st.session_state.attachment_type = "pdf"
        with option_cols[1]:
            if st.button("Image", key="upload_image"):
                st.session_state.attachment_type = "image"
        with option_cols[2]:
            if st.button("DOCX", key="upload_docx"):
                st.session_state.attachment_type = "docx"
        with option_cols[3]:
            if st.button("TXT", key="upload_txt"):
                st.session_state.attachment_type = "txt"

        st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

    st.markdown("<div class='chatbar-row'>", unsafe_allow_html=True)
    cols = st.columns([0.08, 0.92], gap="small")
    with cols[0]:
        if st.button("+", key="attach_btn", help="Upload a file"):
            st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
            st.session_state.attachment_type = None
    with cols[1]:
        prompt = st.chat_input("Message Student Teacher Agent")
    st.markdown("</div>", unsafe_allow_html=True)

    st.markdown("</div>", unsafe_allow_html=True)

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260608000451.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        .chatbar-wrapper {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            padding: 0 0.5rem;
            pointer-events: auto;
        }

        .chatbar-row {
            position: relative;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            padding: 0.55rem 0.8rem;
        }

        .chatbar-row .stButton > button {
            min-width: 3rem;
            min-height: 3rem;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #2f2f2f;
            color: var(--text);
            font-size: 20px;
            padding: 0;
        }

        .chatbar-row .stButton > button:hover {
            background: #383838;
        }

        .chatbar-row [data-testid="stChatInput"] {
            flex: 1;
        }

        .chatbar-row [data-testid="stChatInput"] textarea {
            background: transparent;
            border: none;
            min-height: 3rem;
            padding: 0.75rem 1rem;
            color: var(--text);
        }

        .attachment-popup {
            position: absolute;
            bottom: calc(100% + 0.5rem);
            left: 0;
            width: 100%;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.25);
        }

        .upload-preview {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 108px;
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            z-index: 10009;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 78px;
            color: #8df57f;
            font-size: 0.92rem;
            margin: 0;
            z-index: 10009;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>"
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

with st.container():
    st.markdown("<div class='chatbar-wrapper'>", unsafe_allow_html=True)

    if st.session_state.attachment_menu_open:
        st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
        st.write("**Attach a file**")
        option_cols = st.columns([1, 1, 1, 1])

        with option_cols[0]:
            if st.button("PDF", key="upload_pdf"):
                st.session_state.attachment_type = "pdf"
        with option_cols[1]:
            if st.button("Image", key="upload_image"):
                st.session_state.attachment_type = "image"
        with option_cols[2]:
            if st.button("DOCX", key="upload_docx"):
                st.session_state.attachment_type = "docx"
        with option_cols[3]:
            if st.button("TXT", key="upload_txt"):
                st.session_state.attachment_type = "txt"

        st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

    st.markdown("<div class='chatbar-row'>", unsafe_allow_html=True)
    cols = st.columns([0.08, 0.92], gap="small")
    with cols[0]:
        if st.button("+", key="attach_btn", help="Upload a file"):
            st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
            st.session_state.attachment_type = None
    with cols[1]:
        prompt = st.chat_input("Message Student Teacher Agent")
    st.markdown("</div>", unsafe_allow_html=True)

    st.markdown("</div>", unsafe_allow_html=True)

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260608000501.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        .chatbar-wrapper {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            padding: 0 0.5rem;
            pointer-events: auto;
        }

        .chatbar-row {
            position: relative;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            padding: 0.55rem 0.8rem;
        }

        .chatbar-row .stButton > button {
            min-width: 3rem;
            min-height: 3rem;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #2f2f2f;
            color: var(--text);
            font-size: 20px;
            padding: 0;
        }

        .chatbar-row .stButton > button:hover {
            background: #383838;
        }

        .chatbar-row [data-testid="stChatInput"] {
            flex: 1;
        }

        .chatbar-row [data-testid="stChatInput"] textarea {
            background: transparent;
            border: none;
            min-height: 3rem;
            padding: 0.75rem 1rem;
            color: var(--text);
        }

        .attachment-popup {
            position: absolute;
            bottom: calc(100% + 0.5rem);
            left: 0;
            width: 100%;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.25);
        }

        .upload-preview {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 108px;
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            z-index: 10009;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.9rem 1rem;
            margin-bottom: 0.6rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 118px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        .attachment-success {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 78px;
            color: #8df57f;
            font-size: 0.92rem;
            margin: 0;
            z-index: 10009;
        }

        .attach-row {
            display: flex;
            align-items: center;
            gap: 0.65rem;
            width: 100%;
            margin-bottom: 0.2rem;
        }

        .attach-button button {
            background: #2f2f2f;
            border: 1px solid #444;
            border-radius: 14px;
            color: var(--text);
            min-height: 3rem;
            width: 3rem;
            font-size: 20px;
            padding: 0;
        }

        .attach-button button:hover {
            background: #383838;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>"
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

with st.container():
    st.markdown("<div class='chatbar-wrapper'>", unsafe_allow_html=True)

    if st.session_state.attachment_menu_open:
        st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
        st.write("**Attach a file**")
        option_cols = st.columns([1, 1, 1, 1])

        with option_cols[0]:
            if st.button("PDF", key="upload_pdf"):
                st.session_state.attachment_type = "pdf"
        with option_cols[1]:
            if st.button("Image", key="upload_image"):
                st.session_state.attachment_type = "image"
        with option_cols[2]:
            if st.button("DOCX", key="upload_docx"):
                st.session_state.attachment_type = "docx"
        with option_cols[3]:
            if st.button("TXT", key="upload_txt"):
                st.session_state.attachment_type = "txt"

        st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

    st.markdown("<div class='chatbar-row'>", unsafe_allow_html=True)
    cols = st.columns([0.08, 0.92], gap="small")
    with cols[0]:
        if st.button("+", key="attach_btn", help="Upload a file"):
            st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
            st.session_state.attachment_type = None
    with cols[1]:
        prompt = st.chat_input("Message Student Teacher Agent")
    st.markdown("</div>", unsafe_allow_html=True)

    st.markdown("</div>", unsafe_allow_html=True)

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260608000539.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        .chatbar-wrapper {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            padding: 0 0.5rem;
            pointer-events: auto;
        }

        .chatbar-row {
            position: relative;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            padding: 0.55rem 0.8rem;
        }

        .chatbar-row .stButton > button {
            min-width: 3rem;
            min-height: 3rem;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #2f2f2f;
            color: var(--text);
            font-size: 20px;
            padding: 0;
        }

        .chatbar-row .stButton > button:hover {
            background: #383838;
        }

        .chatbar-row [data-testid="stChatInput"] {
            flex: 1;
        }

        .chatbar-row [data-testid="stChatInput"] textarea {
            background: transparent;
            border: none;
            min-height: 3rem;
            padding: 0.75rem 1rem;
            color: var(--text);
        }

        .attachment-popup {
            position: absolute;
            bottom: calc(100% + 0.5rem);
            left: 0;
            width: 100%;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.25);
        }

        .upload-preview {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 108px;
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            z-index: 10009;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-success {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 78px;
            color: #8df57f;
            font-size: 0.92rem;
            margin: 0;
            z-index: 10009;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>"
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

with st.container():
    st.markdown("<div class='chatbar-wrapper'>", unsafe_allow_html=True)

    if st.session_state.attachment_menu_open:
        st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
        st.write("**Attach a file**")
        option_cols = st.columns([1, 1, 1, 1])

        with option_cols[0]:
            if st.button("PDF", key="upload_pdf"):
                st.session_state.attachment_type = "pdf"
        with option_cols[1]:
            if st.button("Image", key="upload_image"):
                st.session_state.attachment_type = "image"
        with option_cols[2]:
            if st.button("DOCX", key="upload_docx"):
                st.session_state.attachment_type = "docx"
        with option_cols[3]:
            if st.button("TXT", key="upload_txt"):
                st.session_state.attachment_type = "txt"

        st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

    st.markdown("<div class='chatbar-row'>", unsafe_allow_html=True)
    cols = st.columns([0.08, 0.92], gap="small")
    with cols[0]:
        if st.button("+", key="attach_btn", help="Upload a file"):
            st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
            st.session_state.attachment_type = None
    with cols[1]:
        prompt = st.chat_input("Message Student Teacher Agent")
    st.markdown("</div>", unsafe_allow_html=True)

    st.markdown("</div>", unsafe_allow_html=True)

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260608000546.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        .chatbar-wrapper {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            padding: 0 0.5rem;
            pointer-events: auto;
        }

        .chatbar-row {
            position: relative;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            padding: 0.55rem 0.8rem;
        }

        .chatbar-row .stButton > button {
            min-width: 3rem;
            min-height: 3rem;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #2f2f2f;
            color: var(--text);
            font-size: 20px;
            padding: 0;
        }

        .chatbar-row .stButton > button:hover {
            background: #383838;
        }

        .chatbar-row [data-testid="stChatInput"] {
            flex: 1;
        }

        .chatbar-row [data-testid="stChatInput"] textarea {
            background: transparent;
            border: none;
            min-height: 3rem;
            padding: 0.75rem 1rem;
            color: var(--text);
        }

        .attachment-popup {
            position: absolute;
            bottom: calc(100% + 0.5rem);
            left: 0;
            width: 100%;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.25);
        }

        .upload-preview {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 108px;
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            z-index: 10009;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-success {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 78px;
            color: #8df57f;
            font-size: 0.92rem;
            margin: 0;
            z-index: 10009;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>"
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

with st.container():
    st.markdown("<div class='chatbar-wrapper'>", unsafe_allow_html=True)

    if st.session_state.attachment_menu_open:
        st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
        st.write("**Attach a file**")
        option_cols = st.columns([1, 1, 1, 1])

        with option_cols[0]:
            if st.button("PDF", key="upload_pdf"):
                st.session_state.attachment_type = "pdf"
        with option_cols[1]:
            if st.button("Image", key="upload_image"):
                st.session_state.attachment_type = "image"
        with option_cols[2]:
            if st.button("DOCX", key="upload_docx"):
                st.session_state.attachment_type = "docx"
        with option_cols[3]:
            if st.button("TXT", key="upload_txt"):
                st.session_state.attachment_type = "txt"

        st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

    st.markdown("<div class='chatbar-row'>", unsafe_allow_html=True)
    cols = st.columns([0.08, 0.92], gap="small")
    with cols[0]:
        if st.button("+", key="attach_btn", help="Upload a file"):
            st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
            st.session_state.attachment_type = None
    with cols[1]:
        prompt = st.chat_input("Message Student Teacher Agent")
    st.markdown("</div>", unsafe_allow_html=True)

    st.markdown("</div>", unsafe_allow_html=True)

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260608000907.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        .chatbar-wrapper {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            padding: 0 0.5rem;
            pointer-events: auto;
        }

        .chatbar-row {
            position: relative;
            display: flex;
            align-items: center;
            gap: 0;
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            padding: 0.25rem;
        }

        .chatbar-row .stButton {
            margin: 0;
            padding: 0;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .chatbar-row .stButton > button {
            min-width: 3rem;
            min-height: 3rem;
            border-radius: 999px 0 0 999px;
            border: 1px solid #3f3f3f;
            border-right: none;
            background: #252525;
            color: var(--text);
            font-size: 20px;
            padding: 0;
        }

        .chatbar-row .stButton > button:hover {
            background: #323232;
        }

        .chatbar-row [data-testid="stChatInput"] {
            flex: 1;
            margin: 0;
        }

        .chatbar-row [data-testid="stChatInput"] textarea {
            background: transparent;
            border: none;
            min-height: 3rem;
            padding: 0.75rem 1rem;
            color: var(--text);
            border-radius: 0 18px 18px 0;
        }

        .chatbar-row [data-testid="stChatInput"] textarea:focus {
            outline: none;
            box-shadow: none;
        }

        .attachment-popup {
            position: absolute;
            bottom: calc(100% + 0.5rem);
            left: 0;
            width: 100%;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.25);
        }

        .upload-preview {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 108px;
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            z-index: 10009;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-success {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 78px;
            color: #8df57f;
            font-size: 0.92rem;
            margin: 0;
            z-index: 10009;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>"
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

with st.container():
    st.markdown("<div class='chatbar-wrapper'>", unsafe_allow_html=True)

    if st.session_state.attachment_menu_open:
        st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
        st.write("**Attach a file**")
        option_cols = st.columns([1, 1, 1, 1])

        with option_cols[0]:
            if st.button("PDF", key="upload_pdf"):
                st.session_state.attachment_type = "pdf"
        with option_cols[1]:
            if st.button("Image", key="upload_image"):
                st.session_state.attachment_type = "image"
        with option_cols[2]:
            if st.button("DOCX", key="upload_docx"):
                st.session_state.attachment_type = "docx"
        with option_cols[3]:
            if st.button("TXT", key="upload_txt"):
                st.session_state.attachment_type = "txt"

        st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

    st.markdown("<div class='chatbar-row'>", unsafe_allow_html=True)
    cols = st.columns([0.08, 0.92], gap="small")
    with cols[0]:
        if st.button("+", key="attach_btn", help="Upload a file"):
            st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
            st.session_state.attachment_type = None
    with cols[1]:
        prompt = st.chat_input("Message Student Teacher Agent")
    st.markdown("</div>", unsafe_allow_html=True)

    st.markdown("</div>", unsafe_allow_html=True)

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260608000917.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        .chatbar-wrapper {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            padding: 0 0.5rem;
            pointer-events: auto;
        }

        .chatbar-row {
            position: relative;
            display: flex;
            align-items: center;
            gap: 0;
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            padding: 0.25rem;
        }

        .chatbar-row .stButton {
            margin: 0;
            padding: 0;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .chatbar-row .stButton > button {
            min-width: 3rem;
            min-height: 3rem;
            border-radius: 999px 0 0 999px;
            border: 1px solid #3f3f3f;
            border-right: none;
            background: #252525;
            color: var(--text);
            font-size: 20px;
            padding: 0;
        }

        .chatbar-row .stButton > button:hover {
            background: #323232;
        }

        .chatbar-row [data-testid="stChatInput"] {
            flex: 1;
            margin: 0;
        }

        .chatbar-row [data-testid="stChatInput"] textarea {
            background: transparent;
            border: none;
            min-height: 3rem;
            padding: 0.75rem 1rem;
            color: var(--text);
            border-radius: 0 18px 18px 0;
        }

        .chatbar-row [data-testid="stChatInput"] textarea:focus {
            outline: none;
            box-shadow: none;
        }

        .attachment-popup {
            position: absolute;
            bottom: calc(100% + 0.5rem);
            left: 0;
            width: 100%;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.25);
        }

        .upload-preview {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 108px;
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            z-index: 10009;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-success {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 78px;
            color: #8df57f;
            font-size: 0.92rem;
            margin: 0;
            z-index: 10009;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>"
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

with st.container():
    st.markdown("<div class='chatbar-wrapper'>", unsafe_allow_html=True)

    if st.session_state.attachment_menu_open:
        st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
        st.write("**Attach a file**")
        option_cols = st.columns([1, 1, 1, 1])

        with option_cols[0]:
            if st.button("PDF", key="upload_pdf"):
                st.session_state.attachment_type = "pdf"
        with option_cols[1]:
            if st.button("Image", key="upload_image"):
                st.session_state.attachment_type = "image"
        with option_cols[2]:
            if st.button("DOCX", key="upload_docx"):
                st.session_state.attachment_type = "docx"
        with option_cols[3]:
            if st.button("TXT", key="upload_txt"):
                st.session_state.attachment_type = "txt"

        st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

    st.markdown("<div class='chatbar-row'>", unsafe_allow_html=True)
    cols = st.columns([0.08, 0.92], gap="none")
    with cols[0]:
        if st.button("+", key="attach_btn", help="Upload a file"):
            st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
            st.session_state.attachment_type = None
    with cols[1]:
        prompt = st.chat_input("Message Student Teacher Agent")
    st.markdown("</div>", unsafe_allow_html=True)

    st.markdown("</div>", unsafe_allow_html=True)

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\app_20260608000938.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        .chatbar-wrapper {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            padding: 0 0.5rem;
            pointer-events: auto;
        }

        .chatbar-row {
            position: relative;
            display: flex;
            align-items: center;
            gap: 0;
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            padding: 0.25rem;
        }

        .chatbar-row .stButton {
            margin: 0;
            padding: 0;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .chatbar-row .stButton > button {
            min-width: 3rem;
            min-height: 3rem;
            border-radius: 999px 0 0 999px;
            border: 1px solid #3f3f3f;
            border-right: none;
            background: #252525;
            color: var(--text);
            font-size: 20px;
            padding: 0;
        }

        .chatbar-row .stButton > button:hover {
            background: #323232;
        }

        .chatbar-row [data-testid="stChatInput"] {
            flex: 1;
            margin: 0;
        }

        .chatbar-row [data-testid="stChatInput"] textarea {
            background: transparent;
            border: none;
            min-height: 3rem;
            padding: 0.75rem 1rem;
            color: var(--text);
            border-radius: 0 18px 18px 0;
        }

        .chatbar-row [data-testid="stChatInput"] textarea:focus {
            outline: none;
            box-shadow: none;
        }

        .attachment-popup {
            position: absolute;
            bottom: calc(100% + 0.5rem);
            left: 0;
            width: 100%;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.25);
        }

        .upload-preview {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 108px;
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            z-index: 10009;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-success {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 78px;
            color: #8df57f;
            font-size: 0.92rem;
            margin: 0;
            z-index: 10009;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>"
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

with st.container():
    st.markdown("<div class='chatbar-wrapper'>", unsafe_allow_html=True)

    if st.session_state.attachment_menu_open:
        st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
        st.write("**Attach a file**")
        option_cols = st.columns([1, 1, 1, 1])

        with option_cols[0]:
            if st.button("PDF", key="upload_pdf"):
                st.session_state.attachment_type = "pdf"
        with option_cols[1]:
            if st.button("Image", key="upload_image"):
                st.session_state.attachment_type = "image"
        with option_cols[2]:
            if st.button("DOCX", key="upload_docx"):
                st.session_state.attachment_type = "docx"
        with option_cols[3]:
            if st.button("TXT", key="upload_txt"):
                st.session_state.attachment_type = "txt"

        st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

    st.markdown("<div class='chatbar-row'>", unsafe_allow_html=True)
    cols = st.columns([0.08, 0.92], gap="none")
    with cols[0]:
        if st.button("+", key="attach_btn", help="Upload a file"):
            st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
            st.session_state.attachment_type = None
    with cols[1]:
        prompt = st.chat_input("Message Student Teacher Agent")
    st.markdown("</div>", unsafe_allow_html=True)

    st.markdown("</div>", unsafe_allow_html=True)

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: .history\config_20260604233139.py
================================================================================



================================================================================
FILE: .history\config_20260605003752.py
================================================================================

from dotenv import load_dotenv
import os

load_dotenv()

OPENAI_API_KEY = os.getenv("OPENAI_API_KEY")

================================================================================
FILE: .history\config_20260605225644.py
================================================================================

from dotenv import load_dotenv
import os

load_dotenv()

NVIDIA_API_KEY = os.getenv("NVIDIA_API_KEY")

================================================================================
FILE: .history\config_20260605225912.py
================================================================================

from dotenv import load_dotenv
import os

load_dotenv()

NVIDIA_API_KEY = os.getenv("NVIDIA_API_KEY")

================================================================================
FILE: .history\config_20260605225913.py
================================================================================

from dotenv import load_dotenv
import os

load_dotenv()

NVIDIA_API_KEY = os.getenv("NVIDIA_API_KEY")

================================================================================
FILE: .history\config_20260605225914.py
================================================================================

from dotenv import load_dotenv
import os

load_dotenv()

NVIDIA_API_KEY = os.getenv("NVIDIA_API_KEY")

================================================================================
FILE: .history\export_project_20260605232706.py
================================================================================



================================================================================
FILE: .history\export_project_20260605232710.py
================================================================================

#!/usr/bin/env python3
# ============================================================================
# PROJECT EXTRACTION UTILITY
# ============================================================================
# Purpose: Generate a comprehensive report of the entire Python codebase
# Output: Single text file containing:
#         1. Visual tree structure of project directories/files
#         2. Complete content of all Python files
#         3. Summary statistics
# Usage: python Project_extract.py
# Use Case: Code review, documentation, sharing codebase overview
# ============================================================================

"""
Extract all Python files with their content and project structure into a single text file.
"""

import os
import sys
from pathlib import Path


def get_project_structure(root_dir, prefix="", max_depth=10, current_depth=0, ignore_dirs=None):
    """
    Generate a text representation of the project structure.
    
    Creates a tree-style visualization of directories and files.
    Recursively traverses subdirectories while respecting depth limits.
    
    Args:
        root_dir: Starting directory path
        prefix: String prefix for tree indentation
        max_depth: Maximum recursion depth to prevent infinite loops
        current_depth: Current recursion level
        ignore_dirs: Set of directory names to skip
    
    Returns:
        String containing formatted directory tree
    """
    if ignore_dirs is None:
        ignore_dirs = {".git", "__pycache__", ".pytest_cache", "*.egg-info", ".venv", "venv", "env", ".csv"}
    
    if current_depth >= max_depth:
        return ""
    
    structure = ""
    try:
        items = sorted(os.listdir(root_dir))
    except PermissionError:
        return ""
    
    dirs = []
    files = []
    
    for item in items:
        # Skip hidden files/dirs and common ignore patterns
        if item.startswith("."):
            continue
        if item in ignore_dirs or any(item.endswith(x.replace("*", "")) for x in ignore_dirs if "*" in x):
            continue
        
        item_path = os.path.join(root_dir, item)
        if os.path.isdir(item_path):
            dirs.append(item)
        else:
            files.append(item)
    
    # Add files first
    for i, file in enumerate(files):
        is_last_file = (i == len(files) - 1) and len(dirs) == 0
        structure += f"{prefix}{'└── ' if is_last_file else '├── '}{file}\n"
    
    # Add directories
    for i, dir_name in enumerate(dirs):
        is_last = i == len(dirs) - 1
        structure += f"{prefix}{'└── ' if is_last else '├── '}{dir_name}/\n"
        
        dir_path = os.path.join(root_dir, dir_name)
        extension = "    " if is_last else "│   "
        structure += get_project_structure(dir_path, prefix + extension, max_depth, current_depth + 1, ignore_dirs)
    
    return structure


def extract_python_files(root_dir, output_file):
    """
    Extract all Python files and their content.
    
    Main extraction function that:
    1. Walks through directory tree to find all .py files
    2. Generates project structure visualization
    3. Writes formatted output with all file contents
    4. Provides summary statistics
    
    Args:
        root_dir: Root directory to scan
        output_file: Path to output text file
    """
    py_files = []
    
    # Collect all Python files
    for root, dirs, files in os.walk(root_dir):
        # Skip certain directories
        dirs[:] = [d for d in dirs if d not in {".git", "__pycache__", ".pytest_cache", ".venv", "venv", "env", ".csv"}]
        
        for file in files:
            if file.endswith(".py"):
                file_path = os.path.join(root, file)
                py_files.append(file_path)
    
    # Sort files for consistent output
    py_files.sort()
    
    # Write to output file
    with open(output_file, "w", encoding="utf-8") as out:
        # Header
        out.write("=" * 80 + "\n")
        out.write("PROJECT EXTRACTION REPORT\n")
        out.write("=" * 80 + "\n\n")
        
        # Project Structure
        out.write("PROJECT STRUCTURE:\n")
        out.write("-" * 80 + "\n\n")
        out.write(f"{os.path.basename(root_dir)}/\n")
        out.write(get_project_structure(root_dir))
        out.write("\n\n")
        
        # File Contents
        out.write("=" * 80 + "\n")
        out.write("FILE CONTENTS\n")
        out.write("=" * 80 + "\n\n")
        
        for file_path in py_files:
            # Calculate relative path
            rel_path = os.path.relpath(file_path, root_dir)
            
            # Write file header
            out.write(f"\n{'=' * 80}\n")
            out.write(f"FILE: {rel_path}\n")
            out.write(f"{'=' * 80}\n\n")
            
            try:
                with open(file_path, "r", encoding="utf-8") as f:
                    content = f.read()
                    out.write(content)
            except Exception as e:
                out.write(f"[ERROR] Could not read file: {str(e)}\n")
            
            out.write("\n")
        
        # Summary
        out.write("\n" + "=" * 80 + "\n")
        out.write(f"SUMMARY: {len(py_files)} Python files extracted\n")
        out.write("=" * 80 + "\n")
    
    print(f"✓ Extraction complete!")
    print(f"✓ Total Python files: {len(py_files)}")
    print(f"✓ Output saved to: {output_file}")
    print(f"✓ File size: {os.path.getsize(output_file) / 1024:.2f} KB")


if __name__ == "__main__":
    # Get project root (current directory)
    project_root = os.getcwd()
    
    # Output file
    output_filename = "project_extraction.txt"
    output_path = os.path.join(project_root, output_filename)
    
    print(f"Extracting Python files from: {project_root}")
    print(f"Output file: {output_path}\n")
    
    extract_python_files(project_root, output_path)


================================================================================
FILE: .history\llm\__init___20260605232458.py
================================================================================



================================================================================
FILE: .history\llm\__init___20260605232504.py
================================================================================



================================================================================
FILE: .history\llm\nvidia_client_20260605233304.py
================================================================================

from openai import OpenAI


class NvidiaClient:

    def __init__(self, api_key):

        self.client = OpenAI(
            base_url="https://integrate.api.nvidia.com/v1",
            api_key=api_key
        )

    def ask(self, question):

        completion = self.client.chat.completions.create(
            model="meta/llama-3.1-70b-instruct",
            messages=[
                {
                    "role": "user",
                    "content": question
                }
            ]
        )

        return completion.choices[0].message.content


================================================================================
FILE: .history\llm\nvidia_client_20260605233307.py
================================================================================

from openai import OpenAI


class NvidiaClient:

    def __init__(self, api_key):

        self.client = OpenAI(
            base_url="https://integrate.api.nvidia.com/v1",
            api_key=api_key
        )

    def ask(self, question):

        completion = self.client.chat.completions.create(
            model="meta/llama-3.1-70b-instruct",
            messages=[
                {
                    "role": "user",
                    "content": question
                }
            ]
        )

        return completion.choices[0].message.content


================================================================================
FILE: .history\llm\nvidia_client_20260605234821.py
================================================================================

from openai import OpenAI


class NvidiaClient:

    def __init__(self, api_key):

        self.client = OpenAI(
            base_url="https://integrate.api.nvidia.com/v1",
            api_key=api_key
        )

    def ask(self, question):

        completion = self.client.chat.completions.create(
            model="meta/llama-3.1-8b-instruct"",
            messages=[
                {
                    "role": "user",
                    "content": question
                }
            ]
        )

        return completion.choices[0].message.content


================================================================================
FILE: .history\llm\nvidia_client_20260605234824.py
================================================================================

from openai import OpenAI


class NvidiaClient:

    def __init__(self, api_key):

        self.client = OpenAI(
            base_url="https://integrate.api.nvidia.com/v1",
            api_key=api_key
        )

    def ask(self, question):

        completion = self.client.chat.completions.create(
            model="meta/llama-3.1-8b-instruct",
            messages=[
                {
                    "role": "user",
                    "content": question
                }
            ]
        )

        return completion.choices[0].message.content


================================================================================
FILE: .history\llm\nvidia_client_20260605234825.py
================================================================================

from openai import OpenAI


class NvidiaClient:

    def __init__(self, api_key):

        self.client = OpenAI(
            base_url="https://integrate.api.nvidia.com/v1",
            api_key=api_key
        )

    def ask(self, question):

        completion = self.client.chat.completions.create(
            model="meta/llama-3.1-8b-instruct",
            messages=[
                {
                    "role": "user",
                    "content": question
                }
            ]
        )

        return completion.choices[0].message.content


================================================================================
FILE: .history\llm\nvidia_client_20260605234826.py
================================================================================

from openai import OpenAI


class NvidiaClient:

    def __init__(self, api_key):

        self.client = OpenAI(
            base_url="https://integrate.api.nvidia.com/v1",
            api_key=api_key
        )

    def ask(self, question):

        completion = self.client.chat.completions.create(
            model="meta/llama-3.1-8b-instruct",
            messages=[
                {
                    "role": "user",
                    "content": question
                }
            ]
        )

        return completion.choices[0].message.content


================================================================================
FILE: .history\llm\nvidia_client_20260605235154.py
================================================================================

from openai import OpenAI


class NvidiaClient:

    def __init__(self, api_key):

        self.client = OpenAI(
            base_url="https://integrate.api.nvidia.com/v1",
            api_key=api_key,
            timeout=30
        )

    def ask(self, question):

        print("Sending request to NVIDIA...")

        completion = self.client.chat.completions.create(
            model="meta/llama-3.1-8b-instruct",
            messages=[
                {
                    "role": "user",
                    "content": question
                }
            ],
            temperature=0.7,
            max_tokens=300
        )

        print("Response received!")

        return completion.choices[0].message.content

================================================================================
FILE: .history\llm\openai_client_20260605003521.py
================================================================================



================================================================================
FILE: .history\llm\openai_client_20260605003715.py
================================================================================

from openai import OpenAI


class OpenAIClient:

    def __init__(self, api_key):

        self.client = OpenAI(api_key=api_key)

    def ask(self, question):

        response = self.client.chat.completions.create(

            model="gpt-4o-mini",

            messages=[
                {
                    "role": "user",
                    "content": question
                }
            ]

        )

        return response.choices[0].message.content

================================================================================
FILE: .history\llm\openai_client_20260605003716.py
================================================================================

from openai import OpenAI


class OpenAIClient:

    def __init__(self, api_key):

        self.client = OpenAI(api_key=api_key)

    def ask(self, question):

        response = self.client.chat.completions.create(

            model="gpt-4o-mini",

            messages=[
                {
                    "role": "user",
                    "content": question
                }
            ]

        )

        return response.choices[0].message.content

================================================================================
FILE: .history\llm\test_nvidia_20260605231159.py
================================================================================



================================================================================
FILE: .history\llm\test_nvidia_20260605231205.py
================================================================================

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

client = NvidiaClient(NVIDIA_API_KEY)

response = client.ask("What is AI?")

print(response)

================================================================================
FILE: .history\llm\test_nvidia_20260605231709.py
================================================================================

from nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

client = NvidiaClient(NVIDIA_API_KEY)

response = client.ask("What is AI?")

print(response)

================================================================================
FILE: .history\main_20260604232331.py
================================================================================



================================================================================
FILE: .history\main_20260604234535.py
================================================================================

from agents.student_agent import StudentAgent

def main():
    print("=== Student Teacher Agent ===")

    student = StudentAgent()

    while True:
        question = input("\nYou: ")

        if question.lower() == "exit":
            print("Goodbye!")
            break

        response = student.ask_teacher(question)

        print(f"\nTeacher: {response}")


if __name__ == "__main__":
    main()

================================================================================
FILE: .history\main_20260604235445.py
================================================================================

from agents.student_agent import StudentAgent

def main():

    print("=== Student Teacher Agent ===")

    student = StudentAgent()

    while True:

        question = input("\nYou: ")

        if question.lower() == "exit":
            print("Goodbye!")
            break

        response = student.ask_teacher(question)

        print(f"\nTeacher: {response}")

        print("\nMemory:")
        print(student.teacher.memory.get_history())


if __name__ == "__main__":
    main()

================================================================================
FILE: .history\main_20260605003200.py
================================================================================

from agents.student_agent import StudentAgent

def main():

    print("=== Student Teacher Agent ===")

    student = StudentAgent()

    while True:

        question = input("\nYou: ")

        if question.lower() == "exit":
            print("Goodbye!")
            break

        response = student.ask_teacher(question)

        print(f"\nTeacher: {response}")

        print("\nMemory:")
       #S print(student.teacher.memory.get_history())


if __name__ == "__main__":
    main()

================================================================================
FILE: .history\main_20260605003215.py
================================================================================

from agents.student_agent import StudentAgent

def main():

    print("=== Student Teacher Agent ===")

    student = StudentAgent()

    while True:

        question = input("\nYou: ")

        if question.lower() == "exit":
            print("Goodbye!")
            break

        response = student.ask_teacher(question)

        print(f"\nTeacher: {response}")

        print("\nMemory:")
       # print(student.teacher.memory.get_history())


if __name__ == "__main__":
    main()

================================================================================
FILE: .history\memory\__init___20260604234133.py
================================================================================



================================================================================
FILE: .history\memory\__init___20260604234134.py
================================================================================



================================================================================
FILE: .history\memory\memory_20260604232436.py
================================================================================



================================================================================
FILE: .history\memory\memory_20260604234134.py
================================================================================



================================================================================
FILE: .history\memory\memory_20260604235144.py
================================================================================

class Memory:

    def __init__(self):
        self.chat_history = []

    def save(self, role, message):

        self.chat_history.append(
            {
                "role": role,
                "message": message
            }
        )

    def get_history(self):

        return self.chat_history

================================================================================
FILE: .history\memory\memory_20260605001907.py
================================================================================

import json


class Memory:

    def __init__(self):

        self.file_path = "data/chat_history.json"

        self.chat_history = self.load_history()

    def load_history(self):

        try:

            with open(self.file_path, "r") as file:

                return json.load(file)

        except:

            return []

    def save(self, role, message):

        self.chat_history.append(
            {
                "role": role,
                "message": message
            }
        )

        self.save_history()

    def save_history(self):

        with open(self.file_path, "w") as file:

            json.dump(
                self.chat_history,
                file,
                indent=4
            )

    def get_history(self):

        return self.chat_history

================================================================================
FILE: .history\memory\memory_20260605001908.py
================================================================================

import json


class Memory:

    def __init__(self):

        self.file_path = "data/chat_history.json"

        self.chat_history = self.load_history()

    def load_history(self):

        try:

            with open(self.file_path, "r") as file:

                return json.load(file)

        except:

            return []

    def save(self, role, message):

        self.chat_history.append(
            {
                "role": role,
                "message": message
            }
        )

        self.save_history()

    def save_history(self):

        with open(self.file_path, "w") as file:

            json.dump(
                self.chat_history,
                file,
                indent=4
            )

    def get_history(self):

        return self.chat_history

================================================================================
FILE: .history\memory\memory_20260605001909.py
================================================================================

import json


class Memory:

    def __init__(self):

        self.file_path = "data/chat_history.json"

        self.chat_history = self.load_history()

    def load_history(self):

        try:

            with open(self.file_path, "r") as file:

                return json.load(file)

        except:

            return []

    def save(self, role, message):

        self.chat_history.append(
            {
                "role": role,
                "message": message
            }
        )

        self.save_history()

    def save_history(self):

        with open(self.file_path, "w") as file:

            json.dump(
                self.chat_history,
                file,
                indent=4
            )

    def get_history(self):

        return self.chat_history

================================================================================
FILE: .history\memory\memory_20260605001911.py
================================================================================

import json


class Memory:

    def __init__(self):

        self.file_path = "data/chat_history.json"

        self.chat_history = self.load_history()

    def load_history(self):

        try:

            with open(self.file_path, "r") as file:

                return json.load(file)

        except:

            return []

    def save(self, role, message):

        self.chat_history.append(
            {
                "role": role,
                "message": message
            }
        )

        self.save_history()

    def save_history(self):

        with open(self.file_path, "w") as file:

            json.dump(
                self.chat_history,
                file,
                indent=4
            )

    def get_history(self):

        return self.chat_history

================================================================================
FILE: .history\rag\chunker_20260606153056.py
================================================================================



================================================================================
FILE: .history\rag\chunker_20260606153105.py
================================================================================

chunker.py

================================================================================
FILE: .history\rag\chunker_20260606153106.py
================================================================================

chunker.py

================================================================================
FILE: .history\rag\chunker_20260606153107.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter

def create_chunks(text):

```
splitter = RecursiveCharacterTextSplitter(
    chunk_size=1000,
    chunk_overlap=200
)

chunks = splitter.split_text(text)

return chunks
```


================================================================================
FILE: .history\rag\chunker_20260606153108.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter

def create_chunks(text):

```
splitter = RecursiveCharacterTextSplitter(
    chunk_size=1000,
    chunk_overlap=200
)

chunks = splitter.split_text(text)

return chunks
```


================================================================================
FILE: .history\rag\chunker_20260606153120.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter

def create_chunks(text):

```
splitter = RecursiveCharacterTextSplitter(
    chunk_size=1000,
    chunk_overlap=200
)

chunks = splitter.split_text(text)

return chunks
```


================================================================================
FILE: .history\rag\chunker_20260606153236.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter


def create_chunks(text):
    splitter = RecursiveCharacterTextSplitter(
        chunk_size=1000,
        chunk_overlap=200
    )
    chunks = splitter.split_text(text)
    return chunks


================================================================================
FILE: .history\rag\chunker_20260606153238.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter


def create_chunks(text):
    splitter = RecursiveCharacterTextSplitter(
        chunk_size=1000,
        chunk_overlap=200
    )
    chunks = splitter.split_text(text)
    return chunks


================================================================================
FILE: .history\rag\chunker_20260606153242.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter


def create_chunks(text):
    splitter = RecursiveCharacterTextSplitter(
        chunk_size=1000,
        chunk_overlap=200
    )
    chunks = splitter.split_text(text)
    return chunks


================================================================================
FILE: .history\rag\chunker_20260606224849.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter


def create_chunks(text):

    splitter = RecursiveCharacterTextSplitter(
        chunk_size=500,
        chunk_overlap=100
    )

    chunks = splitter.split_text(text)


================================================================================
FILE: .history\rag\chunker_20260606224859.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter


def create_chunks(text):

    splitter = RecursiveCharacterTextSplitter(
        chunk_size=500,
        chunk_overlap=100
    )

    chunks = splitter.split_text(text)


================================================================================
FILE: .history\rag\chunker_20260606225420.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter


def create_chunks(text):
    # Handle list of dicts from pdf_reader
    if isinstance(text, list):
        text = "\n".join([item.get("text", "") for item in text])
    
    splitter = RecursiveCharacterTextSplitter(
        chunk_size=500,
        chunk_overlap=100
    )

    chunks = splitter.split_text(text)
    return chunks


================================================================================
FILE: .history\rag\chunker_20260606225434.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter


def create_chunks(text):
    # Handle list of dicts from pdf_reader
    if isinstance(text, list):
        text = "\n".join([item.get("text", "") for item in text])
    
    splitter = RecursiveCharacterTextSplitter(
        chunk_size=500,
        chunk_overlap=100
    )

    chunks = splitter.split_text(text)
    return chunks


================================================================================
FILE: .history\rag\chunker_20260606233007.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter


def create_chunks(text):

    splitter = RecursiveCharacterTextSplitter(
        chunk_size=1000,
        chunk_overlap=200
    )

    chunks = splitter.split_text(text)

    return chunks

================================================================================
FILE: .history\rag\chunker_20260606233008.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter


def create_chunks(text):

    splitter = RecursiveCharacterTextSplitter(
        chunk_size=1000,
        chunk_overlap=200
    )

    chunks = splitter.split_text(text)

    return chunks

================================================================================
FILE: .history\rag\chunker_20260606233208.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter


def create_chunks(text):
    # Handle list of dicts from pdf_reader
    if isinstance(text, list):
        text = "\n".join([item.get("text", "") for item in text])
    
    splitter = RecursiveCharacterTextSplitter(
        chunk_size=500,
        chunk_overlap=100
    )

    chunks = splitter.split_text(text)
    return chunks


================================================================================
FILE: .history\rag\chunker_20260606233302.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter


def create_chunks(text):

    splitter = RecursiveCharacterTextSplitter(

        chunk_size=1000,
        chunk_overlap=200,

        separators=[
            "\n\n",
            "\n",
            ". ",
            " ",
            ""
        ]
    )

    chunks = splitter.split_text(text)

    return chunks

================================================================================
FILE: .history\rag\chunker_20260606233303.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter


def create_chunks(text):

    splitter = RecursiveCharacterTextSplitter(

        chunk_size=1000,
        chunk_overlap=200,

        separators=[
            "\n\n",
            "\n",
            ". ",
            " ",
            ""
        ]
    )

    chunks = splitter.split_text(text)

    return chunks

================================================================================
FILE: .history\rag\embedding_model_20260606154824.py
================================================================================



================================================================================
FILE: .history\rag\embedding_model_20260606154832.py
================================================================================

from sentence_transformers import SentenceTransformer

model = SentenceTransformer(
    "all-MiniLM-L6-v2"
)

def create_embeddings(chunks):

    embeddings = model.encode(chunks)

    return embeddings

================================================================================
FILE: .history\rag\embedding_model_20260606225635.py
================================================================================

from sentence_transformers import SentenceTransformer

# Lazy load - only when needed
_model = None

def get_model():
    global _model
    if _model is None:
        print("Loading embedding model...")
        _model = SentenceTransformer("all-MiniLM-L6-v2")
    return _model

def create_embeddings(chunks):
    model = get_model()
    embeddings = model.encode(chunks, show_progress_bar=False)
    return embeddings

================================================================================
FILE: .history\rag\embedding_model_20260606225711.py
================================================================================

from sentence_transformers import SentenceTransformer

# Lazy load - only when needed
_model = None

def get_model():
    global _model
    if _model is None:
        print("Loading embedding model...")
        _model = SentenceTransformer("all-MiniLM-L6-v2")
    return _model

def create_embeddings(chunks):
    model = get_model()
    embeddings = model.encode(chunks, show_progress_bar=False)
    return embeddings

================================================================================
FILE: .history\rag\embedding_model_20260606233250.py
================================================================================

from sentence_transformers import SentenceTransformer

# Load only once
_model = None


def get_model():

    global _model

    if _model is None:

        print("Loading embedding model...")

        _model = SentenceTransformer(
            "all-MiniLM-L6-v2"
        )

    return _model


def create_embeddings(chunks):

    model = get_model()

    embeddings = model.encode(
        chunks,
        show_progress_bar=False
    )

    return embeddings

================================================================================
FILE: .history\rag\pdf_reader_20260606004044.py
================================================================================

from pypdf import PdfReader


def read_pdf(pdf_path):
    reader = PdfReader(pdf_path)
    text = ""
    for page in reader.pages:
        page_text = page.extract_text()
        if page_text:
            text += page_text + "\n"
    return text


================================================================================
FILE: .history\rag\pdf_reader_20260606224524.py
================================================================================

from pypdf import PdfReader


def read_pdf(pdf_path):

    reader = PdfReader(pdf_path)

    pages = []

    for page_num, page in enumerate(reader.pages):

        page_text = page.extract_text()

        if page_text:

            pages.append(
                {
                    "page": page_num + 1,
                    "text": page_text
                }
            )

    return pages

================================================================================
FILE: .history\rag\pdf_reader_20260606233722.py
================================================================================

from pypdf import PdfReader


def read_pdf(pdf_path):
    reader = PdfReader(pdf_path)
    text = ""
    for page in reader.pages:
        page_text = page.extract_text()
        if page_text:
            text += page_text + "\n"
    return text

================================================================================
FILE: .history\rag\pdf_reader_20260606233815.py
================================================================================

from pypdf import PdfReader


def read_pdf(pdf_path):
    reader = PdfReader(pdf_path)
    text = ""
    for page in reader.pages:
        page_text = page.extract_text()
        if page_text:
            text += page_text + "\n"
    return text

================================================================================
FILE: .history\rag\rag_chat_20260606161608.py
================================================================================



================================================================================
FILE: .history\rag\rag_chat_20260606161613.py
================================================================================

rag_chat.py

================================================================================
FILE: .history\rag\rag_chat_20260606161614.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore
from retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question
        )

        context = "\n\n".join(docs)

        prompt = f"""
Use the context below to answer.

Context:
{context}

Question:
{question}
"""

        return self.llm.ask(prompt)

================================================================================
FILE: .history\rag\rag_chat_20260606161615.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore
from retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question
        )

        context = "\n\n".join(docs)

        prompt = f"""
Use the context below to answer.

Context:
{context}

Question:
{question}
"""

        return self.llm.ask(prompt)

================================================================================
FILE: .history\rag\rag_chat_20260606175706.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore
from retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:

I could not find that information in the PDF.

4. Do not guess.
5. Do not make assumptions.
6. Keep answers clear and concise.

Context:
{context}

Question:
{question}

Answer:
"""

        return self.llm.ask(prompt)

================================================================================
FILE: .history\rag\rag_chat_20260606175707.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore
from retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:

I could not find that information in the PDF.

4. Do not guess.
5. Do not make assumptions.
6. Keep answers clear and concise.

Context:
{context}

Question:
{question}

Answer:
"""

        return self.llm.ask(prompt)

================================================================================
FILE: .history\rag\rag_chat_20260606180235.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore
from retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:

I could not find that information in the PDF.

4. Do not guess.
5. Do not make assumptions.
6. Keep answers clear and concise.

Context:
{context}

Question:
{question}

Answer:
"""

        return self.llm.ask(prompt)

================================================================================
FILE: .history\rag\rag_chat_20260606180242.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore
from retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:

I could not find that information in the PDF.

4. Do not guess.
5. Do not make assumptions.
6. Keep answers clear and concise.

Context:
{context}

Question:
{question}

Answer:
"""

        return self.llm.ask(prompt)

================================================================================
FILE: .history\rag\rag_chat_20260606224929.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore
from retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260606224930.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore
from retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260606231246.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore
from retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260606231247.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore
from retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260606231307.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore
from retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260606233749.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260606233815.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607012154.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607012343.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )
        
        
        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607012346.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )
        
        print(type(embeddings))
        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607012347.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )
        
        print(type(embeddings))
        
        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607012348.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )
        
        print(type(embeddings))
        
        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607012655.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)
        
        
        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )
        
        print(type(embeddings))

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607012658.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)
        
        with open("chunks.pkl", "wb") as f:
    pickle.dump(self.chunks, f)
        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )
        
        print(type(embeddings))

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607012701.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)
        
        with open("chunks.pkl", "wb") as f:
        pickle.dump(self.chunks, f)
        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )
        
        print(type(embeddings))

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607012703.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)
        
        with open("chunks.pkl", "wb") as f:
        
        pickle.dump(self.chunks, f)
        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )
        
        print(type(embeddings))

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607012705.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)
        
        with open("chunks.pkl", "wb") as f:
        pickle.dump(self.chunks, f)
        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )
        
        print(type(embeddings))

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607012708.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)
        
        with open("chunks.pkl", "wb") as f:
        pickle.dump(self.chunks, f)
        
        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )
        
        print(type(embeddings))

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607012712.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)
        
        with open("chunks.pkl", "wb") as f:
          pickle.dump(self.chunks, f)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )
        
        print(type(embeddings))

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607012714.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)
        
        with open("chunks.pkl", "wb") as f:
           pickle.dump(self.chunks, f)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )
        
        print(type(embeddings))

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607012715.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)
        
        with open("chunks.pkl", "wb") as f:
           pickle.dump(self.chunks, f)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )
        
        print(type(embeddings))

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607012716.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)
        
        with open("chunks.pkl", "wb") as f:
           pickle.dump(self.chunks, f)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )
        
        print(type(embeddings))

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607012959.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)
        
        with open("chunks.pkl", "wb") as f:
           pickle.dump(self.chunks, f)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )
        
        print(type(embeddings))

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )
        

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607013000.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)
        
        with open("chunks.pkl", "wb") as f:
           pickle.dump(self.chunks, f)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )
        
        print(type(embeddings))

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607013002.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)
        
        with open("chunks.pkl", "wb") as f:
           pickle.dump(self.chunks, f)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )
        
        print(type(embeddings))

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        store.save("faiss_index.bin")

print("FAISS Saved!")

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607013013.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)
        
        with open("chunks.pkl", "wb") as f:
           pickle.dump(self.chunks, f)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )
        
        print(type(embeddings))

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        store.save("faiss_index.bin")

       print("FAISS Saved!")

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607013015.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)
        
        with open("chunks.pkl", "wb") as f:
           pickle.dump(self.chunks, f)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )
        
        print(type(embeddings))

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        store.save("faiss_index.bin")

        print("FAISS Saved!")

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607013016.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)
        
        with open("chunks.pkl", "wb") as f:
           pickle.dump(self.chunks, f)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )
        
        print(type(embeddings))

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        store.save("faiss_index.bin")

        print("FAISS Saved!")

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607013017.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)
        
        with open("chunks.pkl", "wb") as f:
           pickle.dump(self.chunks, f)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )
        
        print(type(embeddings))

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        store.save("faiss_index.bin")

        print("FAISS Saved!")

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607013018.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)
        
        with open("chunks.pkl", "wb") as f:
           pickle.dump(self.chunks, f)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )
        
        print(type(embeddings))

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        store.save("faiss_index.bin")

        print("FAISS Saved!")

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607013020.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)
        
        with open("chunks.pkl", "wb") as f:
           pickle.dump(self.chunks, f)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )
        
        print(type(embeddings))

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        store.save("faiss_index.bin")

        print("FAISS Saved!")

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607013749.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

    store = VectorStore()

    # Load existing files if available
    if (
        os.path.exists("chunks.pkl")
        and os.path.exists("faiss_index.bin")
    ):

        print("Loading Saved Chunks...")

        with open(
            "chunks.pkl",
            "rb"
        ) as f:

            self.chunks = pickle.load(f)

        print("Loading Saved FAISS...")

        store.load(
            "faiss_index.bin"
        )

    else:

        print("Loading PDF...")

        text = read_pdf(
            "sample.pdf"
        )

        print("Creating Chunks...")

        self.chunks = create_chunks(
            text
        )

        with open(
            "chunks.pkl",
            "wb"
        ) as f:

            pickle.dump(
                self.chunks,
                f
            )

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        store.save(
            "faiss_index.bin"
        )

        print("FAISS Saved!")

    self.retriever = Retriever(
        self.chunks,
        store
    )

    self.llm = NvidiaClient(
        NVIDIA_API_KEY
    )IDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607013753.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

    store = VectorStore()

    # Load existing files if available
    if (
        os.path.exists("chunks.pkl")
        and os.path.exists("faiss_index.bin")
    ):

        print("Loading Saved Chunks...")

        with open(
            "chunks.pkl",
            "rb"
        ) as f:

            self.chunks = pickle.load(f)

        print("Loading Saved FAISS...")

        store.load(
            "faiss_index.bin"
        )

    else:

        print("Loading PDF...")

        text = read_pdf(
            "sample.pdf"
        )

        print("Creating Chunks...")

        self.chunks = create_chunks(
            text
        )

        with open(
            "chunks.pkl",
            "wb"
        ) as f:

            pickle.dump(
                self.chunks,
                f
            )

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        store.save(
            "faiss_index.bin"
        )

        print("FAISS Saved!")

    self.retriever = Retriever(
        self.chunks,
        store
    )

    self.llm = NvidiaClient(
        NVIDIA_API_KEY
    )IDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607013824.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)
        
        with open("chunks.pkl", "wb") as f:
           pickle.dump(self.chunks, f)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )
        
        print(type(embeddings))

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        store.save("faiss_index.bin")

        print("FAISS Saved!")

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607013900.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

   def __init__(self):

    store = VectorStore()

    # Load existing files if available
    if (
        os.path.exists("chunks.pkl")
        and os.path.exists("faiss_index.bin")
    ):

        print("Loading Saved Chunks...")

        with open(
            "chunks.pkl",
            "rb"
        ) as f:

            self.chunks = pickle.load(f)

        print("Loading Saved FAISS...")

        store.load(
            "faiss_index.bin"
        )

    else:

        print("Loading PDF...")

        text = read_pdf(
            "sample.pdf"
        )

        print("Creating Chunks...")

        self.chunks = create_chunks(
            text
        )

        with open(
            "chunks.pkl",
            "wb"
        ) as f:

            pickle.dump(
                self.chunks,
                f
            )

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        store.save(
            "faiss_index.bin"
        )

        print("FAISS Saved!")

    self.retriever = Retriever(
        self.chunks,
        store
    )

    self.llm = NvidiaClient(
        NVIDIA_API_KEY
    )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607013903.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

   def __init__(self):

    store = VectorStore()

    # Load existing files if available
    if (
        os.path.exists("chunks.pkl")
        and os.path.exists("faiss_index.bin")
    ):

        print("Loading Saved Chunks...")

        with open(
            "chunks.pkl",
            "rb"
        ) as f:

            self.chunks = pickle.load(f)

        print("Loading Saved FAISS...")

        store.load(
            "faiss_index.bin"
        )

    else:

        print("Loading PDF...")

        text = read_pdf(
            "sample.pdf"
        )

        print("Creating Chunks...")

        self.chunks = create_chunks(
            text
        )

        with open(
            "chunks.pkl",
            "wb"
        ) as f:

            pickle.dump(
                self.chunks,
                f
            )

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        store.save(
            "faiss_index.bin"
        )

        print("FAISS Saved!")

    self.retriever = Retriever(
        self.chunks,
        store
    )

    self.llm = NvidiaClient(
        NVIDIA_API_KEY
    )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260607013904.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

   def __init__(self):

    store = VectorStore()

    # Load existing files if available
    if (
        os.path.exists("chunks.pkl")
        and os.path.exists("faiss_index.bin")
    ):

        print("Loading Saved Chunks...")

        with open(
            "chunks.pkl",
            "rb"
        ) as f:

            self.chunks = pickle.load(f)

        print("Loading Saved FAISS...")

        store.load(
            "faiss_index.bin"
        )

    else:

        print("Loading PDF...")

        text = read_pdf(
            "sample.pdf"
        )

        print("Creating Chunks...")

        self.chunks = create_chunks(
            text
        )

        with open(
            "chunks.pkl",
            "wb"
        ) as f:

            pickle.dump(
                self.chunks,
                f
            )

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        store.save(
            "faiss_index.bin"
        )

        print("FAISS Saved!")

    self.retriever = Retriever(
        self.chunks,
        store
    )

    self.llm = NvidiaClient(
        NVIDIA_API_KEY
    )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\retriever_20260606160936.py
================================================================================



================================================================================
FILE: .history\rag\retriever_20260606160942.py
================================================================================

from embedding_model import create_embeddings


class Retriever:

    def __init__(self, chunks, vector_store):

        self.chunks = chunks
        self.vector_store = vector_store

    def retrieve(self, question, k=3):

        query_embedding = create_embeddings(
            [question]
        )[0]

        indices = self.vector_store.search(
            query_embedding,
            k
        )

        results = []

        for idx in indices:

            results.append(
                self.chunks[idx]
            )

        return results

================================================================================
FILE: .history\rag\retriever_20260606160943.py
================================================================================

from embedding_model import create_embeddings


class Retriever:

    def __init__(self, chunks, vector_store):

        self.chunks = chunks
        self.vector_store = vector_store

    def retrieve(self, question, k=3):

        query_embedding = create_embeddings(
            [question]
        )[0]

        indices = self.vector_store.search(
            query_embedding,
            k
        )

        results = []

        for idx in indices:

            results.append(
                self.chunks[idx]
            )

        return results

================================================================================
FILE: .history\rag\retriever_20260606181345.py
================================================================================

from embedding_model import model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        query_embedding = model.encode(query)

        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        for idx in indices:
            results.append(
                self.chunks[idx]
            )

        return results

================================================================================
FILE: .history\rag\retriever_20260606181346.py
================================================================================

from embedding_model import model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        query_embedding = model.encode(query)

        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        for idx in indices:
            results.append(
                self.chunks[idx]
            )

        return results

================================================================================
FILE: .history\rag\retriever_20260606225032.py
================================================================================

from embedding_model import model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        query_embedding = model.encode(query)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results

================================================================================
FILE: .history\rag\retriever_20260606225033.py
================================================================================

from embedding_model import model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        query_embedding = model.encode(query)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results

================================================================================
FILE: .history\rag\retriever_20260606225910.py
================================================================================

from embedding_model import get_model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        model = get_model()
        query_embedding = model.encode(query, show_progress_bar=False)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results


    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        query_embedding = model.encode(query)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results

================================================================================
FILE: .history\rag\retriever_20260606225920.py
================================================================================

from embedding_model import get_model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        model = get_model()
        query_embedding = model.encode(query, show_progress_bar=False)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results


    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        query_embedding = model.encode(query)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results

================================================================================
FILE: .history\rag\retriever_20260606225931.py
================================================================================

from embedding_model import get_model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        model = get_model()
        query_embedding = model.encode(query, show_progress_bar=False)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results


    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        query_embedding = model.encode(query, show_progress_bar=False)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results

================================================================================
FILE: .history\rag\retriever_20260606225938.py
================================================================================

from embedding_model import get_model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        model = get_model()
        query_embedding = model.encode(query, show_progress_bar=False)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results


    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        query_embedding = model.encode(query, show_progress_bar=False)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results

================================================================================
FILE: .history\rag\retriever_20260606231418.py
================================================================================

from embedding_model import model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        query_embedding = model.encode(query)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results

================================================================================
FILE: .history\rag\retriever_20260606231419.py
================================================================================

from embedding_model import model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        query_embedding = model.encode(query)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results

================================================================================
FILE: .history\rag\retriever_20260606231714.py
================================================================================

from embedding_model import get_model


class Retriever:

    def __init__(self, chunks, store):
        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):
        # Convert question into embedding
        model = get_model()
        query_embedding = model.encode(query, show_progress_bar=False)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:
            if idx < len(self.chunks):
                results.append(
                    self.chunks[idx]
                )

        return results


================================================================================
FILE: .history\rag\retriever_20260606231716.py
================================================================================

from embedding_model import get_model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        model = get_model()
        query_embedding = model.encode(query, show_progress_bar=False)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results


    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        query_embedding = model.encode(query, show_progress_bar=False)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results

================================================================================
FILE: .history\rag\retriever_20260606231718.py
================================================================================

from embedding_model import get_model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        model = get_model()
        query_embedding = model.encode(query, show_progress_bar=False)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results


    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        query_embedding = model.encode(query, show_progress_bar=False)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results

================================================================================
FILE: .history\rag\retriever_20260606232353.py
================================================================================

from sentence_transformers import SentenceTransformer


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

        print("Loading embedding model...")

        self.model = SentenceTransformer(
            "sentence-transformers/all-MiniLM-L6-v2"
        )

    def retrieve(self, query, k=5):

        query_embedding = self.model.encode(
            query,
            show_progress_bar=False
        )

        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        for idx in indices:

            if 0 <= idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results

================================================================================
FILE: .history\rag\retriever_20260607011312.py
================================================================================

from rag.embedding_model import get_model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

        self.model = get_model()

    def retrieve(self, query, k=5):

        query_embedding = self.model.encode(
            query,
            show_progress_bar=False
        )

        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        for idx in indices:

            if 0 <= idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results

================================================================================
FILE: .history\rag\retriever_20260607011313.py
================================================================================

from rag.embedding_model import get_model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

        self.model = get_model()

    def retrieve(self, query, k=5):

        query_embedding = self.model.encode(
            query,
            show_progress_bar=False
        )

        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        for idx in indices:

            if 0 <= idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results

================================================================================
FILE: .history\rag\test_chunks_20260606153139.py
================================================================================



================================================================================
FILE: .history\rag\test_chunks_20260606153148.py
================================================================================

test_chunks.py

================================================================================
FILE: .history\rag\test_chunks_20260606153149.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks

text = read_pdf("../sample.pdf")

chunks = create_chunks(text)

print("Total Chunks:", len(chunks))

print("\nFirst Chunk:\n")
print(chunks[0])


================================================================================
FILE: .history\rag\test_chunks_20260606153150.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks

text = read_pdf("../sample.pdf")

chunks = create_chunks(text)

print("Total Chunks:", len(chunks))

print("\nFirst Chunk:\n")
print(chunks[0])


================================================================================
FILE: .history\rag\test_chunks_20260606153151.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks

text = read_pdf("../sample.pdf")

chunks = create_chunks(text)

print("Total Chunks:", len(chunks))

print("\nFirst Chunk:\n")
print(chunks[0])


================================================================================
FILE: .history\rag\test_chunks_20260606153735.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks

text = read_pdf("sample.pdf")

chunks = create_chunks(text)

print("Total Chunks:", len(chunks))

print("\nFirst Chunk:\n")
print(chunks[0])


================================================================================
FILE: .history\rag\test_chunks_20260606153744.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks

text = read_pdf("sample.pdf")

chunks = create_chunks(text)

print("Total Chunks:", len(chunks))

print("\nFirst Chunk:\n")
print(chunks[0])


================================================================================
FILE: .history\rag\test_embeddings_20260606154908.py
================================================================================



================================================================================
FILE: .history\rag\test_embeddings_20260606154915.py
================================================================================

test_embeddings.py

================================================================================
FILE: .history\rag\test_embeddings_20260606154916.py
================================================================================

test_embeddings.py

================================================================================
FILE: .history\rag\test_embeddings_20260606154917.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings

text = read_pdf("sample.pdf")

chunks = create_chunks(text)

embeddings = create_embeddings(chunks)

print("Total Chunks:", len(chunks))
print("Total Embeddings:", len(embeddings))

print("\nEmbedding Shape:")
print(embeddings.shape)

================================================================================
FILE: .history\rag\test_embeddings_20260606154918.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings

text = read_pdf("sample.pdf")

chunks = create_chunks(text)

embeddings = create_embeddings(chunks)

print("Total Chunks:", len(chunks))
print("Total Embeddings:", len(embeddings))

print("\nEmbedding Shape:")
print(embeddings.shape)

================================================================================
FILE: .history\rag\test_embeddings_20260606154920.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings

text = read_pdf("sample.pdf")

chunks = create_chunks(text)

embeddings = create_embeddings(chunks)

print("Total Chunks:", len(chunks))
print("Total Embeddings:", len(embeddings))

print("\nEmbedding Shape:")
print(embeddings.shape)

================================================================================
FILE: .history\rag\test_faiss_20260606160203.py
================================================================================



================================================================================
FILE: .history\rag\test_faiss_20260606160208.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore

text = read_pdf("sample.pdf")

chunks = create_chunks(text)

embeddings = create_embeddings(chunks)

store = VectorStore(embeddings)

query = "What are Python functions?"

query_embedding = create_embeddings(
    [query]
)[0]

results = store.search(
    query_embedding,
    k=3
)

print("Best Chunks:\n")

for idx in results:

    print("=" * 50)

    print(chunks[idx][:500])

    print("\n")

================================================================================
FILE: .history\rag\test_faiss_20260606160209.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore

text = read_pdf("sample.pdf")

chunks = create_chunks(text)

embeddings = create_embeddings(chunks)

store = VectorStore(embeddings)

query = "What are Python functions?"

query_embedding = create_embeddings(
    [query]
)[0]

results = store.search(
    query_embedding,
    k=3
)

print("Best Chunks:\n")

for idx in results:

    print("=" * 50)

    print(chunks[idx][:500])

    print("\n")

================================================================================
FILE: .history\rag\test_faiss_20260606160211.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore

text = read_pdf("sample.pdf")

chunks = create_chunks(text)

embeddings = create_embeddings(chunks)

store = VectorStore(embeddings)

query = "What are Python functions?"

query_embedding = create_embeddings(
    [query]
)[0]

results = store.search(
    query_embedding,
    k=3
)

print("Best Chunks:\n")

for idx in results:

    print("=" * 50)

    print(chunks[idx][:500])

    print("\n")

================================================================================
FILE: .history\rag\test_pdf_20260606003633.py
================================================================================

from rag.pdf_reader import read_pdf

text = read_pdf("sample.pdf")

print(text[:1000])


================================================================================
FILE: .history\rag\test_pdf_20260606004527.py
================================================================================

from pdf_reader import read_pdf

text = read_pdf("sample.pdf")

print(text[:1000])

================================================================================
FILE: .history\rag\test_rag_20260606161636.py
================================================================================



================================================================================
FILE: .history\rag\test_rag_20260606161648.py
================================================================================

from rag_chat import RAGChat

bot = RAGChat()

while True:

    question = input("\nAsk: ")

    if question.lower() == "exit":
        break

    answer = bot.ask(question)

    print("\nAnswer:\n")

    print(answer)

================================================================================
FILE: .history\rag\test_rag_20260606161649.py
================================================================================

from rag_chat import RAGChat

bot = RAGChat()

while True:

    question = input("\nAsk: ")

    if question.lower() == "exit":
        break

    answer = bot.ask(question)

    print("\nAnswer:\n")

    print(answer)

================================================================================
FILE: .history\rag\test_rag_20260606161837.py
================================================================================

from rag_chat import RAGChat
import sys
import os

BASE_DIR = os.path.dirname(
    os.path.dirname(os.path.abspath(__file__))
)

sys.path.append(BASE_DIR)
bot = RAGChat()

while True:

    question = input("\nAsk: ")

    if question.lower() == "exit":
        break

    answer = bot.ask(question)

    print("\nAnswer:\n")

    print(answer)

================================================================================
FILE: .history\rag\test_rag_20260606161844.py
================================================================================

from rag_chat import RAGChat
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import sys
import os

BASE_DIR = os.path.dirname(
    os.path.dirname(os.path.abspath(__file__))
)

sys.path.append(BASE_DIR)
bot = RAGChat()

while True:

    question = input("\nAsk: ")

    if question.lower() == "exit":
        break

    answer = bot.ask(question)

    print("\nAnswer:\n")

    print(answer)

================================================================================
FILE: .history\rag\test_rag_20260606161938.py
================================================================================

import sys
import os

BASE_DIR = os.path.dirname(
    os.path.dirname(os.path.abspath(__file__))
)

sys.path.append(BASE_DIR)

from rag_chat import RAGChat

bot = RAGChat()

while True:

    question = input("\nAsk: ")

    if question.lower() == "exit":
        break

    answer = bot.ask(question)

    print("\nAnswer:\n")

    print(answer)

================================================================================
FILE: .history\rag\test_rag_20260606233753.py
================================================================================

import sys
import os

BASE_DIR = os.path.dirname(
    os.path.dirname(os.path.abspath(__file__))
)

sys.path.append(BASE_DIR)

from rag.rag_chat import RAGChat

bot = RAGChat()

while True:

    question = input("\nAsk: ")

    if question.lower() == "exit":
        break

    answer = bot.ask(question)

    print("\nAnswer:\n")

    print(answer)

================================================================================
FILE: .history\rag\test_rag_20260606233815.py
================================================================================

import sys
import os

BASE_DIR = os.path.dirname(
    os.path.dirname(os.path.abspath(__file__))
)

sys.path.append(BASE_DIR)

from rag.rag_chat import RAGChat

bot = RAGChat()

while True:

    question = input("\nAsk: ")

    if question.lower() == "exit":
        break

    answer = bot.ask(question)

    print("\nAnswer:\n")

    print(answer)

================================================================================
FILE: .history\rag\test_retriever_20260606161005.py
================================================================================



================================================================================
FILE: .history\rag\test_retriever_20260606161013.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore
from retriever import Retriever


text = read_pdf("sample.pdf")

chunks = create_chunks(text)

embeddings = create_embeddings(chunks)

store = VectorStore(embeddings)

retriever = Retriever(
    chunks,
    store
)

results = retriever.retrieve(
    "What are Python functions?"
)

for i, chunk in enumerate(results):

    print("\n")
    print("=" * 50)

    print(f"RESULT {i+1}")

    print("=" * 50)

    print(chunk[:500])

================================================================================
FILE: .history\rag\vector_store_20260606160052.py
================================================================================



================================================================================
FILE: .history\rag\vector_store_20260606160104.py
================================================================================

import faiss
import numpy as np


class VectorStore:

    def __init__(self, embeddings):

        self.index = faiss.IndexFlatL2(
            embeddings.shape[1]
        )

        self.index.add(
            np.array(embeddings).astype("float32")
        )

    def search(self, query_embedding, k=3):

        distances, indices = self.index.search(
            np.array([query_embedding]).astype("float32"),
            k
        )

        return indices[0]

================================================================================
FILE: .history\rag\vector_store_20260606181323.py
================================================================================

import faiss
import numpy as np


class VectorStore:

    def __init__(self, embeddings):

        self.index = faiss.IndexFlatL2(
            embeddings.shape[1]
        )

        self.index.add(
            np.array(embeddings).astype("float32")
        )

    def search(self, query_embedding, k=5):

        distances, indices = self.index.search(
            np.array([query_embedding]).astype("float32"),
            k
        )

        return indices[0], distances[0]

================================================================================
FILE: .history\rag\vector_store_20260606181339.py
================================================================================

import faiss
import numpy as np


class VectorStore:

    def __init__(self, embeddings):

        self.index = faiss.IndexFlatL2(
            embeddings.shape[1]
        )

        self.index.add(
            np.array(embeddings).astype("float32")
        )

    def search(self, query_embedding, k=5):

        distances, indices = self.index.search(
            np.array([query_embedding]).astype("float32"),
            k
        )

        return indices[0], distances[0]

================================================================================
FILE: .history\rag\vector_store_20260607010357.py
================================================================================

import faiss
import numpy as np
import os


class VectorStore:

    def __init__(self, embeddings=None):

        self.index = None

        if embeddings is not None:

            self.index = faiss.IndexFlatL2(
                embeddings.shape[1]
            )

            self.index.add(
                np.array(
                    embeddings
                ).astype("float32")
            )

    def save(self, path="faiss_index.bin"):

        faiss.write_index(
            self.index,
            path
        )

    def load(self, path="faiss_index.bin"):

        self.index = faiss.read_index(
            path
        )

    def exists(self, path="faiss_index.bin"):

        return os.path.exists(path)

    def search(self, query_embedding, k=5):

        distances, indices = self.index.search(
            np.array(
                [query_embedding]
            ).astype("float32"),
            k
        )

        return indices[0], distances[0]

================================================================================
FILE: .history\rag\vector_store_20260607011957.py
================================================================================

import faiss
import numpy as np
import os


class VectorStore:

    def __init__(self, embeddings=None):

        self.index = None

        if embeddings is not None:

            self.index = faiss.IndexFlatL2(
                embeddings.shape[1]
            )

            self.index.add(
                np.array(
                    embeddings
                ).astype("float32")
            )

    def save(self, path="faiss_index.bin"):

        faiss.write_index(
            self.index,
            path
        )

    def load(self, path="faiss_index.bin"):

        self.index = faiss.read_index(
            path
        )

    def exists(self, path="faiss_index.bin"):

        return os.path.exists(path)

    def search(self, query_embedding, k=5):

        distances, indices = self.index.search(
            np.array(
                [query_embedding]
            ).astype("float32"),
            k
        )

        return indices[0], distances[0]

================================================================================
FILE: .history\rag\vector_store_20260607011959.py
================================================================================

import faiss
import numpy as np
import os


class VectorStore:

    def __init__(self, embeddings=None):

        self.index = None

        if embeddings is not None:

            self.index = faiss.IndexFlatL2(
                embeddings.shape[1]
            )

            self.index.add(
                np.array(
                    embeddings
                ).astype("float32")
            )

    def save(self, path="faiss_index.bin"):

        faiss.write_index(
            self.index,
            path
        )

    def load(self, path="faiss_index.bin"):

        self.index = faiss.read_index(
            path
        )

    def exists(self, path="faiss_index.bin"):

        return os.path.exists(path)

    def search(self, query_embedding, k=5):

        distances, indices = self.index.search(
            np.array(
                [query_embedding]
            ).astype("float32"),
            k
        )

        return indices[0], distances[0]

================================================================================
FILE: .history\rag\vector_store_20260607012004.py
================================================================================

import faiss
import numpy as np
import os


class VectorStore:

    def __init__(self, embeddings=None):

        self.index = None

        if embeddings is not None:

            self.index = faiss.IndexFlatL2(
                embeddings.shape[1]
            )

            self.index.add(
                np.array(
                    embeddings
                ).astype("float32")
            )

    def save(self, path="faiss_index.bin"):

        faiss.write_index(
            self.index,
            path
        )

    def load(self, path="faiss_index.bin"):

        self.index = faiss.read_index(
            path
        )

    def exists(self, path="faiss_index.bin"):

        return os.path.exists(path)

    def search(self, query_embedding, k=5):

        distances, indices = self.index.search(
            np.array(
                [query_embedding]
            ).astype("float32"),
            k
        )

        return indices[0], distances[0]

================================================================================
FILE: .history\rag\vector_store_20260607012006.py
================================================================================

import faiss
import numpy as np
import os


class VectorStore:

    def __init__(self, embeddings=None):

        self.index = None

        if embeddings is not None:

            self.index = faiss.IndexFlatL2(
                embeddings.shape[1]
            )

            self.index.add(
                np.array(
                    embeddings
                ).astype("float32")
            )

    def save(self, path="faiss_index.bin"):

        faiss.write_index(
            self.index,
            path
        )

    def load(self, path="faiss_index.bin"):

        self.index = faiss.read_index(
            path
        )

    def exists(self, path="faiss_index.bin"):

        return os.path.exists(path)

    def search(self, query_embedding, k=5):

        distances, indices = self.index.search(
            np.array(
                [query_embedding]
            ).astype("float32"),
            k
        )

        return indices[0], distances[0]

================================================================================
FILE: .history\test_key_20260605005015.py
================================================================================



================================================================================
FILE: .history\test_key_20260605005026.py
================================================================================

from config import OPENAI_API_KEY

print(OPENAI_API_KEY)

================================================================================
FILE: .history\test_key_20260605005027.py
================================================================================

from config import OPENAI_API_KEY

print(OPENAI_API_KEY)

================================================================================
FILE: .history\test_nvidia_20260605231830.py
================================================================================

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

client = NvidiaClient(NVIDIA_API_KEY)

response = client.ask("What is AI?")

print(response)

================================================================================
FILE: .history\test_nvidia_20260605232538.py
================================================================================

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

client = NvidiaClient(NVIDIA_API_KEY)

response = client.ask("What is AI?")

print(response)

================================================================================
FILE: .history\test_nvidia_key_20260605225939.py
================================================================================



================================================================================
FILE: .history\test_nvidia_key_20260605225945.py
================================================================================

from config import NVIDIA_API_KEY

print(NVIDIA_API_KEY)

================================================================================
FILE: .history\test_openai_20260605005105.py
================================================================================



================================================================================
FILE: .history\test_openai_20260605005111.py
================================================================================

from llm.openai_client import OpenAIClient
from config import OPENAI_API_KEY

client = OpenAIClient(OPENAI_API_KEY)

response = client.ask("What is AI?")

print(response)

================================================================================
FILE: .history\test_openai_20260605005112.py
================================================================================

from llm.openai_client import OpenAIClient
from config import OPENAI_API_KEY

client = OpenAIClient(OPENAI_API_KEY)

response = client.ask("What is AI?")

print(response)

================================================================================
FILE: .history\test_openai_20260605005113.py
================================================================================

from llm.openai_client import OpenAIClient
from config import OPENAI_API_KEY

client = OpenAIClient(OPENAI_API_KEY)

response = client.ask("What is AI?")

print(response)

================================================================================
FILE: .history\test_openai_20260605005114.py
================================================================================

from llm.openai_client import OpenAIClient
from config import OPENAI_API_KEY

client = OpenAIClient(OPENAI_API_KEY)

response = client.ask("What is AI?")

print(response)

================================================================================
FILE: .history\tools\__init___20260604234133.py
================================================================================



================================================================================
FILE: .history\tools\__init___20260604234135.py
================================================================================



================================================================================
FILE: .history\tools\calculator_tool_20260604234134.py
================================================================================



================================================================================
FILE: .history\tools\calculator_tool_20260604234135.py
================================================================================



================================================================================
FILE: .history\tools\calculator_tool_20260605000108.py
================================================================================

class CalculatorTool:

    def calculate(self, expression):

        try:
            result = eval(expression)
            return result

        except:
            return "Invalid Calculation"

================================================================================
FILE: .history\tools\calculator_tool_20260605000212.py
================================================================================


class CalculatorTool:

    def calculate(self, expression):

        try:
            result = eval(expression)
            return result

        except:
            return "Invalid Calculation"

================================================================================
FILE: .history\tools\calculator_tool_20260605000215.py
================================================================================

from tools.calculator_tool import CalculatorTool
class CalculatorTool:

    def calculate(self, expression):

        try:
            result = eval(expression)
            return result

        except:
            return "Invalid Calculation"

================================================================================
FILE: .history\tools\calculator_tool_20260605000422.py
================================================================================

class CalculatorTool:

    def calculate(self, expression):

        try:
            result = eval(expression)
            return result

        except:
            return "Invalid Calculation"

================================================================================
FILE: .history\tools\rag\pdf_reader_20260606003602.py
================================================================================



================================================================================
FILE: .history\tools\rag\pdf_reader_20260606003609.py
================================================================================

from pypdf import PdfReader

def read_pdf(pdf_path):

```
reader = PdfReader(pdf_path)

text = ""

for page in reader.pages:

    text += page.extract_text()

return text
```


================================================================================
FILE: .history\tools\rag\pdf_reader_20260606003611.py
================================================================================

from pypdf import PdfReader

def read_pdf(pdf_path):

```
reader = PdfReader(pdf_path)

text = ""

for page in reader.pages:

    text += page.extract_text()

return text
```


================================================================================
FILE: .history\tools\rag\pdf_reader_20260606003614.py
================================================================================

from pypdf import PdfReader

def read_pdf(pdf_path):

```

reader = PdfReader(pdf_path)

text = ""

for page in reader.pages:

    text += page.extract_text()

return text
```


================================================================================
FILE: .history\tools\rag\pdf_reader_20260606003616.py
================================================================================

from pypdf import PdfReader

def read_pdf(pdf_path):

```
reader = PdfReader(pdf_path)

text = ""

for page in reader.pages:

    text += page.extract_text()

return text
```


================================================================================
FILE: .history\tools\rag\pdf_reader_20260606003717.py
================================================================================

from pypdf import PdfReader


def read_pdf(pdf_path):
    reader = PdfReader(pdf_path)
    text = ""
    for page in reader.pages:
        text += page.extract_text() or ""
    return text


================================================================================
FILE: .history\tools\rag\pdf_reader_20260606003719.py
================================================================================

from pypdf import PdfReader


def read_pdf(pdf_path):
    reader = PdfReader(pdf_path)
    text = ""
    for page in reader.pages:
        text += page.extract_text() or ""
    return text


================================================================================
FILE: .history\tools\rag\pdf_reader_20260606003906.py
================================================================================

from pypdf import PdfReader

def read_pdf(pdf_path):

```
reader = PdfReader(pdf_path)

text = ""

for page in reader.pages:

    page_text = page.extract_text()

    if page_text:

        text += page_text + "\n"

return text
```


================================================================================
FILE: .history\tools\rag\pdf_reader_20260606003908.py
================================================================================

from pypdf import PdfReader


def read_pdf(pdf_path):
    reader = PdfReader(pdf_path)
    text = ""
    for page in reader.pages:
        text += page.extract_text() or ""
    return text


================================================================================
FILE: .history\tools\rag\pdf_reader_20260606003909.py
================================================================================

from pypdf import PdfReader

def read_pdf(pdf_path):

```
reader = PdfReader(pdf_path)

text = ""

for page in reader.pages:

    page_text = page.extract_text()

    if page_text:

        text += page_text + "\n"

return text
```


================================================================================
FILE: .history\tools\rag\pdf_reader_20260606003910.py
================================================================================

from pypdf import PdfReader

def read_pdf(pdf_path):

```
reader = PdfReader(pdf_path)

text = ""

for page in reader.pages:

    page_text = page.extract_text()

    if page_text:

        text += page_text + "\n"

return text
```


================================================================================
FILE: .history\tools\rag\pdf_reader_20260606004033.py
================================================================================

from pypdf import PdfReader


def read_pdf(pdf_path):
    reader = PdfReader(pdf_path)
    text = ""
    for page in reader.pages:
        page_text = page.extract_text()
        if page_text:
            text += page_text + "\n"
    return text


================================================================================
FILE: .history\tools\rag\pdf_reader_20260606004045.py
================================================================================

from pypdf import PdfReader


def read_pdf(pdf_path):
    reader = PdfReader(pdf_path)
    text = ""
    for page in reader.pages:
        page_text = page.extract_text()
        if page_text:
            text += page_text + "\n"
    return text


================================================================================
FILE: .history\tools\rag\test_pdf_20260606003623.py
================================================================================



================================================================================
FILE: .history\tools\rag\test_pdf_20260606003632.py
================================================================================

from rag.pdf_reader import read_pdf

text = read_pdf("sample.pdf")

print(text[:1000])


================================================================================
FILE: .history\tools\rag\test_pdf_20260606003633.py
================================================================================

from rag.pdf_reader import read_pdf

text = read_pdf("sample.pdf")

print(text[:1000])


================================================================================
FILE: .history\tools\rag\test_pdf_20260606003634.py
================================================================================

from rag.pdf_reader import read_pdf

text = read_pdf("sample.pdf")

print(text[:1000])


================================================================================
FILE: .history\tools\search_tool_20260604234134.py
================================================================================



================================================================================
FILE: .history\tools\search_tool_20260604234135.py
================================================================================



================================================================================
FILE: .history\tools\search_tool_20260605001037.py
================================================================================

class SearchTool:

    def search(self, query):

        return f"Searching for: {query}"

================================================================================
FILE: .history\tools\search_tool_20260605001038.py
================================================================================

class SearchTool:

    def search(self, query):

        return f"Searching for: {query}"

================================================================================
FILE: .history\tools\search_tool_20260605001039.py
================================================================================

class SearchTool:

    def search(self, query):

        return f"Searching for: {query}"

================================================================================
FILE: .history\tools\search_tool_20260606001605.py
================================================================================

from duckduckgo_search import DDGS


class SearchTool:

    def search(self, query):

        try:

            with DDGS() as ddgs:

                results = list(
                    ddgs.text(
                        query,
                        max_results=5
                    )
                )

            answer = ""

            for i, result in enumerate(results, start=1):

                answer += (
                    f"{i}. {result['title']}\n"
                    f"{result['href']}\n\n"
                )

            return answer

        except Exception as e:

            return f"Search Error: {e}"

================================================================================
FILE: .history\tools\search_tool_20260606001606.py
================================================================================

from duckduckgo_search import DDGS


class SearchTool:

    def search(self, query):

        try:

            with DDGS() as ddgs:

                results = list(
                    ddgs.text(
                        query,
                        max_results=5
                    )
                )

            answer = ""

            for i, result in enumerate(results, start=1):

                answer += (
                    f"{i}. {result['title']}\n"
                    f"{result['href']}\n\n"
                )

            return answer

        except Exception as e:

            return f"Search Error: {e}"

================================================================================
FILE: .history\tools\search_tool_20260606001855.py
================================================================================

from duckduckgo_search import DDGS

class SearchTool:

```
def search(self, query):

    try:

        print("Searching:", query)

        with DDGS() as ddgs:

            results = list(ddgs.text(query, max_results=5))

        print(results)

        return str(results)

    except Exception as e:

        return f"ERROR: {e}"
```


================================================================================
FILE: .history\tools\search_tool_20260606001856.py
================================================================================

from duckduckgo_search import DDGS

class SearchTool:

```
def search(self, query):

    try:

        print("Searching:", query)

        with DDGS() as ddgs:

            results = list(ddgs.text(query, max_results=5))

        print(results)

        return str(results)

    except Exception as e:

        return f"ERROR: {e}"
```


================================================================================
FILE: .history\tools\search_tool_20260606002030.py
================================================================================

from duckduckgo_search import DDGS

class SearchTool:

```
def search(self, query):

    try:

        print("Searching:", query)

        with DDGS() as ddgs:

            results = list(ddgs.text(query, max_results=5))

        answer = ""

        for i, result in enumerate(results, start=1):

            answer += f"{i}. {result['title']}\n"
            answer += f"{result['href']}\n\n"

        return answer

    except Exception as e:

        return f"Search Error: {e}"
```


================================================================================
FILE: .history\tools\search_tool_20260606002031.py
================================================================================

from duckduckgo_search import DDGS

class SearchTool:

```
def search(self, query):

    try:

        print("Searching:", query)

        with DDGS() as ddgs:

            results = list(ddgs.text(query, max_results=5))

        answer = ""

        for i, result in enumerate(results, start=1):

            answer += f"{i}. {result['title']}\n"
            answer += f"{result['href']}\n\n"

        return answer

    except Exception as e:

        return f"Search Error: {e}"
```


================================================================================
FILE: .history\tools\search_tool_20260606002034.py
================================================================================

from duckduckgo_search import DDGS

class SearchTool:

```
def search(self, query):

    try:

        print("Searching:", query)

        with DDGS() as ddgs:

            results = list(ddgs.text(query, max_results=5))

        answer = ""

        for i, result in enumerate(results, start=1):

            answer += f"{i}. {result['title']}\n"
            answer += f"{result['href']}\n\n"

        return answer

    except Exception as e:

        return f"Search Error: {e}"
```


================================================================================
FILE: .history\tools\search_tool_20260606002044.py
================================================================================

from duckduckgo_search import DDGS

class SearchTool:

```
def search(self, query):

    try:

        print("Searching:", query)

        with DDGS() as ddgs:

            results = list(ddgs.text(query, max_results=5))

        answer = ""

        for i, result in enumerate(results, start=1):

            answer += f"{i}. {result['title']}\n"
            answer += f"{result['href']}\n\n"

        return answer

    except Exception as e:

        return f"Search Error: {e}"



================================================================================
FILE: .history\tools\search_tool_20260606002045.py
================================================================================

from duckduckgo_search import DDGS

class SearchTool:

```
def search(self, query):

    try:

        print("Searching:", query)

        with DDGS() as ddgs:

            results = list(ddgs.text(query, max_results=5))

        answer = ""

        for i, result in enumerate(results, start=1):

            answer += f"{i}. {result['title']}\n"
            answer += f"{result['href']}\n\n"

        return answer

    except Exception as e:

        return f"Search Error: {e}"



================================================================================
FILE: .history\tools\search_tool_20260606002049.py
================================================================================

from duckduckgo_search import DDGS

class SearchTool:

```
  def search(self, query):

    try:

        print("Searching:", query)

        with DDGS() as ddgs:

            results = list(ddgs.text(query, max_results=5))

        answer = ""

        for i, result in enumerate(results, start=1):

            answer += f"{i}. {result['title']}\n"
            answer += f"{result['href']}\n\n"

        return answer

    except Exception as e:

        return f"Search Error: {e}"



================================================================================
FILE: .history\tools\search_tool_20260606002051.py
================================================================================

from duckduckgo_search import DDGS

class SearchTool:

```
    def search(self, query):

    try:

        print("Searching:", query)

        with DDGS() as ddgs:

            results = list(ddgs.text(query, max_results=5))

        answer = ""

        for i, result in enumerate(results, start=1):

            answer += f"{i}. {result['title']}\n"
            answer += f"{result['href']}\n\n"

        return answer

    except Exception as e:

        return f"Search Error: {e}"



================================================================================
FILE: .history\tools\search_tool_20260606002053.py
================================================================================

from duckduckgo_search import DDGS

class SearchTool:

```
def search(self, query):

    try:

        print("Searching:", query)

        with DDGS() as ddgs:

            results = list(ddgs.text(query, max_results=5))

        answer = ""

        for i, result in enumerate(results, start=1):

            answer += f"{i}. {result['title']}\n"
            answer += f"{result['href']}\n\n"

        return answer

    except Exception as e:

        return f"Search Error: {e}"
```


================================================================================
FILE: .history\tools\search_tool_20260606002142.py
================================================================================

from duckduckgo_search import DDGS


class SearchTool:

    def search(self, query):
        try:
            print("Searching:", query)
            with DDGS() as ddgs:
                results = list(ddgs.text(query, max_results=5))

            answer = ""
            for i, result in enumerate(results, start=1):
                answer += f"{i}. {result['title']}\n"
                answer += f"{result['href']}\n\n"

            return answer

        except Exception as e:
            return f"Search Error: {e}"


================================================================================
FILE: .history\tools\search_tool_20260606002322.py
================================================================================

from duckduckgo_search import DDGS


class SearchTool:

    def search(self, query):
        try:
            print("Searching:", query)
            with DDGS() as ddgs:
                results = list(ddgs.text(query, max_results=5))

            answer = ""
            for i, result in enumerate(results, start=1):
                answer += f"{i}. {result['title']}\n"
                answer += f"{result['href']}\n\n"

            return answer

        except Exception as e:
            return f"Search Error: {e}"


================================================================================
FILE: .history\tools\search_tool_20260606002325.py
================================================================================

from duckduckgo_search import DDGS


class SearchTool:

    def search(self, query):
        try:
            print("Searching:", query)
            with DDGS() as ddgs:
                results = list(ddgs.text(query, max_results=5))

            answer = ""
            for i, result in enumerate(results, start=1):
                answer += f"{i}. {result['title']}\n"
                answer += f"{result['href']}\n\n"

            return answer

        except Exception as e:
            return f"Search Error: {e}"
            


================================================================================
FILE: .history\tools\search_tool_20260606002540.py
================================================================================

from duckduckgo_search import DDGS


class SearchTool:

    def search(self, query):
        try:
            print("Searching:", query)
            with DDGS() as ddgs:
                results = list(ddgs.text(query, max_results=5))

            answer = ""
            for i, result in enumerate(results, start=1):
                answer += f"{i}. {result['title']}\n"
                answer += f"{result['href']}\n\n"

            return answer

        except Exception as e:
            return f"Search Error: {e}"
            


================================================================================
FILE: .history\tools\search_tool_20260606002650.py
================================================================================

from ddgs import DDGS

class SearchTool:

```
def search(self, query):

    try:

        print("Searching:", query)

        results = DDGS().text(query, max_results=5)

        answer = ""

        for i, result in enumerate(results, start=1):

            answer += f"{i}. {result.get('title', 'No Title')}\n"

            if 'href' in result:
                answer += f"{result['href']}\n\n"
            elif 'url' in result:
                answer += f"{result['url']}\n\n"

        return answer if answer else "No results found."

    except Exception as e:

        return f"Search Error: {e}"
```


================================================================================
FILE: .history\tools\search_tool_20260606002750.py
================================================================================

from ddgs import DDGS


class SearchTool:

    def search(self, query):
        try:
            print("Searching:", query)
            results = DDGS().text(query, max_results=5)

            answer = ""
            for i, result in enumerate(results, start=1):
                answer += f"{i}. {result.get('title', 'No Title')}\n"
                if 'href' in result:
                    answer += f"{result['href']}\n\n"
                elif 'url' in result:
                    answer += f"{result['url']}\n\n"

            return answer if answer else "No results found."

        except Exception as e:
            return f"Search Error: {e}"


================================================================================
FILE: .history\tools\search_tool_20260606002753.py
================================================================================

from ddgs import DDGS


class SearchTool:

    def search(self, query):
        try:
            print("Searching:", query)
            results = DDGS().text(query, max_results=5)

            answer = ""
            for i, result in enumerate(results, start=1):
                answer += f"{i}. {result.get('title', 'No Title')}\n"
                if 'href' in result:
                    answer += f"{result['href']}\n\n"
                elif 'url' in result:
                    answer += f"{result['url']}\n\n"

            return answer if answer else "No results found."

        except Exception as e:
            return f"Search Error: {e}"


================================================================================
FILE: agents\__init__.py
================================================================================



================================================================================
FILE: agents\answer_generator_agent.py
================================================================================

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class AnswerGeneratorAgent:

    def __init__(self):

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def generate(self, formatted_answer):

        prompt = f"""
You are ChatGPT.

Generate a final polished answer.

Requirements:

- Professional formatting
- Easy language
- Bullet points
- Key takeaways
- Examples when relevant
- Proper conclusion
- Preserve markdown headings and URLs
- Do not add unsupported facts
- Never expose raw SEARCH RESULT or RAG RESULT dumps
- Keep the answer concise but complete
- Use ONLY the content provided below
- Do not add outside facts, version numbers, dates, corrections, or new claims
- Do not change the topic
- If the content contains PDF Findings and Search Findings, preserve those sections and the comparison meaning
- If the content compares Python functions from PDF with Python news, do not convert it into a Python version comparison
- For comparison content, output only:
  # Comparison Report
  ## PDF Findings
  ## Search Findings
  ## Similarities
  ## Differences
  ## Final Conclusion
- Do not add Summary, Key Points, Detailed Explanation, Example, or Conclusion sections to comparison content

Content:

{formatted_answer}
"""

        return self.llm.ask(prompt)


================================================================================
FILE: agents\combiner_agent.py
================================================================================

class CombinerAgent:

    def combine(self, results):

        output = []

        for name, result in results.items():

            output.append(
                f"{name.upper()} RESULT:\n{result}"
            )

        combined_text = "\n\n".join(output)

        print("\n=== COMBINED ===")
        print(combined_text)

        return combined_text


================================================================================
FILE: agents\confidence_agent.py
================================================================================

class ConfidenceAgent:

    def score(self, answer):

        if (
            "could not find"
            in answer.lower()
        ):
            return "Low"

        if len(answer) > 500:
            return "High"

        return "Medium"


================================================================================
FILE: agents\critic_agent.py
================================================================================

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class CriticAgent:

    def __init__(self):

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def critique(self, answer):
        print("CRITIC RUNNING")

        prompt = f"""
You are a strict evidence-preserving critic agent.

Review the answer for clarity and internal consistency only.

Check:

1. Hallucinations
2. Unsupported claims
3. Completeness
4. Clarity

Rules:

- Use ONLY information already present in the answer.
- NEVER add outside facts, version numbers, dates, corrections, or new examples.
- If a claim is unsupported by the answer, remove it instead of replacing it with outside knowledge.
- Preserve the original topic.
- Preserve these headings if present:
  PDF Findings
  Search Findings
  Similarities
  Differences
  Final Conclusion
- If the answer is already clear, return it unchanged.
- If needed, improve wording only and keep the same useful structure.

Answer:

{answer}
"""

        return self.llm.ask(prompt)


================================================================================
FILE: agents\execution_monitor_agent.py
================================================================================

import time


class ExecutionMonitor:

    def start(self):

        self.start_time = (
            time.time()
        )

    def stop(self):

        return round(
            time.time()
            - self.start_time,
            2
        )


================================================================================
FILE: agents\formatter_agent.py
================================================================================

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class FormatterAgent:

    def __init__(self):

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def format(self, answer):

        prompt = f"""
You are an expert AI teacher.

Format the answer professionally.

Rules:

0. NEVER invent information.

0.1 Use ONLY information present in the answer.

0.2 If answer contains:

PDF Findings
Search Findings

Then preserve those sections exactly in meaning and keep the comparison structure.

0.3 Do not change the topic.

0.4 Do not replace comparison topics.

0.5 Do not add outside facts, version numbers, release names, dates, examples, or corrections.

0.6 If the input says Python functions from PDF and Python news from search, do not turn it into a Python version comparison.

0.7 If a section already exists, improve formatting only. Do not rewrite its factual meaning.

0.8 If answer contains:

SEARCH RESULT:
and
RAG RESULT:

ALWAYS create:

# Comparison Report

## PDF Findings

## Search Findings

## Similarities

## Differences

## Final Conclusion

Do not use normal format for comparison answers.

0.9 Never rename comparison topics.

0.10 If PDF discusses functions and Search discusses news, keep those exact topics.

1. Create title.

2. Add:

## Summary

## Key Points

## Detailed Explanation

## Example

## Conclusion

3. If comparison:

Use:

# Comparison Report

## PDF Findings

## Search Findings

## Similarities

## Differences

## Final Conclusion

For comparison answers, use ONLY the comparison format above.
Do not add ## Summary, ## Key Points, ## Detailed Explanation, ## Example, or ## Conclusion to comparison answers.

4. Use bullet points.

5. Keep URLs.

6. Make answer look like ChatGPT.

7. If there is not enough information for ## Example, write a very short example only from the answer. If no example is supported, omit ## Example.

Answer:

{answer}
"""

        return self.llm.ask(prompt)


================================================================================
FILE: agents\intent_agent.py
================================================================================

class IntentAgent:

    def detect(self, question):
        question = (
            question
            .lower()
            .strip()
        )

        if (
            "full form" in question
        ):
            return "short_answer"

        if (
            "exam" in question
            or "2 marks" in question
            or "5 marks" in question
            or "unit" in question
        ):
            return "exam"

        elif (
            "interview" in question
            or "job" in question
        ):
            return "interview"

        elif (
            "compare" in question
        ):
            return "comparison"

        elif (
            "research" in question
        ):
            return "research"

        return "general"


================================================================================
FILE: agents\math_agent.py
================================================================================

from tools.calculator_tool import CalculatorTool


class MathAgent:

    def __init__(self):
        self.calculator = CalculatorTool()

    def run(self, question):
        return self.calculator.calculate(question)

================================================================================
FILE: agents\memory_agent.py
================================================================================

from memory.memory import Memory


class MemoryAgent:

    def __init__(self):

        self.memory = Memory()

    def get_context(self):

        history = self.memory.get_history()

        context = ""

        for item in history[-10:]:

            context += (
                f"{item['role']}: "
                f"{item['message']}\n"
            )

        return context

================================================================================
FILE: agents\planner_agent.py
================================================================================

class PlannerAgent:

    def plan(self, question):

        normalized_question = question.lower().strip()

        has_comparison = any(
            word in normalized_question
            for word in [
                "compare",
                "comparison",
                "difference"
            ]
        )

        has_pdf_context = any(
            word in normalized_question
            for word in [
                "pdf",
                "document",
                "chapter",
                "notes"
            ]
        )

        has_search_context = any(
            word in normalized_question
            for word in [
                "latest",
                "news",
                "search",
                "find",
                "lookup"
            ]
        )

        if (
            has_comparison
            and has_pdf_context
            and has_search_context
        ):

            search_query = normalized_question
            rag_query = normalized_question

            if " with " in normalized_question:
                left, right = normalized_question.split(" with ", 1)
                rag_query = left
                search_query = right

            cleanup_words = [
                "compare",
                "comparison",
                "difference",
                "between",
                "from pdf",
                "in pdf",
                "pdf",
                "document",
                "search",
                "find",
                "lookup"
            ]

            for word in cleanup_words:
                search_query = search_query.replace(word, " ")
                rag_query = rag_query.replace(word, " ")

            search_query = " ".join(search_query.split())
            rag_query = " ".join(rag_query.split())

            if not search_query:
                search_query = normalized_question

            if not rag_query:
                rag_query = normalized_question

            return {
                "search": search_query,
                "rag": rag_query
            }

        return {}


================================================================================
FILE: agents\query_rewriter_agent.py
================================================================================

class QueryRewriterAgent:

    def rewrite(self, query):

        query = query.lower()

        if "python" in query:

            return (
                query
                + " programming language"
            )

        if "ai" in query:

            return (
                query
                + " artificial intelligence"
            )

        return query


================================================================================
FILE: agents\rag_agent.py
================================================================================

from rag.rag_chat import RAGChat


class RAGAgent:

    def __init__(self):
        self.rag = RAGChat()

    def run(self, question):
        print("RAG START")

        result = self.rag.ask(question)

        print("\n=== RAG RESULT ===")
        print(result[:300])

        return result


================================================================================
FILE: agents\reflection_agent.py
================================================================================

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class ReflectionAgent:

    def __init__(self):

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def review(self, search_result, rag_result, combined_result=None):

        evidence = combined_result or f"""
SEARCH RESULT:

{search_result}

PDF RESULT:

{rag_result}
"""

        prompt = f"""
You are a strict evidence-based comparison agent.

Use the evidence below to create a clean comparison answer.

Evidence:

{evidence}

Create exactly these sections:

# Comparison Report

## PDF Findings

## Search Findings

## Similarities

## Differences

## Final Conclusion

Rules:

- Do not return raw search dumps.
- Preserve useful URLs.
- Do not invent facts that are not in the evidence.
- Use PDF RESULT only for PDF Findings.
- Use SEARCH RESULT only for Search Findings.
- Do not change the topic.
- Do not compare Python versions unless the evidence is specifically about Python versions.
- If evidence is about Python functions and Python news, compare fundamentals from the PDF with updates/resources from search.
- Return structured markdown.
"""

        return self.llm.ask(prompt)


================================================================================
FILE: agents\registry.py
================================================================================

from agents.search_agent import SearchAgent
from agents.math_agent import MathAgent
from agents.rag_agent import RAGAgent
from agents.research_agent import ResearchAgent


class AgentRegistry:

    def __init__(self):

        self.agents = {

            "search": SearchAgent(),

            "math": MathAgent(),

            "rag": RAGAgent(),

            "research": ResearchAgent()
        }

    def get_agent(self, name):

        return self.agents.get(name)

================================================================================
FILE: agents\research_agent.py
================================================================================

from agents.search_agent import SearchAgent


class ResearchAgent:

    def __init__(self):

        self.search_agent = SearchAgent()

    def research(self, topic):

        result = self.search_agent.run(topic)

        return f"""
RESEARCH REPORT

Topic:
{topic}

Findings:
{result}
"""

================================================================================
FILE: agents\router.py
================================================================================

import re

from config import NVIDIA_API_KEY
from llm.nvidia_client import NvidiaClient


class Router:

    def __init__(self):
        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def route(self, question):

        question = question.lower().strip()

        if re.fullmatch(
            r"[0-9+\-*/(). ]+",
            question
        ):
            return "calculator"

        if re.search(
            r"\d+\s*[+\-*/]\s*\d+",
            question
        ):
            print("\nROUTER CHOICE:")
            print("math")
            return "calculator"

        comparison_keywords = [
            "compare",
            "comparison",
            "difference"
        ]

        search_keywords = [
            "search",
            "latest",
            "news",
            "find",
            "lookup"
        ]

        pdf_keywords = [
            "pdf",
            "document",
            "chapter",
            "notes",
            "summary",
            "summarize"
        ]

        knowledge_starters = [
            "what is",
            "what are",
            "who is",
            "why is",
            "explain",
            "teach me",
            "how does",
            "how to",
            "define",
            "meaning of"
        ]

        has_comparison = any(
            word in question
            for word in comparison_keywords
        )

        has_search_need = any(
            word in question
            for word in search_keywords
        )

        has_pdf_need = any(
            word in question
            for word in pdf_keywords
        )

        has_knowledge_start = any(
            question.startswith(starter)
            for starter in knowledge_starters
        )

        if (
            has_comparison
            and has_search_need
            and has_pdf_need
        ):
            print("\nROUTER CHOICE:")
            print("multi")
            return "multi"

        if has_search_need:
            print("\nROUTER CHOICE:")
            print("search")
            return "search"

        if has_pdf_need:
            print("\nROUTER CHOICE:")
            print("rag")
            return "rag"

        if has_knowledge_start:
            print("\nROUTER CHOICE:")
            print("knowledge")
            return "knowledge"

        if any(
            word in question
            for word in [
                "python",
                "ai",
                "machine learning",
                "programming",
                "function",
                "variable",
                "class",
                "algorithm"
            ]
        ):
            print("\nROUTER CHOICE:")
            print("knowledge")
            return "knowledge"

        try:
            prompt = f"""
You are an AI Router.

Available Agents:

search
rag
math
knowledge

Question:
{question}

Rules:

- Return ONLY agent names.
- No explanation.
- No code.
- No sentences.
- No markdown.

Examples:

Question: latest ai news
Answer: search

Question: what is 10+10
Answer: math

Question: what is python
Answer: knowledge

Question: summarize chapter 1
Answer: rag

Question: compare python functions from pdf with latest python news
Answer: search,rag

Return only the answer.
"""
            route = self.llm.ask(prompt)
            route = route.strip().lower()

            print("\nROUTER CHOICE:")
            print(route)

            valid_routes = [
                "search",
                "rag",
                "math",
                "knowledge",
                "search,rag"
            ]

            if route not in valid_routes:
                route = "knowledge"

            if route == "search,rag":
                return "multi"

            if route == "math":
                return "calculator"

            if route == "search":
                return "search"

            if route == "rag":
                return "rag"

            if route == "knowledge":
                return "knowledge"

        except Exception as error:
            print("\nROUTER LLM FALLBACK:")
            print(error)

        if any(
            word in question
            for word in comparison_keywords
        ):
            return "multi"

        if any(
            word in question
            for word in search_keywords
        ):
            return "search"

        if any(
            word in question
            for word in pdf_keywords
        ):
            return "rag"

        return "knowledge"


================================================================================
FILE: agents\search_agent.py
================================================================================

from tools.search_tool import SearchTool
from agents.query_rewriter_agent import QueryRewriterAgent


class SearchAgent:

    def __init__(self):
        self.search_tool = SearchTool()
        self.query_rewriter = QueryRewriterAgent()

    def run(self, question):
        print("SEARCH START")

        question = (
            self.query_rewriter
            .rewrite(
                question
            )
        )

        print("SEARCH QUERY:", question)

        result = self.search_tool.search(question)

        print("\n=== SEARCH RESULT ===")
        print(result[:300])

        return result


================================================================================
FILE: agents\search_summarizer_agent.py
================================================================================

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class SearchSummarizerAgent:

    def __init__(self):
        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def summarize(self, search_result):

        prompt = f"""
You are a careful search-result summarization expert.

Convert the raw search results into:

# Topic

## Top Updates

- Important update 1
- Important update 2
- Important update 3

## Key Insights

- Insight 1
- Insight 2

## Sources

Keep all useful URLs.

Strict rules:

- Use ONLY the titles, snippets, and URLs present in Search Results.
- NEVER invent release numbers, dates, facts, features, or company announcements.
- If a result only looks like a general news/resource page, describe it as a source/resource, not as a specific update.
- If the search results do not contain enough detail for a concrete update, say that the source may contain updates instead of guessing.
- Preserve all useful URLs exactly.
- Do not use outside knowledge.
- Do not describe websites.
- Bad: "Python.org provides blogs"
- Bad: "Real Python provides tutorials"
- Good: "The search results point to community updates, tutorials, and Python ecosystem news."
- Good: "The available sources suggest ongoing Python development and learning resources."

Search Results:

{search_result}
"""

        return self.llm.ask(prompt)


================================================================================
FILE: agents\student_agent.py
================================================================================

from agents.teacher_agent import TeacherAgent

class StudentAgent:

    def __init__(self):
        self.teacher = TeacherAgent()

    def ask_teacher(self, question):
        return self.teacher.answer(question)

================================================================================
FILE: agents\supervisor_agent.py
================================================================================

import json
from concurrent.futures import ThreadPoolExecutor
from datetime import datetime
from pathlib import Path

from agents.math_agent import MathAgent
from agents.search_agent import SearchAgent
from agents.rag_agent import RAGAgent

from agents.planner_agent import PlannerAgent
from agents.combiner_agent import CombinerAgent
from agents.reflection_agent import ReflectionAgent
from agents.critic_agent import CriticAgent
from agents.formatter_agent import FormatterAgent
from agents.answer_generator_agent import AnswerGeneratorAgent
from agents.search_summarizer_agent import SearchSummarizerAgent
from agents.confidence_agent import ConfidenceAgent
from agents.execution_monitor_agent import ExecutionMonitor

class SupervisorAgent:

    def __init__(self):

        self.math_agent = MathAgent()
        self.search_agent = SearchAgent()
        self.rag_agent = RAGAgent()

        self.planner = PlannerAgent()
        self.combiner = CombinerAgent()
        self.reflection = ReflectionAgent()
        self.critic = CriticAgent()
        self.formatter = FormatterAgent()
        self.answer_generator = AnswerGeneratorAgent()
        self.search_summarizer = SearchSummarizerAgent()
        self.confidence = ConfidenceAgent()
        self.monitor = ExecutionMonitor()
        self.last_run = {}

    def log_run(self, payload):
        logs_dir = Path("logs")
        logs_dir.mkdir(exist_ok=True)

        with (logs_dir / "agent_runs.jsonl").open("a", encoding="utf-8") as file:
            file.write(
                json.dumps(
                    payload,
                    ensure_ascii=False
                )
            )
            file.write("\n")

    def handle(self, route, question):
        self.monitor.start()

        planned = self.planner.plan(
            question
        )

        if route == "multi" and not planned:
            planned = {
                "search": question,
                "rag": question
            }

        if planned:

            search_query = planned["search"]
            rag_query = planned["rag"]

            with ThreadPoolExecutor() as executor:

                search_future = executor.submit(
                    self.search_agent.run,
                    search_query
                )

                rag_future = executor.submit(
                    self.rag_agent.run,
                    rag_query
                )

                search_result = search_future.result()
                rag_result = rag_future.result()

            search_result = (
                self.search_summarizer
                .summarize(
                    search_result
                )
            )

            results = {
                "search": search_result,
                "rag": rag_result
            }

            combined = (
                self.combiner.combine(
                    results
                )
            )

            reflected = self.reflection.review(
                search_result,
                rag_result,
                combined
            )

            final_answer = self.critic.critique(
                reflected
            )

            formatted_answer = self.formatter.format(
                final_answer
            )

            generated_answer = self.answer_generator.generate(
                formatted_answer
            )

            confidence_score = self.confidence.score(
                generated_answer
            )
            execution_time = self.monitor.stop()
            print("Confidence Score:", confidence_score)
            print(f"Execution Time: {execution_time} sec")

            self.last_run = {
                "query": question,
                "route": "multi",
                "agents": ["SearchAgent", "QueryRewriterAgent", "SearchSummarizerAgent", "RAGAgent", "ReflectionAgent", "CriticAgent", "FormatterAgent", "AnswerGeneratorAgent", "ConfidenceAgent", "ExecutionMonitor"],
                "execution_time": execution_time,
                "confidence": confidence_score,
                "retrieved_chunks": "PDF result generated"
            }
            self.log_run(
                {
                    **self.last_run,
                    "timestamp": datetime.now().isoformat(timespec="seconds")
                }
            )

            return generated_answer

        if route == "calculator":

            result = self.math_agent.run(
                question
            )
            agents = ["MathAgent"]

        elif route == "search":

            result = self.search_agent.run(
                question
            )
            result = (
                self.search_summarizer
                .summarize(
                    result
                )
            )
            agents = ["SearchAgent", "QueryRewriterAgent", "SearchSummarizerAgent"]

        elif route == "rag":

            result = self.rag_agent.run(
                question
            )
            agents = ["RAGAgent"]

        else:
            result = None
            agents = []

        if result is not None:
            formatted_result = self.formatter.format(
                result
            )

            result = self.answer_generator.generate(
                formatted_result
            )
            agents = agents + ["FormatterAgent", "AnswerGeneratorAgent"]

            confidence_score = self.confidence.score(
                result
            )
            agents = agents + ["ConfidenceAgent"]
        else:
            confidence_score = "Low"

        execution_time = self.monitor.stop()
        print("Confidence Score:", confidence_score)
        print(f"Execution Time: {execution_time} sec")
        agents = agents + ["ExecutionMonitor"]

        self.last_run = {
            "query": question,
            "route": route,
            "agents": agents,
            "execution_time": execution_time,
            "confidence": confidence_score,
            "retrieved_chunks": "N/A"
        }
        self.log_run(
            {
                **self.last_run,
                "timestamp": datetime.now().isoformat(timespec="seconds")
            }
        )

        return result


================================================================================
FILE: agents\teacher_agent.py
================================================================================

import json
import time
from datetime import datetime
from pathlib import Path

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
from rag.rag_chat import RAGChat
from agents.supervisor_agent import SupervisorAgent
from agents.formatter_agent import FormatterAgent
from agents.answer_generator_agent import AnswerGeneratorAgent
from agents.intent_agent import IntentAgent

class TeacherAgent:

    def __init__(self):
        self.memory = Memory()
        self.calculator = CalculatorTool()
        self.search_tool = SearchTool()
        self.router = Router()
        self.llm = NvidiaClient(NVIDIA_API_KEY)
        self.rag = RAGChat()
        self.supervisor = SupervisorAgent()
        self.formatter = FormatterAgent()
        self.answer_generator = AnswerGeneratorAgent()
        self.intent = IntentAgent()
        self.last_run = {}

    def log_run(self, payload):
        logs_dir = Path("logs")
        logs_dir.mkdir(exist_ok=True)

        with (logs_dir / "agent_runs.jsonl").open("a", encoding="utf-8") as file:
            file.write(
                json.dumps(
                    payload,
                    ensure_ascii=False
                )
            )
            file.write("\n")

    def answer(self, question):
        started_at = time.perf_counter()
        self.memory.save("student", question)

        intent = (
            self.intent.detect(
                question
            )
        )

        print(
            "INTENT:",
            intent
        )

        if intent == "short_answer":

            prompt = f"""
Answer shortly.

Question:
{question}

Format:

# Topic

Full Form:
...

Meaning:
...

Use:
...
"""

            result = self.llm.ask(prompt)

            self.memory.save(
                "teacher",
                result
            )

            self.last_run = {
                "query": question,
                "route": "knowledge",
                "agents": ["TeacherAgent", "IntentAgent"],
                "execution_time": round(time.perf_counter() - started_at, 2),
                "retrieved_chunks": "N/A"
            }
            self.log_run(
                {
                    **self.last_run,
                    "timestamp": datetime.now().isoformat(timespec="seconds")
                }
            )

            return result

        route = self.router.route(question)

        if route in ["calculator", "search", "rag", "multi"]:
            result = self.supervisor.handle(
                route,
                question
            )

            self.memory.save(
                "teacher",
                str(result)
            )

            self.last_run = self.supervisor.last_run

            return str(result)

        history = self.memory.get_history()
        context = ""
        for item in history[-10:]:
            context += f"{item['role']}: {item['message']}\n"

        full_prompt = f"""
Previous Conversation:

{context}
Current Question:
{question}

Detected Intent:
{intent}

Answer naturally and use the conversation context if relevant.
If intent is exam, explain in an exam-focused way with definitions, key points, and short-answer style.
If intent is interview, explain with interview questions, practical points, and concise sample answers.
If intent is research, include a deeper structured explanation.
"""

        response = self.llm.ask(full_prompt)
        formatted_response = self.formatter.format(
            response
        )
        final_response = self.answer_generator.generate(
            formatted_response
        )
        self.memory.save("teacher", final_response)
        self.last_run = {
            "query": question,
            "route": route,
            "agents": ["TeacherAgent", "IntentAgent", "FormatterAgent", "AnswerGeneratorAgent"],
            "execution_time": round(time.perf_counter() - started_at, 2),
            "retrieved_chunks": "N/A"
        }
        self.log_run(
            {
                **self.last_run,
                "timestamp": datetime.now().isoformat(timespec="seconds")
            }
        )
        return final_response


================================================================================
FILE: app.py
================================================================================

import json
import os
import re
import uuid
from datetime import datetime
from pathlib import Path

import streamlit as st

from agents.teacher_agent import TeacherAgent


APP_ICON = "\U0001F393"
CHAT_HISTORY_FILE = Path("chat_history.json")
UPLOADS_DIR = Path("uploads")


def current_timestamp():
    return datetime.now().isoformat(timespec="seconds")


def create_chat(title="New Chat", messages=None):
    now = current_timestamp()

    return {
        "id": str(uuid.uuid4()),
        "title": title,
        "messages": messages or [],
        "created_at": now,
        "updated_at": now,
    }


def save_chats_to_disk(data=None):
    payload = data or {
        "active_chat_id": st.session_state.active_chat_id,
        "chats": st.session_state.chats,
    }

    with CHAT_HISTORY_FILE.open("w", encoding="utf-8") as file:
        json.dump(payload, file, indent=4, ensure_ascii=False)


def load_chats_from_disk():
    if not CHAT_HISTORY_FILE.exists():
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    try:
        with CHAT_HISTORY_FILE.open("r", encoding="utf-8") as file:
            data = json.load(file)
    except (json.JSONDecodeError, OSError):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)
        return data

    if isinstance(data, list):
        imported_chat = create_chat("Imported Chat", data)
        data = {"active_chat_id": imported_chat["id"], "chats": [imported_chat]}
        save_chats_to_disk(data)
        return data

    if not data.get("chats"):
        default_chat = create_chat()
        data = {"active_chat_id": default_chat["id"], "chats": [default_chat]}
        save_chats_to_disk(data)

    return data


def get_active_chat():
    active_chat_id = st.session_state.get("active_chat_id")

    for chat in st.session_state.get("chats", []):
        if chat["id"] == active_chat_id:
            return chat

    return None


def sync_active_chat():
    active_chat = get_active_chat()

    if not active_chat:
        return

    active_chat["messages"] = st.session_state.messages
    active_chat["updated_at"] = current_timestamp()
    save_chats_to_disk()


def initialize_session_state():
    if "teacher" not in st.session_state:
        st.session_state.teacher = TeacherAgent()

    if "chats" not in st.session_state or "active_chat_id" not in st.session_state:
        saved_data = load_chats_from_disk()
        st.session_state.chats = saved_data["chats"]
        st.session_state.active_chat_id = saved_data.get("active_chat_id")

    if not get_active_chat():
        st.session_state.active_chat_id = st.session_state.chats[0]["id"]

    if "messages" not in st.session_state:
        st.session_state.messages = get_active_chat()["messages"]

    if "uploaded_file_signature" not in st.session_state:
        st.session_state.uploaded_file_signature = None

    if "active_uploaded_pdf" not in st.session_state:
        st.session_state.active_uploaded_pdf = None

    if "last_agent_run" not in st.session_state:
        st.session_state.last_agent_run = {
            "route": "N/A",
            "agents": [],
            "execution_time": 0,
            "retrieved_chunks": "N/A"
        }


def switch_chat(chat_id):
    st.session_state.active_chat_id = chat_id
    active_chat = get_active_chat()
    st.session_state.messages = active_chat["messages"] if active_chat else []
    save_chats_to_disk()


def start_new_chat():
    chat = create_chat()
    st.session_state.chats.insert(0, chat)
    st.session_state.active_chat_id = chat["id"]
    st.session_state.messages = []

    if st.session_state.active_uploaded_pdf:
        load_teacher_from_uploads()
    else:
        st.session_state.teacher = TeacherAgent()

    save_chats_to_disk()


def auto_title_chat(message):
    active_chat = get_active_chat()

    if not active_chat or active_chat["title"] != "New Chat":
        return

    active_chat["title"] = message.strip()[:42] or "New Chat"


def add_message(role, content):
    st.session_state.messages.append({"role": role, "content": content})

    if role == "user":
        auto_title_chat(content)

    sync_active_chat()


def safe_filename(filename):
    clean_name = Path(filename).name
    clean_name = re.sub(r"[^A-Za-z0-9._-]", "_", clean_name)
    return clean_name or f"uploaded_{uuid.uuid4().hex}"


def save_uploaded_file(uploaded_file):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    file_name = safe_filename(uploaded_file.name)
    file_path = UPLOADS_DIR / file_name
    file_bytes = uploaded_file.getvalue()
    file_path.write_bytes(file_bytes)

    return file_path, file_bytes


def rebuild_rag_for_pdf(file_bytes, original_name):
    UPLOADS_DIR.mkdir(parents=True, exist_ok=True)

    (UPLOADS_DIR / "sample.pdf").write_bytes(file_bytes)

    for cache_file in ("chunks.pkl", "faiss_index.bin"):
        cache_path = UPLOADS_DIR / cache_file

        if cache_path.exists():
            cache_path.unlink()

    load_teacher_from_uploads()
    st.session_state.active_uploaded_pdf = original_name


def load_teacher_from_uploads():
    original_cwd = os.getcwd()

    try:
        os.chdir(UPLOADS_DIR)
        st.session_state.teacher = TeacherAgent()
    finally:
        os.chdir(original_cwd)


def handle_uploaded_file(uploaded_file):
    file_path, file_bytes = save_uploaded_file(uploaded_file)
    file_extension = file_path.suffix.lower()

    add_message("user", f"Uploaded file: {uploaded_file.name}")

    if file_extension == ".pdf":
        with st.spinner("Rebuilding RAG index for uploaded PDF..."):
            try:
                rebuild_rag_for_pdf(file_bytes, uploaded_file.name)
                response = f"PDF uploaded and RAG index rebuilt: {uploaded_file.name}"
            except Exception as error:
                response = f"PDF upload failed: {error}"
    else:
        response = f"File uploaded and saved: {uploaded_file.name}"

    add_message("assistant", response)


def run_teacher(prompt):
    add_message("user", prompt)

    with st.spinner("Thinking..."):
        try:
            response = st.session_state.teacher.answer(prompt)
            st.session_state.last_agent_run = st.session_state.teacher.last_run
        except Exception as error:
            response = f"Sorry, something went wrong: {error}"

    add_message("assistant", response)


st.set_page_config(
    page_title="Student Teacher Agent",
    page_icon=APP_ICON,
    layout="wide",
    initial_sidebar_state="expanded",
)


st.markdown(
    """
    <style>
        :root {
            --bg: #212121;
            --sidebar: #171717;
            --sidebar-hover: #2f2f2f;
            --text: #ececec;
            --muted: #b4b4b4;
            --border: #303030;
            --input: #2f2f2f;
        }

        .stApp {
            background: var(--bg);
            color: var(--text);
        }

        .main .block-container {
            max-width: 820px;
            padding-top: 1rem;
            padding-bottom: 8.5rem;
        }

        [data-testid="stSidebar"] {
            background: var(--sidebar);
            border-right: 1px solid var(--border);
        }

        [data-testid="stSidebar"] * {
            color: var(--text);
        }

        .sidebar-title {
            font-size: 1rem;
            font-weight: 700;
            padding: 0.4rem 0 0.8rem;
        }

        .chat-list-label {
            color: var(--muted);
            font-size: 0.78rem;
            font-weight: 600;
            margin: 1rem 0 0.4rem;
            text-transform: uppercase;
        }

        .empty-state {
            align-items: center;
            color: var(--muted);
            display: flex;
            font-size: 1.05rem;
            justify-content: center;
            min-height: 52vh;
            text-align: center;
        }

        /* Chat message container: make messages compact and scrollable */
        [data-testid="stVerticalBlock"]:has([data-testid="stChatMessage"]) {
            gap: 0.25rem;
            max-height: calc(100vh - 170px);
            overflow-y: auto;
            padding-right: 8px;
        }

        [data-testid="stChatMessage"] {
            background: transparent;
            border: 0;
            border-radius: 0;
            display: flex;
            margin-bottom: 0.95rem;
            padding: 0.15rem 0;
            width: 100%;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) {
            flex-direction: row-reverse;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) {
            flex-direction: row;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="stMarkdownContainer"] {
            border-radius: 18px;
            max-width: min(76%, 660px);
            padding: 0.85rem 1rem;
            font-size: 0.96rem;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-user"]) [data-testid="stMarkdownContainer"] {
            background: #2f2f2f;
            margin-left: auto;
        }

        [data-testid="stChatMessage"]:has([data-testid="chatAvatarIcon-assistant"]) [data-testid="stMarkdownContainer"] {
            background: #262626;
            border: 1px solid #343434;
            margin-right: auto;
        }

        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-user"],
        [data-testid="stChatMessage"] [data-testid="chatAvatarIcon-assistant"] {
            margin-top: 0.2rem;
        }

        [data-testid="stChatMessage"] p {
            line-height: 1.7;
            margin-bottom: 0;
        }

        /* Fix chat input to bottom, centered within main content */
        .chatbar-wrapper {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 16px;
            width: min(820px, calc(100% - 48px));
            z-index: 9999;
            padding: 0 0.5rem;
            pointer-events: auto;
        }

        .chatbar-row {
            position: relative;
            display: flex;
            align-items: center;
            gap: 0;
            background: var(--input);
            border: 1px solid #3f3f3f;
            border-radius: 24px;
            padding: 0.25rem;
        }

        .chatbar-row .stButton {
            margin: 0;
            padding: 0;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .chatbar-row .stButton > button {
            min-width: 3rem;
            min-height: 3rem;
            border-radius: 999px 0 0 999px;
            border: 1px solid #3f3f3f;
            border-right: none;
            background: #252525;
            color: var(--text);
            font-size: 20px;
            padding: 0;
        }

        .chatbar-row .stButton > button:hover {
            background: #323232;
        }

        .chatbar-row [data-testid="stChatInput"] {
            flex: 1;
            margin: 0;
        }

        .chatbar-row [data-testid="stChatInput"] textarea {
            background: transparent;
            border: none;
            min-height: 3rem;
            padding: 0.75rem 1rem;
            color: var(--text);
            border-radius: 0 18px 18px 0;
        }

        .chatbar-row [data-testid="stChatInput"] textarea:focus {
            outline: none;
            box-shadow: none;
        }

        .attachment-popup {
            position: absolute;
            bottom: calc(100% + 0.5rem);
            left: 0;
            width: 100%;
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.25);
        }

        .upload-preview {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 108px;
            background: #202020;
            border: 1px solid #333333;
            border-radius: 14px;
            color: var(--text);
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            z-index: 10009;
        }

        .upload-preview span {
            font-size: 0.95rem;
        }

        .upload-ready {
            color: #8df57f;
            font-weight: 600;
        }

        .attachment-success {
            position: fixed;
            left: 50%;
            transform: translateX(-50%);
            bottom: 78px;
            color: #8df57f;
            font-size: 0.92rem;
            margin: 0;
            z-index: 10009;
        }

        .attachment-popup {
            background: #1f1f1f;
            border: 1px solid #363636;
            border-radius: 16px;
            padding: 0.8rem 0.95rem;
            margin-bottom: 0.55rem;
        }

        .attachment-popup div {
            display: flex;
            flex-wrap: wrap;
            gap: 0.55rem;
        }

        .attachment-popup button {
            min-width: 120px;
            border-radius: 14px;
            border: 1px solid #3f3f3f;
            background: #232323;
            color: var(--text);
            padding: 0.55rem 0.75rem;
            cursor: pointer;
        }

        .attachment-popup button:hover {
            background: #2d2d2d;
        }

        [data-testid="stFileUploader"] {
            background: transparent;
            border: 0;
            padding: 0;
        }

        [data-testid="stFileUploader"] section {
            background: #2f2f2f;
            border: 1px solid #3f3f3f;
            border-radius: 12px;
            padding: 0.55rem 0.75rem;
        }

        [data-testid="stFileUploader"] small {
            color: var(--muted);
        }

        .upload-hint {
            color: var(--muted);
            font-size: 0.82rem;
            margin: 0.75rem 0 0.4rem;
        }

        div.stButton > button {
            width: 100%;
            background: transparent;
            border: 1px solid transparent;
            border-radius: 8px;
            color: var(--text);
            font-weight: 500;
            min-height: 2.4rem;
            text-align: left;
        }

        div.stButton > button:hover {
            background: var(--sidebar-hover);
            border-color: var(--sidebar-hover);
            color: #ffffff;
        }

        div.stButton > button:focus {
            box-shadow: none;
        }

        /* Sidebar specific tweaks */
        [data-testid="stSidebar"] .chat-list .stButton > button {
            border-radius: 8px;
            padding-left: 0.6rem;
            padding-right: 0.6rem;
            text-align: left;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:hover {
            background: #272727;
            transform: translateY(-1px);
            cursor: pointer;
        }

        [data-testid="stSidebar"] .chat-list .stButton > button:active {
            transform: translateY(0);
        }

        .chat-list-label {
            margin-top: 0.6rem;
        }

    </style>
    """,
    unsafe_allow_html=True,
)


initialize_session_state()


with st.sidebar:
    # Top title
    st.markdown(
        f"<div class='sidebar-title'>{APP_ICON} Student Teacher Agent</div>",
        unsafe_allow_html=True,
    )

    # Single New Chat button
    if st.button("+  New chat", key="new_chat_sidebar", use_container_width=True):
        start_new_chat()
        st.rerun()

    last_run = st.session_state.last_agent_run
    agents_used = last_run.get("agents", [])
    st.markdown("<div class='chat-list-label'>Agent Dashboard</div>", unsafe_allow_html=True)
    st.write(f"ROUTE: {last_run.get('route', 'N/A')}")
    st.write(
        "AGENTS: "
        + (
            ", ".join(agents_used)
            if agents_used
            else "N/A"
        )
    )
    st.write(f"TIME: {last_run.get('execution_time', 0)} sec")
    st.write(f"RETRIEVED CHUNKS: {last_run.get('retrieved_chunks', 'N/A')}")

    st.markdown("<div class='chat-list-label'>Chats</div>", unsafe_allow_html=True)

    st.markdown(
        "<div class='chat-list' style='max-height:60vh; overflow:auto; padding-right:6px;'>",
        unsafe_allow_html=True,
    )

    sorted_chats = sorted(
        st.session_state.chats,
        key=lambda chat: chat.get("updated_at", ""),
        reverse=True,
    )

    def truncate(text, n=30):
        return text if len(text) <= n else text[: n - 3].rstrip() + "..."

    for chat in sorted_chats:
        chat_id = chat["id"]
        title = chat.get("title", "New Chat")
        is_active = chat_id == st.session_state.active_chat_id

        cols = st.columns([0.08, 0.72, 0.2])

        with cols[0]:
            st.markdown("💬")

        display_title = truncate(title, 28)
        button_label = f"{display_title}"
        if is_active:
            button_label = f"● {display_title}"

        with cols[1]:
            if st.button(button_label, key=f"chat_select_{chat_id}", help=title):
                switch_chat(chat_id)
                st.rerun()

        with cols[2]:
            if st.button("⋮", key=f"menu_{chat_id}"):
                if st.session_state.get("_menu_for") == chat_id:
                    st.session_state._menu_for = None
                else:
                    st.session_state._menu_for = chat_id

        if st.session_state.get("_menu_for") == chat_id:
            action_cols = st.columns([0.5, 0.5])
            with action_cols[0]:
                if st.button("Rename", key=f"rename_action_{chat_id}"):
                    st.session_state._rename_for = chat_id
                    st.session_state._delete_for = None

            with action_cols[1]:
                if st.button("Delete", key=f"delete_action_{chat_id}"):
                    st.session_state._delete_for = chat_id
                    st.session_state._rename_for = None

            if st.session_state.get("_rename_for") == chat_id:
                new_name = st.text_input(
                    "New name",
                    key=f"rename_input_{chat_id}",
                    value=title,
                    label_visibility="collapsed",
                )
                if st.button("Save", key=f"save_rename_{chat_id}"):
                    if new_name:
                        for c in st.session_state.chats:
                            if c["id"] == chat_id:
                                c["title"] = new_name
                                break
                        save_chats_to_disk()
                        st.session_state._menu_for = None
                        st.session_state._rename_for = None
                        st.rerun()
                if st.button("Cancel", key=f"cancel_rename_{chat_id}"):
                    st.session_state._rename_for = None
                    st.session_state._menu_for = None
                    st.rerun()

            if st.session_state.get("_delete_for") == chat_id:
                confirm_delete = st.checkbox("Confirm delete", key=f"delete_confirm_{chat_id}")
                if confirm_delete and st.button("Delete permanently", key=f"confirm_delete_{chat_id}"):
                    st.session_state.chats = [c for c in st.session_state.chats if c["id"] != chat_id]
                    if st.session_state.get("active_chat_id") == chat_id:
                        st.session_state.active_chat_id = st.session_state.chats[0]["id"] if st.session_state.chats else None
                        st.session_state.messages = get_active_chat()["messages"] if st.session_state.active_chat_id else []
                    save_chats_to_disk()
                    st.session_state._menu_for = None
                    st.session_state._delete_for = None
                    st.rerun()

    st.markdown("</div>", unsafe_allow_html=True)


chat_history = st.container(height=620, border=False)

with chat_history:
    if not st.session_state.messages:
        st.markdown(
            "<div class='empty-state'>How can I help you learn today?</div>",
            unsafe_allow_html=True,
        )

    for message in st.session_state.messages:
        with st.chat_message(message["role"]):
            st.write(message["content"])


if "attachment_menu_open" not in st.session_state:
    st.session_state.attachment_menu_open = False

if "attachment_type" not in st.session_state:
    st.session_state.attachment_type = None

if "uploaded_preview" not in st.session_state:
    st.session_state.uploaded_preview = None

if "upload_status" not in st.session_state:
    st.session_state.upload_status = None

if st.session_state.uploaded_preview:
    st.markdown(
        f"<div class='upload-preview'>📄 {st.session_state.uploaded_preview} <span class='upload-ready'>{st.session_state.upload_status or 'Ready'}</span></div>"
        "<div class='attachment-success'>File uploaded successfully.</div>",
        unsafe_allow_html=True,
    )

with st.container():
    st.markdown("<div class='chatbar-wrapper'>", unsafe_allow_html=True)

    if st.session_state.attachment_menu_open:
        st.markdown("<div class='attachment-popup'>", unsafe_allow_html=True)
        st.write("**Attach a file**")
        option_cols = st.columns([1, 1, 1, 1])

        with option_cols[0]:
            if st.button("PDF", key="upload_pdf"):
                st.session_state.attachment_type = "pdf"
        with option_cols[1]:
            if st.button("Image", key="upload_image"):
                st.session_state.attachment_type = "image"
        with option_cols[2]:
            if st.button("DOCX", key="upload_docx"):
                st.session_state.attachment_type = "docx"
        with option_cols[3]:
            if st.button("TXT", key="upload_txt"):
                st.session_state.attachment_type = "txt"

        st.markdown("</div>", unsafe_allow_html=True)

    if st.session_state.attachment_type:
        if st.session_state.attachment_type == "pdf":
            allowed_types = ["pdf"]
            label = "Choose a PDF file"
        elif st.session_state.attachment_type == "image":
            allowed_types = ["png", "jpg", "jpeg"]
            label = "Choose an image file"
        elif st.session_state.attachment_type == "docx":
            allowed_types = ["docx"]
            label = "Choose a DOCX file"
        else:
            allowed_types = ["txt"]
            label = "Choose a TXT file"

        attachment = st.file_uploader(
            label,
            type=allowed_types,
            key="attachment_uploader",
            label_visibility="visible",
        )

        if attachment is not None:
            file_signature = f"{attachment.name}:{attachment.size}"
            if file_signature != st.session_state.uploaded_file_signature:
                st.session_state.uploaded_file_signature = file_signature
                handle_uploaded_file(attachment)
                st.session_state.uploaded_preview = attachment.name
                st.session_state.upload_status = "Ready"
                st.session_state.attachment_menu_open = False
                st.session_state.attachment_type = None
                st.rerun()

    st.markdown("<div class='chatbar-row'>", unsafe_allow_html=True)
    cols = st.columns([0.08, 0.92], gap="small")
    with cols[0]:
        if st.button("+", key="attach_btn", help="Upload a file"):
            st.session_state.attachment_menu_open = not st.session_state.attachment_menu_open
            st.session_state.attachment_type = None
    with cols[1]:
        prompt = st.chat_input("Message Student Teacher Agent")
    st.markdown("</div>", unsafe_allow_html=True)

    st.markdown("</div>", unsafe_allow_html=True)

if prompt:
    run_teacher(prompt)
    st.rerun()


================================================================================
FILE: config.py
================================================================================

from dotenv import load_dotenv
import os

load_dotenv()

NVIDIA_API_KEY = os.getenv("NVIDIA_API_KEY")

================================================================================
FILE: evaluation\evaluate.py
================================================================================

import time
import sys
from pathlib import Path

ROOT_DIR = Path(__file__).resolve().parents[1]

if str(ROOT_DIR) not in sys.path:
    sys.path.insert(0, str(ROOT_DIR))


QUESTIONS = [
    "2 + 2",
    "10 * 5",
    "100 / 4",
    "search latest python news",
    "latest ai news",
    "find machine learning news",
    "lookup python release news",
    "search latest data science news",
    "summarize pdf",
    "explain chapter from pdf",
    "what is in the document",
    "summary of notes",
    "compare python functions from pdf with latest python news",
    "compare chapter 1 with latest ai news",
    "difference between pdf notes and latest news",
    "what is a function in python",
    "explain loops",
    "what is a variable",
    "teach me lists",
    "explain dictionaries",
    "how does recursion work",
    "what is oop",
    "explain classes",
    "what is inheritance",
    "what is polymorphism",
    "latest technology news",
    "search latest programming news",
    "find latest software news",
    "lookup latest education news",
    "search python tutorials",
    "compare pdf summary with latest technology news",
    "compare notes with latest programming news",
    "difference between document and latest ai news",
    "summarize chapter 2",
    "pdf key points",
    "document main ideas",
    "notes summary",
    "3 * (4 + 5)",
    "50 - 17",
    "8 / 2 + 6",
    "what is python",
    "explain modules",
    "what is pip",
    "what is an api",
    "teach me error handling",
    "explain file handling",
    "latest open source news",
    "search latest developer news",
    "compare python functions from pdf with latest developer news",
    "summarize pdf and compare with latest news",
]


def main():
    from agents.teacher_agent import TeacherAgent

    teacher = TeacherAgent()
    total_time = 0
    successes = 0

    for question in QUESTIONS:
        started_at = time.perf_counter()

        try:
            answer = teacher.answer(question)
            success = bool(str(answer).strip())
        except Exception as error:
            answer = f"ERROR: {error}"
            success = False

        elapsed = time.perf_counter() - started_at
        total_time += elapsed
        successes += int(success)

        print(f"QUESTION: {question}")
        print(f"SUCCESS: {success}")
        print(f"TIME: {elapsed:.2f} sec")
        print(f"ANSWER PREVIEW: {str(answer)[:200]}")
        print("-" * 60)

    total = len(QUESTIONS)
    print("EVALUATION SUMMARY")
    print(f"QUESTIONS: {total}")
    print(f"SUCCESS RATE: {(successes / total) * 100:.2f}%")
    print(f"AVERAGE RESPONSE TIME: {total_time / total:.2f} sec")


if __name__ == "__main__":
    main()


================================================================================
FILE: export_project.py
================================================================================

#!/usr/bin/env python3
# ============================================================================
# PROJECT EXTRACTION UTILITY
# ============================================================================
# Purpose: Generate a comprehensive report of the entire Python codebase
# Output: Single text file containing:
#         1. Visual tree structure of project directories/files
#         2. Complete content of all Python files
#         3. Summary statistics
# Usage: python Project_extract.py
# Use Case: Code review, documentation, sharing codebase overview
# ============================================================================

"""
Extract all Python files with their content and project structure into a single text file.
"""

import os
import sys
from pathlib import Path


def get_project_structure(root_dir, prefix="", max_depth=10, current_depth=0, ignore_dirs=None):
    """
    Generate a text representation of the project structure.
    
    Creates a tree-style visualization of directories and files.
    Recursively traverses subdirectories while respecting depth limits.
    
    Args:
        root_dir: Starting directory path
        prefix: String prefix for tree indentation
        max_depth: Maximum recursion depth to prevent infinite loops
        current_depth: Current recursion level
        ignore_dirs: Set of directory names to skip
    
    Returns:
        String containing formatted directory tree
    """
    if ignore_dirs is None:
        ignore_dirs = {".git", "__pycache__", ".pytest_cache", "*.egg-info", ".venv", "venv", "env", ".csv"}
    
    if current_depth >= max_depth:
        return ""
    
    structure = ""
    try:
        items = sorted(os.listdir(root_dir))
    except PermissionError:
        return ""
    
    dirs = []
    files = []
    
    for item in items:
        # Skip hidden files/dirs and common ignore patterns
        if item.startswith("."):
            continue
        if item in ignore_dirs or any(item.endswith(x.replace("*", "")) for x in ignore_dirs if "*" in x):
            continue
        
        item_path = os.path.join(root_dir, item)
        if os.path.isdir(item_path):
            dirs.append(item)
        else:
            files.append(item)
    
    # Add files first
    for i, file in enumerate(files):
        is_last_file = (i == len(files) - 1) and len(dirs) == 0
        structure += f"{prefix}{'└── ' if is_last_file else '├── '}{file}\n"
    
    # Add directories
    for i, dir_name in enumerate(dirs):
        is_last = i == len(dirs) - 1
        structure += f"{prefix}{'└── ' if is_last else '├── '}{dir_name}/\n"
        
        dir_path = os.path.join(root_dir, dir_name)
        extension = "    " if is_last else "│   "
        structure += get_project_structure(dir_path, prefix + extension, max_depth, current_depth + 1, ignore_dirs)
    
    return structure


def extract_python_files(root_dir, output_file):
    """
    Extract all Python files and their content.
    
    Main extraction function that:
    1. Walks through directory tree to find all .py files
    2. Generates project structure visualization
    3. Writes formatted output with all file contents
    4. Provides summary statistics
    
    Args:
        root_dir: Root directory to scan
        output_file: Path to output text file
    """
    py_files = []
    
    # Collect all Python files
    for root, dirs, files in os.walk(root_dir):
        # Skip certain directories
        dirs[:] = [d for d in dirs if d not in {".git", "__pycache__", ".pytest_cache", ".venv", "venv", "env", ".csv"}]
        
        for file in files:
            if file.endswith(".py"):
                file_path = os.path.join(root, file)
                py_files.append(file_path)
    
    # Sort files for consistent output
    py_files.sort()
    
    # Write to output file
    with open(output_file, "w", encoding="utf-8") as out:
        # Header
        out.write("=" * 80 + "\n")
        out.write("PROJECT EXTRACTION REPORT\n")
        out.write("=" * 80 + "\n\n")
        
        # Project Structure
        out.write("PROJECT STRUCTURE:\n")
        out.write("-" * 80 + "\n\n")
        out.write(f"{os.path.basename(root_dir)}/\n")
        out.write(get_project_structure(root_dir))
        out.write("\n\n")
        
        # File Contents
        out.write("=" * 80 + "\n")
        out.write("FILE CONTENTS\n")
        out.write("=" * 80 + "\n\n")
        
        for file_path in py_files:
            # Calculate relative path
            rel_path = os.path.relpath(file_path, root_dir)
            
            # Write file header
            out.write(f"\n{'=' * 80}\n")
            out.write(f"FILE: {rel_path}\n")
            out.write(f"{'=' * 80}\n\n")
            
            try:
                with open(file_path, "r", encoding="utf-8") as f:
                    content = f.read()
                    out.write(content)
            except Exception as e:
                out.write(f"[ERROR] Could not read file: {str(e)}\n")
            
            out.write("\n")
        
        # Summary
        out.write("\n" + "=" * 80 + "\n")
        out.write(f"SUMMARY: {len(py_files)} Python files extracted\n")
        out.write("=" * 80 + "\n")
    
    print(f"✓ Extraction complete!")
    print(f"✓ Total Python files: {len(py_files)}")
    print(f"✓ Output saved to: {output_file}")
    print(f"✓ File size: {os.path.getsize(output_file) / 1024:.2f} KB")


if __name__ == "__main__":
    # Get project root (current directory)
    project_root = os.getcwd()
    
    # Output file
    output_filename = "project_extraction.txt"
    output_path = os.path.join(project_root, output_filename)
    
    print(f"Extracting Python files from: {project_root}")
    print(f"Output file: {output_path}\n")
    
    extract_python_files(project_root, output_path)


================================================================================
FILE: llm\__init__.py
================================================================================



================================================================================
FILE: llm\nvidia_client.py
================================================================================

from openai import OpenAI


class NvidiaClient:

    def __init__(self, api_key):

        self.client = OpenAI(
            base_url="https://integrate.api.nvidia.com/v1",
            api_key=api_key,
            timeout=30
        )

    def ask(self, question):

        print("Sending request to NVIDIA...")

        completion = self.client.chat.completions.create(
            model="meta/llama-3.1-8b-instruct",
            messages=[
                {
                    "role": "user",
                    "content": question
                }
            ],
            temperature=0.7,
            max_tokens=300
        )

        print("Response received!")

        return completion.choices[0].message.content

================================================================================
FILE: main.py
================================================================================

from agents.student_agent import StudentAgent

def main():

    print("=== Student Teacher Agent ===")

    student = StudentAgent()

    while True:

        question = input("\nYou: ")

        if question.lower() == "exit":
            print("Goodbye!")
            break

        response = student.ask_teacher(question)

        print(f"\nTeacher: {response}")

        print("\nMemory:")
       # print(student.teacher.memory.get_history())


if __name__ == "__main__":
    main()

================================================================================
FILE: memory\__init__.py
================================================================================



================================================================================
FILE: memory\memory.py
================================================================================

import json


class Memory:

    def __init__(self):

        self.file_path = "data/chat_history.json"

        self.chat_history = self.load_history()

    def load_history(self):

        try:

            with open(self.file_path, "r") as file:

                return json.load(file)

        except:

            return []

    def save(self, role, message):

        self.chat_history.append(
            {
                "role": role,
                "message": message
            }
        )

        self.save_history()

    def save_history(self):

        with open(self.file_path, "w") as file:

            json.dump(
                self.chat_history,
                file,
                indent=4
            )

    def get_history(self):

        return self.chat_history

================================================================================
FILE: rag\chunker.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter


def create_chunks(text):

    splitter = RecursiveCharacterTextSplitter(

        chunk_size=1000,
        chunk_overlap=200,

        separators=[
            "\n\n",
            "\n",
            ". ",
            " ",
            ""
        ]
    )

    chunks = splitter.split_text(text)

    return chunks

================================================================================
FILE: rag\embedding_model.py
================================================================================

from sentence_transformers import SentenceTransformer

# Load only once
_model = None


def get_model():

    global _model

    if _model is None:

        print("Loading embedding model...")

        _model = SentenceTransformer(
            "all-MiniLM-L6-v2"
        )

    return _model


def create_embeddings(chunks):

    model = get_model()

    embeddings = model.encode(
        chunks,
        show_progress_bar=False
    )

    return embeddings

================================================================================
FILE: rag\pdf_reader.py
================================================================================

from pypdf import PdfReader


def read_pdf(pdf_path):
    reader = PdfReader(pdf_path)
    text = ""
    for page in reader.pages:
        page_text = page.extract_text()
        if page_text:
            text += page_text + "\n"
    return text

================================================================================
FILE: rag\rag_chat.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import os
import pickle

class RAGChat:

   def __init__(self):

    store = VectorStore()

    # Load existing files if available
    if (
        os.path.exists("chunks.pkl")
        and os.path.exists("faiss_index.bin")
    ):

        print("Loading Saved Chunks...")

        with open(
            "chunks.pkl",
            "rb"
        ) as f:

            self.chunks = pickle.load(f)

        print("Loading Saved FAISS...")

        store.load(
            "faiss_index.bin"
        )

    else:

        print("Loading PDF...")

        text = read_pdf(
            "sample.pdf"
        )

        print("Creating Chunks...")

        self.chunks = create_chunks(
            text
        )

        with open(
            "chunks.pkl",
            "wb"
        ) as f:

            pickle.dump(
                self.chunks,
                f
            )

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        store.save(
            "faiss_index.bin"
        )

        print("FAISS Saved!")

    self.retriever = Retriever(
        self.chunks,
        store
    )

    self.llm = NvidiaClient(
        NVIDIA_API_KEY
    )

   def ask(self, question):

    docs = self.retriever.retrieve(
        question,
        k=5
    )

    context = "\n\n".join(docs)

    prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.

Context:
{context}

Question:
{question}

Answer:
"""

    answer = self.llm.ask(prompt)

    return answer


================================================================================
FILE: rag\retriever.py
================================================================================

from rag.embedding_model import get_model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

        self.model = get_model()

    def retrieve(self, query, k=5):

        query_embedding = self.model.encode(
            query,
            show_progress_bar=False
        )

        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        for idx in indices:

            if 0 <= idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results

================================================================================
FILE: rag\test_chunks.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks

text = read_pdf("sample.pdf")

chunks = create_chunks(text)

print("Total Chunks:", len(chunks))

print("\nFirst Chunk:\n")
print(chunks[0])


================================================================================
FILE: rag\test_embeddings.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings

text = read_pdf("sample.pdf")

chunks = create_chunks(text)

embeddings = create_embeddings(chunks)

print("Total Chunks:", len(chunks))
print("Total Embeddings:", len(embeddings))

print("\nEmbedding Shape:")
print(embeddings.shape)

================================================================================
FILE: rag\test_faiss.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore

text = read_pdf("sample.pdf")

chunks = create_chunks(text)

embeddings = create_embeddings(chunks)

store = VectorStore(embeddings)

query = "What are Python functions?"

query_embedding = create_embeddings(
    [query]
)[0]

results = store.search(
    query_embedding,
    k=3
)

print("Best Chunks:\n")

for idx in results:

    print("=" * 50)

    print(chunks[idx][:500])

    print("\n")

================================================================================
FILE: rag\test_pdf.py
================================================================================

from pdf_reader import read_pdf

text = read_pdf("sample.pdf")

print(text[:1000])

================================================================================
FILE: rag\test_rag.py
================================================================================

import sys
import os

BASE_DIR = os.path.dirname(
    os.path.dirname(os.path.abspath(__file__))
)

sys.path.append(BASE_DIR)

from rag.rag_chat import RAGChat

bot = RAGChat()

while True:

    question = input("\nAsk: ")

    if question.lower() == "exit":
        break

    answer = bot.ask(question)

    print("\nAnswer:\n")

    print(answer)

================================================================================
FILE: rag\test_retriever.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore
from retriever import Retriever


text = read_pdf("sample.pdf")

chunks = create_chunks(text)

embeddings = create_embeddings(chunks)

store = VectorStore(embeddings)

retriever = Retriever(
    chunks,
    store
)

results = retriever.retrieve(
    "What are Python functions?"
)

for i, chunk in enumerate(results):

    print("\n")
    print("=" * 50)

    print(f"RESULT {i+1}")

    print("=" * 50)

    print(chunk[:500])

================================================================================
FILE: rag\vector_store.py
================================================================================

import faiss
import numpy as np
import os


class VectorStore:

    def __init__(self, embeddings=None):

        self.index = None

        if embeddings is not None:

            self.index = faiss.IndexFlatL2(
                embeddings.shape[1]
            )

            self.index.add(
                np.array(
                    embeddings
                ).astype("float32")
            )

    def save(self, path="faiss_index.bin"):

        faiss.write_index(
            self.index,
            path
        )

    def load(self, path="faiss_index.bin"):

        self.index = faiss.read_index(
            path
        )

    def exists(self, path="faiss_index.bin"):

        return os.path.exists(path)

    def search(self, query_embedding, k=5):

        distances, indices = self.index.search(
            np.array(
                [query_embedding]
            ).astype("float32"),
            k
        )

        return indices[0], distances[0]

================================================================================
FILE: test_nvidia.py
================================================================================

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

client = NvidiaClient(NVIDIA_API_KEY)

response = client.ask("What is AI?")

print(response)

================================================================================
FILE: test_nvidia_key.py
================================================================================

from config import NVIDIA_API_KEY

print(NVIDIA_API_KEY)

================================================================================
FILE: test_openai.py
================================================================================

from llm.openai_client import OpenAIClient
from config import OPENAI_API_KEY

client = OpenAIClient(OPENAI_API_KEY)

response = client.ask("What is AI?")

print(response)

================================================================================
FILE: tools\__init__.py
================================================================================



================================================================================
FILE: tools\calculator_tool.py
================================================================================

class CalculatorTool:

    def calculate(self, expression):

        try:
            result = eval(expression)
            return result

        except:
            return "Invalid Calculation"

================================================================================
FILE: tools\search_tool.py
================================================================================

from ddgs import DDGS


class SearchTool:

    def search(self, query):
        try:
            print("Searching:", query)
            results = DDGS().text(query, max_results=5)

            answer = ""
            for i, result in enumerate(results, start=1):
                answer += f"{i}. {result.get('title', 'No Title')}\n"
                if 'href' in result:
                    answer += f"{result['href']}\n\n"
                elif 'url' in result:
                    answer += f"{result['url']}\n\n"

            return answer if answer else "No results found."

        except Exception as e:
            return f"Search Error: {e}"


================================================================================
SUMMARY: 468 Python files extracted
================================================================================
