================================================================================
PROJECT EXTRACTION REPORT
================================================================================

PROJECT STRUCTURE:
--------------------------------------------------------------------------------

student_teacher_agent/
├── config.py
├── export_project.py
├── main.py
├── project_extraction.txt
├── requirements.txt
├── sample.pdf
├── test_nvidia.py
├── test_nvidia_key.py
├── test_openai.py
├── agents/
│   ├── __init__.py
│   ├── router.py
│   ├── student_agent.py
│   └── teacher_agent.py
├── data/
│   └── chat_history.json
├── llm/
│   ├── __init__.py
│   └── nvidia_client.py
├── logs/
│   └── app.log
├── memory/
│   ├── __init__.py
│   └── memory.py
├── rag/
│   ├── chunker.py
│   ├── embedding_model.py
│   ├── pdf_reader.py
│   ├── rag_chat.py
│   ├── retriever.py
│   ├── test_chunks.py
│   ├── test_embeddings.py
│   ├── test_faiss.py
│   ├── test_pdf.py
│   ├── test_rag.py
│   ├── test_retriever.py
│   └── vector_store.py
└── tools/
    ├── __init__.py
    ├── calculator_tool.py
    └── search_tool.py


================================================================================
FILE CONTENTS
================================================================================


================================================================================
FILE: .history\agents\__init___20260604234133.py
================================================================================



================================================================================
FILE: .history\agents\__init___20260604234134.py
================================================================================



================================================================================
FILE: .history\agents\__init___20260605234133.py
================================================================================

self.llm = NvidiaClient(NVIDIA_API_KEY)

================================================================================
FILE: .history\agents\__init___20260605234136.py
================================================================================

self.llm = NvidiaClient(NVIDIA_API_KEY)

================================================================================
FILE: .history\agents\__init___20260605234426.py
================================================================================



================================================================================
FILE: .history\agents\router_20260605002543.py
================================================================================



================================================================================
FILE: .history\agents\router_20260605002601.py
================================================================================

class Router:

    def route(self, question):

        question = question.lower()

        if any(op in question for op in ["+", "-", "*", "/"]):
            return "calculator"

        if "search" in question:
            return "search"

        return "knowledge"

================================================================================
FILE: .history\agents\router_20260605002602.py
================================================================================

class Router:

    def route(self, question):

        question = question.lower()

        if any(op in question for op in ["+", "-", "*", "/"]):
            return "calculator"

        if "search" in question:
            return "search"

        return "knowledge"

================================================================================
FILE: .history\agents\router_20260605002603.py
================================================================================

class Router:

    def route(self, question):

        question = question.lower()

        if any(op in question for op in ["+", "-", "*", "/"]):
            return "calculator"

        if "search" in question:
            return "search"

        return "knowledge"

================================================================================
FILE: .history\agents\student_agent_20260604232357.py
================================================================================



================================================================================
FILE: .history\agents\student_agent_20260604234134.py
================================================================================



================================================================================
FILE: .history\agents\student_agent_20260604234623.py
================================================================================

from agents.teacher_agent import TeacherAgent

class StudentAgent:

    def __init__(self):
        self.teacher = TeacherAgent()

    def ask_teacher(self, question):
        return self.teacher.answer(question)

================================================================================
FILE: .history\agents\teacher_agent_20260604232344.py
================================================================================



================================================================================
FILE: .history\agents\teacher_agent_20260604234134.py
================================================================================



================================================================================
FILE: .history\agents\teacher_agent_20260604234653.py
================================================================================

class TeacherAgent:

    def answer(self, question):

        question = question.lower()

        if "ai" in question:
            return "AI means Artificial Intelligence."

        elif "python" in question:
            return "Python is a popular programming language."

        elif "agent" in question:
            return "An AI Agent can think, plan and use tools."

        else:
            return "Sorry, I don't know that yet."

================================================================================
FILE: .history\agents\teacher_agent_20260604234654.py
================================================================================

class TeacherAgent:

    def answer(self, question):

        question = question.lower()

        if "ai" in question:
            return "AI means Artificial Intelligence."

        elif "python" in question:
            return "Python is a popular programming language."

        elif "agent" in question:
            return "An AI Agent can think, plan and use tools."

        else:
            return "Sorry, I don't know that yet."

================================================================================
FILE: .history\agents\teacher_agent_20260604235207.py
================================================================================

from memory.memory import Memory

class TeacherAgent:

    def __init__(self):
        self.memory = Memory()

    def answer(self, question):

        self.memory.save("student", question)

        question = question.lower()

        if "ai" in question:
            response = "AI means Artificial Intelligence."

        elif "python" in question:
            response = "Python is a popular programming language."

        elif "agent" in question:
            response = "An AI Agent can think, plan and use tools."

        else:
            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260604235208.py
================================================================================

from memory.memory import Memory

class TeacherAgent:

    def __init__(self):
        self.memory = Memory()

    def answer(self, question):

        self.memory.save("student", question)

        question = question.lower()

        if "ai" in question:
            response = "AI means Artificial Intelligence."

        elif "python" in question:
            response = "Python is a popular programming language."

        elif "agent" in question:
            response = "An AI Agent can think, plan and use tools."

        else:
            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260604235210.py
================================================================================

from memory.memory import Memory

class TeacherAgent:

    def __init__(self):
        self.memory = Memory()

    def answer(self, question):

        self.memory.save("student", question)

        question = question.lower()

        if "ai" in question:
            response = "AI means Artificial Intelligence."

        elif "python" in question:
            response = "Python is a popular programming language."

        elif "agent" in question:
            response = "An AI Agent can think, plan and use tools."

        else:
            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260604235432.py
================================================================================

from memory.memory import Memory

class TeacherAgent:

    def __init__(self):
        self.memory = Memory()

    def answer(self, question):

        self.memory.save("student", question)

        question = question.lower()

        if "ai" in question:
            response = "AI means Artificial Intelligence."

        elif "python" in question:
            response = "Python is a popular programming language."

        elif "agent" in question:
            response = "An AI Agent can think, plan and use tools."

        else:
            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605000440.py
================================================================================

from memory.memory import Memory
from memory.memory import Memory
from tools.calculator_tool import CalculatorTool

class TeacherAgent:

    def __init__(self):
        self.memory = Memory()

    def answer(self, question):

        self.memory.save("student", question)

        question = question.lower()

        if "ai" in question:
            response = "AI means Artificial Intelligence."

        elif "python" in question:
            response = "Python is a popular programming language."

        elif "agent" in question:
            response = "An AI Agent can think, plan and use tools."

        else:
            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605000446.py
================================================================================

from memory.memory import Memory

from tools.calculator_tool import CalculatorTool

class TeacherAgent:

    def __init__(self):
        self.memory = Memory()

    def answer(self, question):

        self.memory.save("student", question)

        question = question.lower()

        if "ai" in question:
            response = "AI means Artificial Intelligence."

        elif "python" in question:
            response = "Python is a popular programming language."

        elif "agent" in question:
            response = "An AI Agent can think, plan and use tools."

        else:
            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605000447.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool

class TeacherAgent:

    def __init__(self):
        self.memory = Memory()

    def answer(self, question):

        self.memory.save("student", question)

        question = question.lower()

        if "ai" in question:
            response = "AI means Artificial Intelligence."

        elif "python" in question:
            response = "Python is a popular programming language."

        elif "agent" in question:
            response = "An AI Agent can think, plan and use tools."

        else:
            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605000503.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool

class TeacherAgent:

    def __init__(self):
    self.memory = Memory()
    self.calculator = CalculatorTool()

    def answer(self, question):

        self.memory.save("student", question)

        question = question.lower()

        if "ai" in question:
            response = "AI means Artificial Intelligence."

        elif "python" in question:
            response = "Python is a popular programming language."

        elif "agent" in question:
            response = "An AI Agent can think, plan and use tools."

        else:
            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605000504.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool

class TeacherAgent:

    def __init__(self):
    self.memory = Memory()
    self.calculator = CalculatorTool()

    def answer(self, question):

        self.memory.save("student", question)

        question = question.lower()

        if "ai" in question:
            response = "AI means Artificial Intelligence."

        elif "python" in question:
            response = "Python is a popular programming language."

        elif "agent" in question:
            response = "An AI Agent can think, plan and use tools."

        else:
            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605000631.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool


class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

    def answer(self, question):

        self.memory.save("student", question)

        # Calculator Tool
        if any(op in question for op in ["+", "-", "*", "/"]):

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605001109.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool

class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

    def answer(self, question):

        self.memory.save("student", question)

        # Calculator Tool
        if any(op in question for op in ["+", "-", "*", "/"]):

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605001110.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool

class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

    def answer(self, question):

        self.memory.save("student", question)

        # Calculator Tool
        if any(op in question for op in ["+", "-", "*", "/"]):

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605001130.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool

class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

    def answer(self, question):

        self.memory.save("student", question)

        # Calculator Tool
        if any(op in question for op in ["+", "-", "*", "/"]):

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605001131.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool

class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

    def answer(self, question):

        self.memory.save("student", question)

        # Calculator Tool
        if any(op in question for op in ["+", "-", "*", "/"]):

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605001152.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool

class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

    def answer(self, question):

        self.memory.save("student", question)

        # Calculator Tool
        if any(op in question for op in ["+", "-", "*", "/"]):

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605001314.py
================================================================================

self.search_tool = SearchTool()

================================================================================
FILE: .history\agents\teacher_agent_20260605001319.py
================================================================================

self.search_tool = SearchTool()from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool


class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

    def answer(self, question):

        # Student question save
        self.memory.save("student", question)

        # -------------------------
        # Calculator Tool
        # -------------------------
        if any(op in question for op in ["+", "-", "*", "/"]):

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # -------------------------
        # Search Tool
        # -------------------------
        if "search" in question.lower():

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # -------------------------
        # Knowledge Responses
        # -------------------------
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        # Save teacher answer
        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605001320.py
================================================================================

self.search_tool = SearchTool()from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool


class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

    def answer(self, question):

        # Student question save
        self.memory.save("student", question)

        # -------------------------
        # Calculator Tool
        # -------------------------
        if any(op in question for op in ["+", "-", "*", "/"]):

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # -------------------------
        # Search Tool
        # -------------------------
        if "search" in question.lower():

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # -------------------------
        # Knowledge Responses
        # -------------------------
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        # Save teacher answer
        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605001535.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool


class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

    def answer(self, question):

        # Save student message
        self.memory.save("student", question)

        # Calculator Tool
        if any(op in question for op in ["+", "-", "*", "/"]):

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Tool
        if "search" in question.lower():

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Responses
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605001536.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool


class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

    def answer(self, question):

        # Save student message
        self.memory.save("student", question)

        # Calculator Tool
        if any(op in question for op in ["+", "-", "*", "/"]):

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Tool
        if "search" in question.lower():

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Responses
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605002623.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router

class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

    def answer(self, question):

        # Save student message
        self.memory.save("student", question)

        # Calculator Tool
        if any(op in question for op in ["+", "-", "*", "/"]):

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Tool
        if "search" in question.lower():

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Responses
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605002754.py
================================================================================

```python
from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router


class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response
```


================================================================================
FILE: .history\agents\teacher_agent_20260605002812.py
================================================================================

```python
from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router


class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response


================================================================================
FILE: .history\agents\teacher_agent_20260605002818.py
================================================================================

```python
from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router


class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response


================================================================================
FILE: .history\agents\teacher_agent_20260605002951.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router


class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234116.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234122.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234123.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234125.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234126.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = "Sorry, I don't know that yet."

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234200.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

       else:
    response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234204.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

       else:
        
    response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234206.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

       else:

        response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234209.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

        response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234212.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

     response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234215.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234217.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234221.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234455.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234456.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234503.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234548.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

           self.llm = NvidiaClient(NVIDIA_API_KEY)

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234554.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

       self.llm = NvidiaClient(NVIDIA_API_KEY)

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234555.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

        self.llm = NvidiaClient(NVIDIA_API_KEY)

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605234556.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
from config import NVIDIA_API_KEY

class TeacherAgent:

    def __init__(self):
        self.nvidia_client = NvidiaClient(NVIDIA_API_KEY)

        self.memory = Memory()

        self.calculator = CalculatorTool()

        self.search_tool = SearchTool()

        self.router = Router()

        self.llm = NvidiaClient(NVIDIA_API_KEY)

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        # Calculator Route
        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        # Search Route
        elif route == "search":

            result = self.search_tool.search(question)

            response = result

            self.memory.save("teacher", response)

            return response

        # Knowledge Route
        question = question.lower()

        if "ai" in question:

            response = "AI means Artificial Intelligence."

        elif "python" in question:

            response = "Python is a popular programming language."

        elif "agent" in question:

            response = "An AI Agent can think, plan and use tools."

        else:

            response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605235218.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class TeacherAgent:

    def __init__(self):

        self.memory = Memory()
        self.calculator = CalculatorTool()
        self.search_tool = SearchTool()
        self.router = Router()

        self.llm = NvidiaClient(NVIDIA_API_KEY)

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        elif route == "search":

            result = self.search_tool.search(question)

            self.memory.save("teacher", result)

            return result

        response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260605235219.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class TeacherAgent:

    def __init__(self):

        self.memory = Memory()
        self.calculator = CalculatorTool()
        self.search_tool = SearchTool()
        self.router = Router()

        self.llm = NvidiaClient(NVIDIA_API_KEY)

    def answer(self, question):

        self.memory.save("student", question)

        route = self.router.route(question)

        if route == "calculator":

            result = self.calculator.calculate(question)

            response = f"Answer = {result}"

            self.memory.save("teacher", response)

            return response

        elif route == "search":

            result = self.search_tool.search(question)

            self.memory.save("teacher", result)

            return result

        response = self.llm.ask(question)

        self.memory.save("teacher", response)

        return response

================================================================================
FILE: .history\agents\teacher_agent_20260606000701.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

```
def __init__(self):

    self.memory = Memory()
    self.calculator = CalculatorTool()
    self.search_tool = SearchTool()
    self.router = Router()

    self.llm = NvidiaClient(NVIDIA_API_KEY)

def answer(self, question):

    # Save student message
    self.memory.save("student", question)

    route = self.router.route(question)

    # Calculator Tool
    if route == "calculator":

        result = self.calculator.calculate(question)

        response = f"Answer = {result}"

        self.memory.save("teacher", response)

        return response

    # Search Tool
    elif route == "search":

        result = self.search_tool.search(question)

        self.memory.save("teacher", result)

        return result

    # ==========================
    # Memory Context
    # ==========================

    history = self.memory.get_history()

    context = ""

    for item in history[-10:]:

        context += f"{item['role']}: {item['message']}\n"

    full_prompt = f"""
```

Previous Conversation:

{context}

Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

```
    response = self.llm.ask(full_prompt)

    self.memory.save("teacher", response)

    return response
```


================================================================================
FILE: .history\agents\teacher_agent_20260606000951.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

```
dvef __init__(self):

    self.memory = Memory()
    self.calculator = CalculatorTool()
    self.search_tool = SearchTool()
    self.router = Router()

    self.llm = NvidiaClient(NVIDIA_API_KEY)

def answer(self, question):

    # Save student message
    self.memory.save("student", question)

    route = self.router.route(question)

    # Calculator Tool
    if route == "calculator":

        result = self.calculator.calculate(question)

        response = f"Answer = {result}"

        self.memory.save("teacher", response)

        return response

    # Search Tool
    elif route == "search":

        result = self.search_tool.search(question)

        self.memory.save("teacher", result)

        return result

    # ==========================
    # Memory Context
    # ==========================

    history = self.memory.get_history()

    context = ""

    for item in history[-10:]:

        context += f"{item['role']}: {item['message']}\n"

    full_prompt = f"""
```

Previous Conversation:

{context}

Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

```
    response = self.llm.ask(full_prompt)

    self.memory.save("teacher", response)

    return response
```


================================================================================
FILE: .history\agents\teacher_agent_20260606000952.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

```
def __init__(self):

    self.memory = Memory()
    self.calculator = CalculatorTool()
    self.search_tool = SearchTool()
    self.router = Router()

    self.llm = NvidiaClient(NVIDIA_API_KEY)

def answer(self, question):

    # Save student message
    self.memory.save("student", question)

    route = self.router.route(question)

    # Calculator Tool
    if route == "calculator":

        result = self.calculator.calculate(question)

        response = f"Answer = {result}"

        self.memory.save("teacher", response)

        return response

    # Search Tool
    elif route == "search":

        result = self.search_tool.search(question)

        self.memory.save("teacher", result)

        return result

    # ==========================
    # Memory Context
    # ==========================

    history = self.memory.get_history()

    context = ""

    for item in history[-10:]:

        context += f"{item['role']}: {item['message']}\n"

    full_prompt = f"""
```

Previous Conversation:

{context}

Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

```
    response = self.llm.ask(full_prompt)

    self.memory.save("teacher", response)

    return response
```


================================================================================
FILE: .history\agents\teacher_agent_20260606000954.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

``` 
def __init__(self):

    self.memory = Memory()
    self.calculator = CalculatorTool()
    self.search_tool = SearchTool()
    self.router = Router()

    self.llm = NvidiaClient(NVIDIA_API_KEY)

def answer(self, question):

    # Save student message
    self.memory.save("student", question)

    route = self.router.route(question)

    # Calculator Tool
    if route == "calculator":

        result = self.calculator.calculate(question)

        response = f"Answer = {result}"

        self.memory.save("teacher", response)

        return response

    # Search Tool
    elif route == "search":

        result = self.search_tool.search(question)

        self.memory.save("teacher", result)

        return result

    # ==========================
    # Memory Context
    # ==========================

    history = self.memory.get_history()

    context = ""

    for item in history[-10:]:

        context += f"{item['role']}: {item['message']}\n"

    full_prompt = f"""
```

Previous Conversation:

{context}

Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

```
    response = self.llm.ask(full_prompt)

    self.memory.save("teacher", response)

    return response
```


================================================================================
FILE: .history\agents\teacher_agent_20260606000955.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

``` 
  def __init__(self):

    self.memory = Memory()
    self.calculator = CalculatorTool()
    self.search_tool = SearchTool()
    self.router = Router()

    self.llm = NvidiaClient(NVIDIA_API_KEY)

def answer(self, question):

    # Save student message
    self.memory.save("student", question)

    route = self.router.route(question)

    # Calculator Tool
    if route == "calculator":

        result = self.calculator.calculate(question)

        response = f"Answer = {result}"

        self.memory.save("teacher", response)

        return response

    # Search Tool
    elif route == "search":

        result = self.search_tool.search(question)

        self.memory.save("teacher", result)

        return result

    # ==========================
    # Memory Context
    # ==========================

    history = self.memory.get_history()

    context = ""

    for item in history[-10:]:

        context += f"{item['role']}: {item['message']}\n"

    full_prompt = f"""
```

Previous Conversation:

{context}

Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

```
    response = self.llm.ask(full_prompt)

    self.memory.save("teacher", response)

    return response
```


================================================================================
FILE: .history\agents\teacher_agent_20260606000958.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

``` def __init__(self):

    self.memory = Memory()
    self.calculator = CalculatorTool()
    self.search_tool = SearchTool()
    self.router = Router()

    self.llm = NvidiaClient(NVIDIA_API_KEY)

def answer(self, question):

    # Save student message
    self.memory.save("student", question)

    route = self.router.route(question)

    # Calculator Tool
    if route == "calculator":

        result = self.calculator.calculate(question)

        response = f"Answer = {result}"

        self.memory.save("teacher", response)

        return response

    # Search Tool
    elif route == "search":

        result = self.search_tool.search(question)

        self.memory.save("teacher", result)

        return result

    # ==========================
    # Memory Context
    # ==========================

    history = self.memory.get_history()

    context = ""

    for item in history[-10:]:

        context += f"{item['role']}: {item['message']}\n"

    full_prompt = f"""
```

Previous Conversation:

{context}

Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

```
    response = self.llm.ask(full_prompt)

    self.memory.save("teacher", response)

    return response
```


================================================================================
FILE: .history\agents\teacher_agent_20260606001001.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

```
def __init__(self):

    self.memory = Memory()
    self.calculator = CalculatorTool()
    self.search_tool = SearchTool()
    self.router = Router()

    self.llm = NvidiaClient(NVIDIA_API_KEY)

def answer(self, question):

    # Save student message
    self.memory.save("student", question)

    route = self.router.route(question)

    # Calculator Tool
    if route == "calculator":

        result = self.calculator.calculate(question)

        response = f"Answer = {result}"

        self.memory.save("teacher", response)

        return response

    # Search Tool
    elif route == "search":

        result = self.search_tool.search(question)

        self.memory.save("teacher", result)

        return result

    # ==========================
    # Memory Context
    # ==========================

    history = self.memory.get_history()

    context = ""

    for item in history[-10:]:

        context += f"{item['role']}: {item['message']}\n"

    full_prompt = f"""
```

Previous Conversation:

{context}

Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

```
    response = self.llm.ask(full_prompt)

    self.memory.save("teacher", response)

    return response
```


================================================================================
FILE: .history\agents\teacher_agent_20260606001014.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

```
def __init__(self):

    self.memory = Memory()
    self.calculator = CalculatorTool()
    self.search_tool = SearchTool()
    self.router = Router()

    self.llm = NvidiaClient(NVIDIA_API_KEY)

def answer(self, question):

    self.memory.save("student", question)

    route = self.router.route(question)

    if route == "calculator":

        result = self.calculator.calculate(question)

        response = f"Answer = {result}"

        self.memory.save("teacher", response)

        return response

    elif route == "search":

        result = self.search_tool.search(question)

        self.memory.save("teacher", result)

        return result

    history = self.memory.get_history()

    context = ""

    for item in history[-10:]:

        context += f"{item['role']}: {item['message']}\n"

    full_prompt = f"""
```

Previous Conversation:

{context}

Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

```
    response = self.llm.ask(full_prompt)

    self.memory.save("teacher", response)

    return response
```


================================================================================
FILE: .history\agents\teacher_agent_20260606001015.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

class TeacherAgent:

```
def __init__(self):

    self.memory = Memory()
    self.calculator = CalculatorTool()
    self.search_tool = SearchTool()
    self.router = Router()

    self.llm = NvidiaClient(NVIDIA_API_KEY)

def answer(self, question):

    self.memory.save("student", question)

    route = self.router.route(question)

    if route == "calculator":

        result = self.calculator.calculate(question)

        response = f"Answer = {result}"

        self.memory.save("teacher", response)

        return response

    elif route == "search":

        result = self.search_tool.search(question)

        self.memory.save("teacher", result)

        return result

    history = self.memory.get_history()

    context = ""

    for item in history[-10:]:

        context += f"{item['role']}: {item['message']}\n"

    full_prompt = f"""
```

Previous Conversation:

{context}

Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

```
    response = self.llm.ask(full_prompt)

    self.memory.save("teacher", response)

    return response
```


================================================================================
FILE: .history\agents\teacher_agent_20260606001110.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class TeacherAgent:

    def __init__(self):
        self.memory = Memory()
        self.calculator = CalculatorTool()
        self.search_tool = SearchTool()
        self.router = Router()
        self.llm = NvidiaClient(NVIDIA_API_KEY)

    def answer(self, question):
        self.memory.save("student", question)

        route = self.router.route(question)

        if route == "calculator":
            result = self.calculator.calculate(question)
            response = f"Answer = {result}"
            self.memory.save("teacher", response)
            return response

        elif route == "search":
            result = self.search_tool.search(question)
            self.memory.save("teacher", result)
            return result

        history = self.memory.get_history()
        context = ""
        for item in history[-10:]:
            context += f"{item['role']}: {item['message']}\n"

        full_prompt = f"""
Previous Conversation:

{context}
Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

        response = self.llm.ask(full_prompt)
        self.memory.save("teacher", response)
        return response


================================================================================
FILE: .history\agents\teacher_agent_20260606001425.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class TeacherAgent:

    def __init__(self):
        self.memory = Memory()
        self.calculator = CalculatorTool()
        self.search_tool = SearchTool()
        self.router = Router()
        self.llm = NvidiaClient(NVIDIA_API_KEY)

    def answer(self, question):
        self.memory.save("student", question)

        route = self.router.route(question)

        if route == "calculator":
            result = self.calculator.calculate(question)
            response = f"Answer = {result}"
            self.memory.save("teacher", response)
            return response

        elif route == "search":
            result = self.search_tool.search(question)
            self.memory.save("teacher", result)
            return result

        history = self.memory.get_history()
        context = ""
        for item in history[-10:]:
            context += f"{item['role']}: {item['message']}\n"

        full_prompt = f"""
Previous Conversation:

{context}
Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

        response = self.llm.ask(full_prompt)
        self.memory.save("teacher", response)
        return response


================================================================================
FILE: .history\config_20260604233139.py
================================================================================



================================================================================
FILE: .history\config_20260605003752.py
================================================================================

from dotenv import load_dotenv
import os

load_dotenv()

OPENAI_API_KEY = os.getenv("OPENAI_API_KEY")

================================================================================
FILE: .history\config_20260605225644.py
================================================================================

from dotenv import load_dotenv
import os

load_dotenv()

NVIDIA_API_KEY = os.getenv("NVIDIA_API_KEY")

================================================================================
FILE: .history\config_20260605225912.py
================================================================================

from dotenv import load_dotenv
import os

load_dotenv()

NVIDIA_API_KEY = os.getenv("NVIDIA_API_KEY")

================================================================================
FILE: .history\config_20260605225913.py
================================================================================

from dotenv import load_dotenv
import os

load_dotenv()

NVIDIA_API_KEY = os.getenv("NVIDIA_API_KEY")

================================================================================
FILE: .history\config_20260605225914.py
================================================================================

from dotenv import load_dotenv
import os

load_dotenv()

NVIDIA_API_KEY = os.getenv("NVIDIA_API_KEY")

================================================================================
FILE: .history\export_project_20260605232706.py
================================================================================



================================================================================
FILE: .history\export_project_20260605232710.py
================================================================================

#!/usr/bin/env python3
# ============================================================================
# PROJECT EXTRACTION UTILITY
# ============================================================================
# Purpose: Generate a comprehensive report of the entire Python codebase
# Output: Single text file containing:
#         1. Visual tree structure of project directories/files
#         2. Complete content of all Python files
#         3. Summary statistics
# Usage: python Project_extract.py
# Use Case: Code review, documentation, sharing codebase overview
# ============================================================================

"""
Extract all Python files with their content and project structure into a single text file.
"""

import os
import sys
from pathlib import Path


def get_project_structure(root_dir, prefix="", max_depth=10, current_depth=0, ignore_dirs=None):
    """
    Generate a text representation of the project structure.
    
    Creates a tree-style visualization of directories and files.
    Recursively traverses subdirectories while respecting depth limits.
    
    Args:
        root_dir: Starting directory path
        prefix: String prefix for tree indentation
        max_depth: Maximum recursion depth to prevent infinite loops
        current_depth: Current recursion level
        ignore_dirs: Set of directory names to skip
    
    Returns:
        String containing formatted directory tree
    """
    if ignore_dirs is None:
        ignore_dirs = {".git", "__pycache__", ".pytest_cache", "*.egg-info", ".venv", "venv", "env", ".csv"}
    
    if current_depth >= max_depth:
        return ""
    
    structure = ""
    try:
        items = sorted(os.listdir(root_dir))
    except PermissionError:
        return ""
    
    dirs = []
    files = []
    
    for item in items:
        # Skip hidden files/dirs and common ignore patterns
        if item.startswith("."):
            continue
        if item in ignore_dirs or any(item.endswith(x.replace("*", "")) for x in ignore_dirs if "*" in x):
            continue
        
        item_path = os.path.join(root_dir, item)
        if os.path.isdir(item_path):
            dirs.append(item)
        else:
            files.append(item)
    
    # Add files first
    for i, file in enumerate(files):
        is_last_file = (i == len(files) - 1) and len(dirs) == 0
        structure += f"{prefix}{'└── ' if is_last_file else '├── '}{file}\n"
    
    # Add directories
    for i, dir_name in enumerate(dirs):
        is_last = i == len(dirs) - 1
        structure += f"{prefix}{'└── ' if is_last else '├── '}{dir_name}/\n"
        
        dir_path = os.path.join(root_dir, dir_name)
        extension = "    " if is_last else "│   "
        structure += get_project_structure(dir_path, prefix + extension, max_depth, current_depth + 1, ignore_dirs)
    
    return structure


def extract_python_files(root_dir, output_file):
    """
    Extract all Python files and their content.
    
    Main extraction function that:
    1. Walks through directory tree to find all .py files
    2. Generates project structure visualization
    3. Writes formatted output with all file contents
    4. Provides summary statistics
    
    Args:
        root_dir: Root directory to scan
        output_file: Path to output text file
    """
    py_files = []
    
    # Collect all Python files
    for root, dirs, files in os.walk(root_dir):
        # Skip certain directories
        dirs[:] = [d for d in dirs if d not in {".git", "__pycache__", ".pytest_cache", ".venv", "venv", "env", ".csv"}]
        
        for file in files:
            if file.endswith(".py"):
                file_path = os.path.join(root, file)
                py_files.append(file_path)
    
    # Sort files for consistent output
    py_files.sort()
    
    # Write to output file
    with open(output_file, "w", encoding="utf-8") as out:
        # Header
        out.write("=" * 80 + "\n")
        out.write("PROJECT EXTRACTION REPORT\n")
        out.write("=" * 80 + "\n\n")
        
        # Project Structure
        out.write("PROJECT STRUCTURE:\n")
        out.write("-" * 80 + "\n\n")
        out.write(f"{os.path.basename(root_dir)}/\n")
        out.write(get_project_structure(root_dir))
        out.write("\n\n")
        
        # File Contents
        out.write("=" * 80 + "\n")
        out.write("FILE CONTENTS\n")
        out.write("=" * 80 + "\n\n")
        
        for file_path in py_files:
            # Calculate relative path
            rel_path = os.path.relpath(file_path, root_dir)
            
            # Write file header
            out.write(f"\n{'=' * 80}\n")
            out.write(f"FILE: {rel_path}\n")
            out.write(f"{'=' * 80}\n\n")
            
            try:
                with open(file_path, "r", encoding="utf-8") as f:
                    content = f.read()
                    out.write(content)
            except Exception as e:
                out.write(f"[ERROR] Could not read file: {str(e)}\n")
            
            out.write("\n")
        
        # Summary
        out.write("\n" + "=" * 80 + "\n")
        out.write(f"SUMMARY: {len(py_files)} Python files extracted\n")
        out.write("=" * 80 + "\n")
    
    print(f"✓ Extraction complete!")
    print(f"✓ Total Python files: {len(py_files)}")
    print(f"✓ Output saved to: {output_file}")
    print(f"✓ File size: {os.path.getsize(output_file) / 1024:.2f} KB")


if __name__ == "__main__":
    # Get project root (current directory)
    project_root = os.getcwd()
    
    # Output file
    output_filename = "project_extraction.txt"
    output_path = os.path.join(project_root, output_filename)
    
    print(f"Extracting Python files from: {project_root}")
    print(f"Output file: {output_path}\n")
    
    extract_python_files(project_root, output_path)


================================================================================
FILE: .history\llm\__init___20260605232458.py
================================================================================



================================================================================
FILE: .history\llm\__init___20260605232504.py
================================================================================



================================================================================
FILE: .history\llm\nvidia_client_20260605233304.py
================================================================================

from openai import OpenAI


class NvidiaClient:

    def __init__(self, api_key):

        self.client = OpenAI(
            base_url="https://integrate.api.nvidia.com/v1",
            api_key=api_key
        )

    def ask(self, question):

        completion = self.client.chat.completions.create(
            model="meta/llama-3.1-70b-instruct",
            messages=[
                {
                    "role": "user",
                    "content": question
                }
            ]
        )

        return completion.choices[0].message.content


================================================================================
FILE: .history\llm\nvidia_client_20260605233307.py
================================================================================

from openai import OpenAI


class NvidiaClient:

    def __init__(self, api_key):

        self.client = OpenAI(
            base_url="https://integrate.api.nvidia.com/v1",
            api_key=api_key
        )

    def ask(self, question):

        completion = self.client.chat.completions.create(
            model="meta/llama-3.1-70b-instruct",
            messages=[
                {
                    "role": "user",
                    "content": question
                }
            ]
        )

        return completion.choices[0].message.content


================================================================================
FILE: .history\llm\nvidia_client_20260605234821.py
================================================================================

from openai import OpenAI


class NvidiaClient:

    def __init__(self, api_key):

        self.client = OpenAI(
            base_url="https://integrate.api.nvidia.com/v1",
            api_key=api_key
        )

    def ask(self, question):

        completion = self.client.chat.completions.create(
            model="meta/llama-3.1-8b-instruct"",
            messages=[
                {
                    "role": "user",
                    "content": question
                }
            ]
        )

        return completion.choices[0].message.content


================================================================================
FILE: .history\llm\nvidia_client_20260605234824.py
================================================================================

from openai import OpenAI


class NvidiaClient:

    def __init__(self, api_key):

        self.client = OpenAI(
            base_url="https://integrate.api.nvidia.com/v1",
            api_key=api_key
        )

    def ask(self, question):

        completion = self.client.chat.completions.create(
            model="meta/llama-3.1-8b-instruct",
            messages=[
                {
                    "role": "user",
                    "content": question
                }
            ]
        )

        return completion.choices[0].message.content


================================================================================
FILE: .history\llm\nvidia_client_20260605234825.py
================================================================================

from openai import OpenAI


class NvidiaClient:

    def __init__(self, api_key):

        self.client = OpenAI(
            base_url="https://integrate.api.nvidia.com/v1",
            api_key=api_key
        )

    def ask(self, question):

        completion = self.client.chat.completions.create(
            model="meta/llama-3.1-8b-instruct",
            messages=[
                {
                    "role": "user",
                    "content": question
                }
            ]
        )

        return completion.choices[0].message.content


================================================================================
FILE: .history\llm\nvidia_client_20260605234826.py
================================================================================

from openai import OpenAI


class NvidiaClient:

    def __init__(self, api_key):

        self.client = OpenAI(
            base_url="https://integrate.api.nvidia.com/v1",
            api_key=api_key
        )

    def ask(self, question):

        completion = self.client.chat.completions.create(
            model="meta/llama-3.1-8b-instruct",
            messages=[
                {
                    "role": "user",
                    "content": question
                }
            ]
        )

        return completion.choices[0].message.content


================================================================================
FILE: .history\llm\nvidia_client_20260605235154.py
================================================================================

from openai import OpenAI


class NvidiaClient:

    def __init__(self, api_key):

        self.client = OpenAI(
            base_url="https://integrate.api.nvidia.com/v1",
            api_key=api_key,
            timeout=30
        )

    def ask(self, question):

        print("Sending request to NVIDIA...")

        completion = self.client.chat.completions.create(
            model="meta/llama-3.1-8b-instruct",
            messages=[
                {
                    "role": "user",
                    "content": question
                }
            ],
            temperature=0.7,
            max_tokens=300
        )

        print("Response received!")

        return completion.choices[0].message.content

================================================================================
FILE: .history\llm\openai_client_20260605003521.py
================================================================================



================================================================================
FILE: .history\llm\openai_client_20260605003715.py
================================================================================

from openai import OpenAI


class OpenAIClient:

    def __init__(self, api_key):

        self.client = OpenAI(api_key=api_key)

    def ask(self, question):

        response = self.client.chat.completions.create(

            model="gpt-4o-mini",

            messages=[
                {
                    "role": "user",
                    "content": question
                }
            ]

        )

        return response.choices[0].message.content

================================================================================
FILE: .history\llm\openai_client_20260605003716.py
================================================================================

from openai import OpenAI


class OpenAIClient:

    def __init__(self, api_key):

        self.client = OpenAI(api_key=api_key)

    def ask(self, question):

        response = self.client.chat.completions.create(

            model="gpt-4o-mini",

            messages=[
                {
                    "role": "user",
                    "content": question
                }
            ]

        )

        return response.choices[0].message.content

================================================================================
FILE: .history\llm\test_nvidia_20260605231159.py
================================================================================



================================================================================
FILE: .history\llm\test_nvidia_20260605231205.py
================================================================================

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

client = NvidiaClient(NVIDIA_API_KEY)

response = client.ask("What is AI?")

print(response)

================================================================================
FILE: .history\llm\test_nvidia_20260605231709.py
================================================================================

from nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

client = NvidiaClient(NVIDIA_API_KEY)

response = client.ask("What is AI?")

print(response)

================================================================================
FILE: .history\main_20260604232331.py
================================================================================



================================================================================
FILE: .history\main_20260604234535.py
================================================================================

from agents.student_agent import StudentAgent

def main():
    print("=== Student Teacher Agent ===")

    student = StudentAgent()

    while True:
        question = input("\nYou: ")

        if question.lower() == "exit":
            print("Goodbye!")
            break

        response = student.ask_teacher(question)

        print(f"\nTeacher: {response}")


if __name__ == "__main__":
    main()

================================================================================
FILE: .history\main_20260604235445.py
================================================================================

from agents.student_agent import StudentAgent

def main():

    print("=== Student Teacher Agent ===")

    student = StudentAgent()

    while True:

        question = input("\nYou: ")

        if question.lower() == "exit":
            print("Goodbye!")
            break

        response = student.ask_teacher(question)

        print(f"\nTeacher: {response}")

        print("\nMemory:")
        print(student.teacher.memory.get_history())


if __name__ == "__main__":
    main()

================================================================================
FILE: .history\main_20260605003200.py
================================================================================

from agents.student_agent import StudentAgent

def main():

    print("=== Student Teacher Agent ===")

    student = StudentAgent()

    while True:

        question = input("\nYou: ")

        if question.lower() == "exit":
            print("Goodbye!")
            break

        response = student.ask_teacher(question)

        print(f"\nTeacher: {response}")

        print("\nMemory:")
       #S print(student.teacher.memory.get_history())


if __name__ == "__main__":
    main()

================================================================================
FILE: .history\main_20260605003215.py
================================================================================

from agents.student_agent import StudentAgent

def main():

    print("=== Student Teacher Agent ===")

    student = StudentAgent()

    while True:

        question = input("\nYou: ")

        if question.lower() == "exit":
            print("Goodbye!")
            break

        response = student.ask_teacher(question)

        print(f"\nTeacher: {response}")

        print("\nMemory:")
       # print(student.teacher.memory.get_history())


if __name__ == "__main__":
    main()

================================================================================
FILE: .history\memory\__init___20260604234133.py
================================================================================



================================================================================
FILE: .history\memory\__init___20260604234134.py
================================================================================



================================================================================
FILE: .history\memory\memory_20260604232436.py
================================================================================



================================================================================
FILE: .history\memory\memory_20260604234134.py
================================================================================



================================================================================
FILE: .history\memory\memory_20260604235144.py
================================================================================

class Memory:

    def __init__(self):
        self.chat_history = []

    def save(self, role, message):

        self.chat_history.append(
            {
                "role": role,
                "message": message
            }
        )

    def get_history(self):

        return self.chat_history

================================================================================
FILE: .history\memory\memory_20260605001907.py
================================================================================

import json


class Memory:

    def __init__(self):

        self.file_path = "data/chat_history.json"

        self.chat_history = self.load_history()

    def load_history(self):

        try:

            with open(self.file_path, "r") as file:

                return json.load(file)

        except:

            return []

    def save(self, role, message):

        self.chat_history.append(
            {
                "role": role,
                "message": message
            }
        )

        self.save_history()

    def save_history(self):

        with open(self.file_path, "w") as file:

            json.dump(
                self.chat_history,
                file,
                indent=4
            )

    def get_history(self):

        return self.chat_history

================================================================================
FILE: .history\memory\memory_20260605001908.py
================================================================================

import json


class Memory:

    def __init__(self):

        self.file_path = "data/chat_history.json"

        self.chat_history = self.load_history()

    def load_history(self):

        try:

            with open(self.file_path, "r") as file:

                return json.load(file)

        except:

            return []

    def save(self, role, message):

        self.chat_history.append(
            {
                "role": role,
                "message": message
            }
        )

        self.save_history()

    def save_history(self):

        with open(self.file_path, "w") as file:

            json.dump(
                self.chat_history,
                file,
                indent=4
            )

    def get_history(self):

        return self.chat_history

================================================================================
FILE: .history\memory\memory_20260605001909.py
================================================================================

import json


class Memory:

    def __init__(self):

        self.file_path = "data/chat_history.json"

        self.chat_history = self.load_history()

    def load_history(self):

        try:

            with open(self.file_path, "r") as file:

                return json.load(file)

        except:

            return []

    def save(self, role, message):

        self.chat_history.append(
            {
                "role": role,
                "message": message
            }
        )

        self.save_history()

    def save_history(self):

        with open(self.file_path, "w") as file:

            json.dump(
                self.chat_history,
                file,
                indent=4
            )

    def get_history(self):

        return self.chat_history

================================================================================
FILE: .history\memory\memory_20260605001911.py
================================================================================

import json


class Memory:

    def __init__(self):

        self.file_path = "data/chat_history.json"

        self.chat_history = self.load_history()

    def load_history(self):

        try:

            with open(self.file_path, "r") as file:

                return json.load(file)

        except:

            return []

    def save(self, role, message):

        self.chat_history.append(
            {
                "role": role,
                "message": message
            }
        )

        self.save_history()

    def save_history(self):

        with open(self.file_path, "w") as file:

            json.dump(
                self.chat_history,
                file,
                indent=4
            )

    def get_history(self):

        return self.chat_history

================================================================================
FILE: .history\rag\chunker_20260606153056.py
================================================================================



================================================================================
FILE: .history\rag\chunker_20260606153105.py
================================================================================

chunker.py

================================================================================
FILE: .history\rag\chunker_20260606153106.py
================================================================================

chunker.py

================================================================================
FILE: .history\rag\chunker_20260606153107.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter

def create_chunks(text):

```
splitter = RecursiveCharacterTextSplitter(
    chunk_size=1000,
    chunk_overlap=200
)

chunks = splitter.split_text(text)

return chunks
```


================================================================================
FILE: .history\rag\chunker_20260606153108.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter

def create_chunks(text):

```
splitter = RecursiveCharacterTextSplitter(
    chunk_size=1000,
    chunk_overlap=200
)

chunks = splitter.split_text(text)

return chunks
```


================================================================================
FILE: .history\rag\chunker_20260606153120.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter

def create_chunks(text):

```
splitter = RecursiveCharacterTextSplitter(
    chunk_size=1000,
    chunk_overlap=200
)

chunks = splitter.split_text(text)

return chunks
```


================================================================================
FILE: .history\rag\chunker_20260606153236.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter


def create_chunks(text):
    splitter = RecursiveCharacterTextSplitter(
        chunk_size=1000,
        chunk_overlap=200
    )
    chunks = splitter.split_text(text)
    return chunks


================================================================================
FILE: .history\rag\chunker_20260606153238.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter


def create_chunks(text):
    splitter = RecursiveCharacterTextSplitter(
        chunk_size=1000,
        chunk_overlap=200
    )
    chunks = splitter.split_text(text)
    return chunks


================================================================================
FILE: .history\rag\chunker_20260606153242.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter


def create_chunks(text):
    splitter = RecursiveCharacterTextSplitter(
        chunk_size=1000,
        chunk_overlap=200
    )
    chunks = splitter.split_text(text)
    return chunks


================================================================================
FILE: .history\rag\chunker_20260606224849.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter


def create_chunks(text):

    splitter = RecursiveCharacterTextSplitter(
        chunk_size=500,
        chunk_overlap=100
    )

    chunks = splitter.split_text(text)


================================================================================
FILE: .history\rag\chunker_20260606224859.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter


def create_chunks(text):

    splitter = RecursiveCharacterTextSplitter(
        chunk_size=500,
        chunk_overlap=100
    )

    chunks = splitter.split_text(text)


================================================================================
FILE: .history\rag\chunker_20260606225420.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter


def create_chunks(text):
    # Handle list of dicts from pdf_reader
    if isinstance(text, list):
        text = "\n".join([item.get("text", "") for item in text])
    
    splitter = RecursiveCharacterTextSplitter(
        chunk_size=500,
        chunk_overlap=100
    )

    chunks = splitter.split_text(text)
    return chunks


================================================================================
FILE: .history\rag\chunker_20260606225434.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter


def create_chunks(text):
    # Handle list of dicts from pdf_reader
    if isinstance(text, list):
        text = "\n".join([item.get("text", "") for item in text])
    
    splitter = RecursiveCharacterTextSplitter(
        chunk_size=500,
        chunk_overlap=100
    )

    chunks = splitter.split_text(text)
    return chunks


================================================================================
FILE: .history\rag\chunker_20260606233007.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter


def create_chunks(text):

    splitter = RecursiveCharacterTextSplitter(
        chunk_size=1000,
        chunk_overlap=200
    )

    chunks = splitter.split_text(text)

    return chunks

================================================================================
FILE: .history\rag\chunker_20260606233008.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter


def create_chunks(text):

    splitter = RecursiveCharacterTextSplitter(
        chunk_size=1000,
        chunk_overlap=200
    )

    chunks = splitter.split_text(text)

    return chunks

================================================================================
FILE: .history\rag\chunker_20260606233208.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter


def create_chunks(text):
    # Handle list of dicts from pdf_reader
    if isinstance(text, list):
        text = "\n".join([item.get("text", "") for item in text])
    
    splitter = RecursiveCharacterTextSplitter(
        chunk_size=500,
        chunk_overlap=100
    )

    chunks = splitter.split_text(text)
    return chunks


================================================================================
FILE: .history\rag\chunker_20260606233302.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter


def create_chunks(text):

    splitter = RecursiveCharacterTextSplitter(

        chunk_size=1000,
        chunk_overlap=200,

        separators=[
            "\n\n",
            "\n",
            ". ",
            " ",
            ""
        ]
    )

    chunks = splitter.split_text(text)

    return chunks

================================================================================
FILE: .history\rag\chunker_20260606233303.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter


def create_chunks(text):

    splitter = RecursiveCharacterTextSplitter(

        chunk_size=1000,
        chunk_overlap=200,

        separators=[
            "\n\n",
            "\n",
            ". ",
            " ",
            ""
        ]
    )

    chunks = splitter.split_text(text)

    return chunks

================================================================================
FILE: .history\rag\embedding_model_20260606154824.py
================================================================================



================================================================================
FILE: .history\rag\embedding_model_20260606154832.py
================================================================================

from sentence_transformers import SentenceTransformer

model = SentenceTransformer(
    "all-MiniLM-L6-v2"
)

def create_embeddings(chunks):

    embeddings = model.encode(chunks)

    return embeddings

================================================================================
FILE: .history\rag\embedding_model_20260606225635.py
================================================================================

from sentence_transformers import SentenceTransformer

# Lazy load - only when needed
_model = None

def get_model():
    global _model
    if _model is None:
        print("Loading embedding model...")
        _model = SentenceTransformer("all-MiniLM-L6-v2")
    return _model

def create_embeddings(chunks):
    model = get_model()
    embeddings = model.encode(chunks, show_progress_bar=False)
    return embeddings

================================================================================
FILE: .history\rag\embedding_model_20260606225711.py
================================================================================

from sentence_transformers import SentenceTransformer

# Lazy load - only when needed
_model = None

def get_model():
    global _model
    if _model is None:
        print("Loading embedding model...")
        _model = SentenceTransformer("all-MiniLM-L6-v2")
    return _model

def create_embeddings(chunks):
    model = get_model()
    embeddings = model.encode(chunks, show_progress_bar=False)
    return embeddings

================================================================================
FILE: .history\rag\embedding_model_20260606233250.py
================================================================================

from sentence_transformers import SentenceTransformer

# Load only once
_model = None


def get_model():

    global _model

    if _model is None:

        print("Loading embedding model...")

        _model = SentenceTransformer(
            "all-MiniLM-L6-v2"
        )

    return _model


def create_embeddings(chunks):

    model = get_model()

    embeddings = model.encode(
        chunks,
        show_progress_bar=False
    )

    return embeddings

================================================================================
FILE: .history\rag\pdf_reader_20260606004044.py
================================================================================

from pypdf import PdfReader


def read_pdf(pdf_path):
    reader = PdfReader(pdf_path)
    text = ""
    for page in reader.pages:
        page_text = page.extract_text()
        if page_text:
            text += page_text + "\n"
    return text


================================================================================
FILE: .history\rag\pdf_reader_20260606224524.py
================================================================================

from pypdf import PdfReader


def read_pdf(pdf_path):

    reader = PdfReader(pdf_path)

    pages = []

    for page_num, page in enumerate(reader.pages):

        page_text = page.extract_text()

        if page_text:

            pages.append(
                {
                    "page": page_num + 1,
                    "text": page_text
                }
            )

    return pages

================================================================================
FILE: .history\rag\pdf_reader_20260606233722.py
================================================================================

from pypdf import PdfReader


def read_pdf(pdf_path):
    reader = PdfReader(pdf_path)
    text = ""
    for page in reader.pages:
        page_text = page.extract_text()
        if page_text:
            text += page_text + "\n"
    return text

================================================================================
FILE: .history\rag\pdf_reader_20260606233815.py
================================================================================

from pypdf import PdfReader


def read_pdf(pdf_path):
    reader = PdfReader(pdf_path)
    text = ""
    for page in reader.pages:
        page_text = page.extract_text()
        if page_text:
            text += page_text + "\n"
    return text

================================================================================
FILE: .history\rag\rag_chat_20260606161608.py
================================================================================



================================================================================
FILE: .history\rag\rag_chat_20260606161613.py
================================================================================

rag_chat.py

================================================================================
FILE: .history\rag\rag_chat_20260606161614.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore
from retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question
        )

        context = "\n\n".join(docs)

        prompt = f"""
Use the context below to answer.

Context:
{context}

Question:
{question}
"""

        return self.llm.ask(prompt)

================================================================================
FILE: .history\rag\rag_chat_20260606161615.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore
from retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question
        )

        context = "\n\n".join(docs)

        prompt = f"""
Use the context below to answer.

Context:
{context}

Question:
{question}
"""

        return self.llm.ask(prompt)

================================================================================
FILE: .history\rag\rag_chat_20260606175706.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore
from retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:

I could not find that information in the PDF.

4. Do not guess.
5. Do not make assumptions.
6. Keep answers clear and concise.

Context:
{context}

Question:
{question}

Answer:
"""

        return self.llm.ask(prompt)

================================================================================
FILE: .history\rag\rag_chat_20260606175707.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore
from retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:

I could not find that information in the PDF.

4. Do not guess.
5. Do not make assumptions.
6. Keep answers clear and concise.

Context:
{context}

Question:
{question}

Answer:
"""

        return self.llm.ask(prompt)

================================================================================
FILE: .history\rag\rag_chat_20260606180235.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore
from retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:

I could not find that information in the PDF.

4. Do not guess.
5. Do not make assumptions.
6. Keep answers clear and concise.

Context:
{context}

Question:
{question}

Answer:
"""

        return self.llm.ask(prompt)

================================================================================
FILE: .history\rag\rag_chat_20260606180242.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore
from retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:

I could not find that information in the PDF.

4. Do not guess.
5. Do not make assumptions.
6. Keep answers clear and concise.

Context:
{context}

Question:
{question}

Answer:
"""

        return self.llm.ask(prompt)

================================================================================
FILE: .history\rag\rag_chat_20260606224929.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore
from retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260606224930.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore
from retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260606231246.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore
from retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260606231247.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore
from retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260606231307.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore
from retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260606233749.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\rag_chat_20260606233815.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: .history\rag\retriever_20260606160936.py
================================================================================



================================================================================
FILE: .history\rag\retriever_20260606160942.py
================================================================================

from embedding_model import create_embeddings


class Retriever:

    def __init__(self, chunks, vector_store):

        self.chunks = chunks
        self.vector_store = vector_store

    def retrieve(self, question, k=3):

        query_embedding = create_embeddings(
            [question]
        )[0]

        indices = self.vector_store.search(
            query_embedding,
            k
        )

        results = []

        for idx in indices:

            results.append(
                self.chunks[idx]
            )

        return results

================================================================================
FILE: .history\rag\retriever_20260606160943.py
================================================================================

from embedding_model import create_embeddings


class Retriever:

    def __init__(self, chunks, vector_store):

        self.chunks = chunks
        self.vector_store = vector_store

    def retrieve(self, question, k=3):

        query_embedding = create_embeddings(
            [question]
        )[0]

        indices = self.vector_store.search(
            query_embedding,
            k
        )

        results = []

        for idx in indices:

            results.append(
                self.chunks[idx]
            )

        return results

================================================================================
FILE: .history\rag\retriever_20260606181345.py
================================================================================

from embedding_model import model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        query_embedding = model.encode(query)

        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        for idx in indices:
            results.append(
                self.chunks[idx]
            )

        return results

================================================================================
FILE: .history\rag\retriever_20260606181346.py
================================================================================

from embedding_model import model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        query_embedding = model.encode(query)

        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        for idx in indices:
            results.append(
                self.chunks[idx]
            )

        return results

================================================================================
FILE: .history\rag\retriever_20260606225032.py
================================================================================

from embedding_model import model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        query_embedding = model.encode(query)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results

================================================================================
FILE: .history\rag\retriever_20260606225033.py
================================================================================

from embedding_model import model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        query_embedding = model.encode(query)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results

================================================================================
FILE: .history\rag\retriever_20260606225910.py
================================================================================

from embedding_model import get_model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        model = get_model()
        query_embedding = model.encode(query, show_progress_bar=False)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results


    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        query_embedding = model.encode(query)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results

================================================================================
FILE: .history\rag\retriever_20260606225920.py
================================================================================

from embedding_model import get_model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        model = get_model()
        query_embedding = model.encode(query, show_progress_bar=False)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results


    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        query_embedding = model.encode(query)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results

================================================================================
FILE: .history\rag\retriever_20260606225931.py
================================================================================

from embedding_model import get_model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        model = get_model()
        query_embedding = model.encode(query, show_progress_bar=False)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results


    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        query_embedding = model.encode(query, show_progress_bar=False)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results

================================================================================
FILE: .history\rag\retriever_20260606225938.py
================================================================================

from embedding_model import get_model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        model = get_model()
        query_embedding = model.encode(query, show_progress_bar=False)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results


    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        query_embedding = model.encode(query, show_progress_bar=False)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results

================================================================================
FILE: .history\rag\retriever_20260606231418.py
================================================================================

from embedding_model import model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        query_embedding = model.encode(query)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results

================================================================================
FILE: .history\rag\retriever_20260606231419.py
================================================================================

from embedding_model import model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        query_embedding = model.encode(query)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results

================================================================================
FILE: .history\rag\retriever_20260606231714.py
================================================================================

from embedding_model import get_model


class Retriever:

    def __init__(self, chunks, store):
        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):
        # Convert question into embedding
        model = get_model()
        query_embedding = model.encode(query, show_progress_bar=False)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:
            if idx < len(self.chunks):
                results.append(
                    self.chunks[idx]
                )

        return results


================================================================================
FILE: .history\rag\retriever_20260606231716.py
================================================================================

from embedding_model import get_model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        model = get_model()
        query_embedding = model.encode(query, show_progress_bar=False)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results


    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        query_embedding = model.encode(query, show_progress_bar=False)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results

================================================================================
FILE: .history\rag\retriever_20260606231718.py
================================================================================

from embedding_model import get_model


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        model = get_model()
        query_embedding = model.encode(query, show_progress_bar=False)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results


    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

    def retrieve(self, query, k=5):

        # Convert question into embedding
        query_embedding = model.encode(query, show_progress_bar=False)

        # Search FAISS index
        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        # Get matching chunks
        for idx in indices:

            if idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results

================================================================================
FILE: .history\rag\retriever_20260606232353.py
================================================================================

from sentence_transformers import SentenceTransformer


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

        print("Loading embedding model...")

        self.model = SentenceTransformer(
            "sentence-transformers/all-MiniLM-L6-v2"
        )

    def retrieve(self, query, k=5):

        query_embedding = self.model.encode(
            query,
            show_progress_bar=False
        )

        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        for idx in indices:

            if 0 <= idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results

================================================================================
FILE: .history\rag\test_chunks_20260606153139.py
================================================================================



================================================================================
FILE: .history\rag\test_chunks_20260606153148.py
================================================================================

test_chunks.py

================================================================================
FILE: .history\rag\test_chunks_20260606153149.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks

text = read_pdf("../sample.pdf")

chunks = create_chunks(text)

print("Total Chunks:", len(chunks))

print("\nFirst Chunk:\n")
print(chunks[0])


================================================================================
FILE: .history\rag\test_chunks_20260606153150.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks

text = read_pdf("../sample.pdf")

chunks = create_chunks(text)

print("Total Chunks:", len(chunks))

print("\nFirst Chunk:\n")
print(chunks[0])


================================================================================
FILE: .history\rag\test_chunks_20260606153151.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks

text = read_pdf("../sample.pdf")

chunks = create_chunks(text)

print("Total Chunks:", len(chunks))

print("\nFirst Chunk:\n")
print(chunks[0])


================================================================================
FILE: .history\rag\test_chunks_20260606153735.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks

text = read_pdf("sample.pdf")

chunks = create_chunks(text)

print("Total Chunks:", len(chunks))

print("\nFirst Chunk:\n")
print(chunks[0])


================================================================================
FILE: .history\rag\test_chunks_20260606153744.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks

text = read_pdf("sample.pdf")

chunks = create_chunks(text)

print("Total Chunks:", len(chunks))

print("\nFirst Chunk:\n")
print(chunks[0])


================================================================================
FILE: .history\rag\test_embeddings_20260606154908.py
================================================================================



================================================================================
FILE: .history\rag\test_embeddings_20260606154915.py
================================================================================

test_embeddings.py

================================================================================
FILE: .history\rag\test_embeddings_20260606154916.py
================================================================================

test_embeddings.py

================================================================================
FILE: .history\rag\test_embeddings_20260606154917.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings

text = read_pdf("sample.pdf")

chunks = create_chunks(text)

embeddings = create_embeddings(chunks)

print("Total Chunks:", len(chunks))
print("Total Embeddings:", len(embeddings))

print("\nEmbedding Shape:")
print(embeddings.shape)

================================================================================
FILE: .history\rag\test_embeddings_20260606154918.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings

text = read_pdf("sample.pdf")

chunks = create_chunks(text)

embeddings = create_embeddings(chunks)

print("Total Chunks:", len(chunks))
print("Total Embeddings:", len(embeddings))

print("\nEmbedding Shape:")
print(embeddings.shape)

================================================================================
FILE: .history\rag\test_embeddings_20260606154920.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings

text = read_pdf("sample.pdf")

chunks = create_chunks(text)

embeddings = create_embeddings(chunks)

print("Total Chunks:", len(chunks))
print("Total Embeddings:", len(embeddings))

print("\nEmbedding Shape:")
print(embeddings.shape)

================================================================================
FILE: .history\rag\test_faiss_20260606160203.py
================================================================================



================================================================================
FILE: .history\rag\test_faiss_20260606160208.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore

text = read_pdf("sample.pdf")

chunks = create_chunks(text)

embeddings = create_embeddings(chunks)

store = VectorStore(embeddings)

query = "What are Python functions?"

query_embedding = create_embeddings(
    [query]
)[0]

results = store.search(
    query_embedding,
    k=3
)

print("Best Chunks:\n")

for idx in results:

    print("=" * 50)

    print(chunks[idx][:500])

    print("\n")

================================================================================
FILE: .history\rag\test_faiss_20260606160209.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore

text = read_pdf("sample.pdf")

chunks = create_chunks(text)

embeddings = create_embeddings(chunks)

store = VectorStore(embeddings)

query = "What are Python functions?"

query_embedding = create_embeddings(
    [query]
)[0]

results = store.search(
    query_embedding,
    k=3
)

print("Best Chunks:\n")

for idx in results:

    print("=" * 50)

    print(chunks[idx][:500])

    print("\n")

================================================================================
FILE: .history\rag\test_faiss_20260606160211.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore

text = read_pdf("sample.pdf")

chunks = create_chunks(text)

embeddings = create_embeddings(chunks)

store = VectorStore(embeddings)

query = "What are Python functions?"

query_embedding = create_embeddings(
    [query]
)[0]

results = store.search(
    query_embedding,
    k=3
)

print("Best Chunks:\n")

for idx in results:

    print("=" * 50)

    print(chunks[idx][:500])

    print("\n")

================================================================================
FILE: .history\rag\test_pdf_20260606003633.py
================================================================================

from rag.pdf_reader import read_pdf

text = read_pdf("sample.pdf")

print(text[:1000])


================================================================================
FILE: .history\rag\test_pdf_20260606004527.py
================================================================================

from pdf_reader import read_pdf

text = read_pdf("sample.pdf")

print(text[:1000])

================================================================================
FILE: .history\rag\test_rag_20260606161636.py
================================================================================



================================================================================
FILE: .history\rag\test_rag_20260606161648.py
================================================================================

from rag_chat import RAGChat

bot = RAGChat()

while True:

    question = input("\nAsk: ")

    if question.lower() == "exit":
        break

    answer = bot.ask(question)

    print("\nAnswer:\n")

    print(answer)

================================================================================
FILE: .history\rag\test_rag_20260606161649.py
================================================================================

from rag_chat import RAGChat

bot = RAGChat()

while True:

    question = input("\nAsk: ")

    if question.lower() == "exit":
        break

    answer = bot.ask(question)

    print("\nAnswer:\n")

    print(answer)

================================================================================
FILE: .history\rag\test_rag_20260606161837.py
================================================================================

from rag_chat import RAGChat
import sys
import os

BASE_DIR = os.path.dirname(
    os.path.dirname(os.path.abspath(__file__))
)

sys.path.append(BASE_DIR)
bot = RAGChat()

while True:

    question = input("\nAsk: ")

    if question.lower() == "exit":
        break

    answer = bot.ask(question)

    print("\nAnswer:\n")

    print(answer)

================================================================================
FILE: .history\rag\test_rag_20260606161844.py
================================================================================

from rag_chat import RAGChat
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY
import sys
import os

BASE_DIR = os.path.dirname(
    os.path.dirname(os.path.abspath(__file__))
)

sys.path.append(BASE_DIR)
bot = RAGChat()

while True:

    question = input("\nAsk: ")

    if question.lower() == "exit":
        break

    answer = bot.ask(question)

    print("\nAnswer:\n")

    print(answer)

================================================================================
FILE: .history\rag\test_rag_20260606161938.py
================================================================================

import sys
import os

BASE_DIR = os.path.dirname(
    os.path.dirname(os.path.abspath(__file__))
)

sys.path.append(BASE_DIR)

from rag_chat import RAGChat

bot = RAGChat()

while True:

    question = input("\nAsk: ")

    if question.lower() == "exit":
        break

    answer = bot.ask(question)

    print("\nAnswer:\n")

    print(answer)

================================================================================
FILE: .history\rag\test_rag_20260606233753.py
================================================================================

import sys
import os

BASE_DIR = os.path.dirname(
    os.path.dirname(os.path.abspath(__file__))
)

sys.path.append(BASE_DIR)

from rag.rag_chat import RAGChat

bot = RAGChat()

while True:

    question = input("\nAsk: ")

    if question.lower() == "exit":
        break

    answer = bot.ask(question)

    print("\nAnswer:\n")

    print(answer)

================================================================================
FILE: .history\rag\test_rag_20260606233815.py
================================================================================

import sys
import os

BASE_DIR = os.path.dirname(
    os.path.dirname(os.path.abspath(__file__))
)

sys.path.append(BASE_DIR)

from rag.rag_chat import RAGChat

bot = RAGChat()

while True:

    question = input("\nAsk: ")

    if question.lower() == "exit":
        break

    answer = bot.ask(question)

    print("\nAnswer:\n")

    print(answer)

================================================================================
FILE: .history\rag\test_retriever_20260606161005.py
================================================================================



================================================================================
FILE: .history\rag\test_retriever_20260606161013.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore
from retriever import Retriever


text = read_pdf("sample.pdf")

chunks = create_chunks(text)

embeddings = create_embeddings(chunks)

store = VectorStore(embeddings)

retriever = Retriever(
    chunks,
    store
)

results = retriever.retrieve(
    "What are Python functions?"
)

for i, chunk in enumerate(results):

    print("\n")
    print("=" * 50)

    print(f"RESULT {i+1}")

    print("=" * 50)

    print(chunk[:500])

================================================================================
FILE: .history\rag\vector_store_20260606160052.py
================================================================================



================================================================================
FILE: .history\rag\vector_store_20260606160104.py
================================================================================

import faiss
import numpy as np


class VectorStore:

    def __init__(self, embeddings):

        self.index = faiss.IndexFlatL2(
            embeddings.shape[1]
        )

        self.index.add(
            np.array(embeddings).astype("float32")
        )

    def search(self, query_embedding, k=3):

        distances, indices = self.index.search(
            np.array([query_embedding]).astype("float32"),
            k
        )

        return indices[0]

================================================================================
FILE: .history\rag\vector_store_20260606181323.py
================================================================================

import faiss
import numpy as np


class VectorStore:

    def __init__(self, embeddings):

        self.index = faiss.IndexFlatL2(
            embeddings.shape[1]
        )

        self.index.add(
            np.array(embeddings).astype("float32")
        )

    def search(self, query_embedding, k=5):

        distances, indices = self.index.search(
            np.array([query_embedding]).astype("float32"),
            k
        )

        return indices[0], distances[0]

================================================================================
FILE: .history\rag\vector_store_20260606181339.py
================================================================================

import faiss
import numpy as np


class VectorStore:

    def __init__(self, embeddings):

        self.index = faiss.IndexFlatL2(
            embeddings.shape[1]
        )

        self.index.add(
            np.array(embeddings).astype("float32")
        )

    def search(self, query_embedding, k=5):

        distances, indices = self.index.search(
            np.array([query_embedding]).astype("float32"),
            k
        )

        return indices[0], distances[0]

================================================================================
FILE: .history\test_key_20260605005015.py
================================================================================



================================================================================
FILE: .history\test_key_20260605005026.py
================================================================================

from config import OPENAI_API_KEY

print(OPENAI_API_KEY)

================================================================================
FILE: .history\test_key_20260605005027.py
================================================================================

from config import OPENAI_API_KEY

print(OPENAI_API_KEY)

================================================================================
FILE: .history\test_nvidia_20260605231830.py
================================================================================

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

client = NvidiaClient(NVIDIA_API_KEY)

response = client.ask("What is AI?")

print(response)

================================================================================
FILE: .history\test_nvidia_20260605232538.py
================================================================================

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

client = NvidiaClient(NVIDIA_API_KEY)

response = client.ask("What is AI?")

print(response)

================================================================================
FILE: .history\test_nvidia_key_20260605225939.py
================================================================================



================================================================================
FILE: .history\test_nvidia_key_20260605225945.py
================================================================================

from config import NVIDIA_API_KEY

print(NVIDIA_API_KEY)

================================================================================
FILE: .history\test_openai_20260605005105.py
================================================================================



================================================================================
FILE: .history\test_openai_20260605005111.py
================================================================================

from llm.openai_client import OpenAIClient
from config import OPENAI_API_KEY

client = OpenAIClient(OPENAI_API_KEY)

response = client.ask("What is AI?")

print(response)

================================================================================
FILE: .history\test_openai_20260605005112.py
================================================================================

from llm.openai_client import OpenAIClient
from config import OPENAI_API_KEY

client = OpenAIClient(OPENAI_API_KEY)

response = client.ask("What is AI?")

print(response)

================================================================================
FILE: .history\test_openai_20260605005113.py
================================================================================

from llm.openai_client import OpenAIClient
from config import OPENAI_API_KEY

client = OpenAIClient(OPENAI_API_KEY)

response = client.ask("What is AI?")

print(response)

================================================================================
FILE: .history\test_openai_20260605005114.py
================================================================================

from llm.openai_client import OpenAIClient
from config import OPENAI_API_KEY

client = OpenAIClient(OPENAI_API_KEY)

response = client.ask("What is AI?")

print(response)

================================================================================
FILE: .history\tools\__init___20260604234133.py
================================================================================



================================================================================
FILE: .history\tools\__init___20260604234135.py
================================================================================



================================================================================
FILE: .history\tools\calculator_tool_20260604234134.py
================================================================================



================================================================================
FILE: .history\tools\calculator_tool_20260604234135.py
================================================================================



================================================================================
FILE: .history\tools\calculator_tool_20260605000108.py
================================================================================

class CalculatorTool:

    def calculate(self, expression):

        try:
            result = eval(expression)
            return result

        except:
            return "Invalid Calculation"

================================================================================
FILE: .history\tools\calculator_tool_20260605000212.py
================================================================================


class CalculatorTool:

    def calculate(self, expression):

        try:
            result = eval(expression)
            return result

        except:
            return "Invalid Calculation"

================================================================================
FILE: .history\tools\calculator_tool_20260605000215.py
================================================================================

from tools.calculator_tool import CalculatorTool
class CalculatorTool:

    def calculate(self, expression):

        try:
            result = eval(expression)
            return result

        except:
            return "Invalid Calculation"

================================================================================
FILE: .history\tools\calculator_tool_20260605000422.py
================================================================================

class CalculatorTool:

    def calculate(self, expression):

        try:
            result = eval(expression)
            return result

        except:
            return "Invalid Calculation"

================================================================================
FILE: .history\tools\rag\pdf_reader_20260606003602.py
================================================================================



================================================================================
FILE: .history\tools\rag\pdf_reader_20260606003609.py
================================================================================

from pypdf import PdfReader

def read_pdf(pdf_path):

```
reader = PdfReader(pdf_path)

text = ""

for page in reader.pages:

    text += page.extract_text()

return text
```


================================================================================
FILE: .history\tools\rag\pdf_reader_20260606003611.py
================================================================================

from pypdf import PdfReader

def read_pdf(pdf_path):

```
reader = PdfReader(pdf_path)

text = ""

for page in reader.pages:

    text += page.extract_text()

return text
```


================================================================================
FILE: .history\tools\rag\pdf_reader_20260606003614.py
================================================================================

from pypdf import PdfReader

def read_pdf(pdf_path):

```

reader = PdfReader(pdf_path)

text = ""

for page in reader.pages:

    text += page.extract_text()

return text
```


================================================================================
FILE: .history\tools\rag\pdf_reader_20260606003616.py
================================================================================

from pypdf import PdfReader

def read_pdf(pdf_path):

```
reader = PdfReader(pdf_path)

text = ""

for page in reader.pages:

    text += page.extract_text()

return text
```


================================================================================
FILE: .history\tools\rag\pdf_reader_20260606003717.py
================================================================================

from pypdf import PdfReader


def read_pdf(pdf_path):
    reader = PdfReader(pdf_path)
    text = ""
    for page in reader.pages:
        text += page.extract_text() or ""
    return text


================================================================================
FILE: .history\tools\rag\pdf_reader_20260606003719.py
================================================================================

from pypdf import PdfReader


def read_pdf(pdf_path):
    reader = PdfReader(pdf_path)
    text = ""
    for page in reader.pages:
        text += page.extract_text() or ""
    return text


================================================================================
FILE: .history\tools\rag\pdf_reader_20260606003906.py
================================================================================

from pypdf import PdfReader

def read_pdf(pdf_path):

```
reader = PdfReader(pdf_path)

text = ""

for page in reader.pages:

    page_text = page.extract_text()

    if page_text:

        text += page_text + "\n"

return text
```


================================================================================
FILE: .history\tools\rag\pdf_reader_20260606003908.py
================================================================================

from pypdf import PdfReader


def read_pdf(pdf_path):
    reader = PdfReader(pdf_path)
    text = ""
    for page in reader.pages:
        text += page.extract_text() or ""
    return text


================================================================================
FILE: .history\tools\rag\pdf_reader_20260606003909.py
================================================================================

from pypdf import PdfReader

def read_pdf(pdf_path):

```
reader = PdfReader(pdf_path)

text = ""

for page in reader.pages:

    page_text = page.extract_text()

    if page_text:

        text += page_text + "\n"

return text
```


================================================================================
FILE: .history\tools\rag\pdf_reader_20260606003910.py
================================================================================

from pypdf import PdfReader

def read_pdf(pdf_path):

```
reader = PdfReader(pdf_path)

text = ""

for page in reader.pages:

    page_text = page.extract_text()

    if page_text:

        text += page_text + "\n"

return text
```


================================================================================
FILE: .history\tools\rag\pdf_reader_20260606004033.py
================================================================================

from pypdf import PdfReader


def read_pdf(pdf_path):
    reader = PdfReader(pdf_path)
    text = ""
    for page in reader.pages:
        page_text = page.extract_text()
        if page_text:
            text += page_text + "\n"
    return text


================================================================================
FILE: .history\tools\rag\pdf_reader_20260606004045.py
================================================================================

from pypdf import PdfReader


def read_pdf(pdf_path):
    reader = PdfReader(pdf_path)
    text = ""
    for page in reader.pages:
        page_text = page.extract_text()
        if page_text:
            text += page_text + "\n"
    return text


================================================================================
FILE: .history\tools\rag\test_pdf_20260606003623.py
================================================================================



================================================================================
FILE: .history\tools\rag\test_pdf_20260606003632.py
================================================================================

from rag.pdf_reader import read_pdf

text = read_pdf("sample.pdf")

print(text[:1000])


================================================================================
FILE: .history\tools\rag\test_pdf_20260606003633.py
================================================================================

from rag.pdf_reader import read_pdf

text = read_pdf("sample.pdf")

print(text[:1000])


================================================================================
FILE: .history\tools\rag\test_pdf_20260606003634.py
================================================================================

from rag.pdf_reader import read_pdf

text = read_pdf("sample.pdf")

print(text[:1000])


================================================================================
FILE: .history\tools\search_tool_20260604234134.py
================================================================================



================================================================================
FILE: .history\tools\search_tool_20260604234135.py
================================================================================



================================================================================
FILE: .history\tools\search_tool_20260605001037.py
================================================================================

class SearchTool:

    def search(self, query):

        return f"Searching for: {query}"

================================================================================
FILE: .history\tools\search_tool_20260605001038.py
================================================================================

class SearchTool:

    def search(self, query):

        return f"Searching for: {query}"

================================================================================
FILE: .history\tools\search_tool_20260605001039.py
================================================================================

class SearchTool:

    def search(self, query):

        return f"Searching for: {query}"

================================================================================
FILE: .history\tools\search_tool_20260606001605.py
================================================================================

from duckduckgo_search import DDGS


class SearchTool:

    def search(self, query):

        try:

            with DDGS() as ddgs:

                results = list(
                    ddgs.text(
                        query,
                        max_results=5
                    )
                )

            answer = ""

            for i, result in enumerate(results, start=1):

                answer += (
                    f"{i}. {result['title']}\n"
                    f"{result['href']}\n\n"
                )

            return answer

        except Exception as e:

            return f"Search Error: {e}"

================================================================================
FILE: .history\tools\search_tool_20260606001606.py
================================================================================

from duckduckgo_search import DDGS


class SearchTool:

    def search(self, query):

        try:

            with DDGS() as ddgs:

                results = list(
                    ddgs.text(
                        query,
                        max_results=5
                    )
                )

            answer = ""

            for i, result in enumerate(results, start=1):

                answer += (
                    f"{i}. {result['title']}\n"
                    f"{result['href']}\n\n"
                )

            return answer

        except Exception as e:

            return f"Search Error: {e}"

================================================================================
FILE: .history\tools\search_tool_20260606001855.py
================================================================================

from duckduckgo_search import DDGS

class SearchTool:

```
def search(self, query):

    try:

        print("Searching:", query)

        with DDGS() as ddgs:

            results = list(ddgs.text(query, max_results=5))

        print(results)

        return str(results)

    except Exception as e:

        return f"ERROR: {e}"
```


================================================================================
FILE: .history\tools\search_tool_20260606001856.py
================================================================================

from duckduckgo_search import DDGS

class SearchTool:

```
def search(self, query):

    try:

        print("Searching:", query)

        with DDGS() as ddgs:

            results = list(ddgs.text(query, max_results=5))

        print(results)

        return str(results)

    except Exception as e:

        return f"ERROR: {e}"
```


================================================================================
FILE: .history\tools\search_tool_20260606002030.py
================================================================================

from duckduckgo_search import DDGS

class SearchTool:

```
def search(self, query):

    try:

        print("Searching:", query)

        with DDGS() as ddgs:

            results = list(ddgs.text(query, max_results=5))

        answer = ""

        for i, result in enumerate(results, start=1):

            answer += f"{i}. {result['title']}\n"
            answer += f"{result['href']}\n\n"

        return answer

    except Exception as e:

        return f"Search Error: {e}"
```


================================================================================
FILE: .history\tools\search_tool_20260606002031.py
================================================================================

from duckduckgo_search import DDGS

class SearchTool:

```
def search(self, query):

    try:

        print("Searching:", query)

        with DDGS() as ddgs:

            results = list(ddgs.text(query, max_results=5))

        answer = ""

        for i, result in enumerate(results, start=1):

            answer += f"{i}. {result['title']}\n"
            answer += f"{result['href']}\n\n"

        return answer

    except Exception as e:

        return f"Search Error: {e}"
```


================================================================================
FILE: .history\tools\search_tool_20260606002034.py
================================================================================

from duckduckgo_search import DDGS

class SearchTool:

```
def search(self, query):

    try:

        print("Searching:", query)

        with DDGS() as ddgs:

            results = list(ddgs.text(query, max_results=5))

        answer = ""

        for i, result in enumerate(results, start=1):

            answer += f"{i}. {result['title']}\n"
            answer += f"{result['href']}\n\n"

        return answer

    except Exception as e:

        return f"Search Error: {e}"
```


================================================================================
FILE: .history\tools\search_tool_20260606002044.py
================================================================================

from duckduckgo_search import DDGS

class SearchTool:

```
def search(self, query):

    try:

        print("Searching:", query)

        with DDGS() as ddgs:

            results = list(ddgs.text(query, max_results=5))

        answer = ""

        for i, result in enumerate(results, start=1):

            answer += f"{i}. {result['title']}\n"
            answer += f"{result['href']}\n\n"

        return answer

    except Exception as e:

        return f"Search Error: {e}"



================================================================================
FILE: .history\tools\search_tool_20260606002045.py
================================================================================

from duckduckgo_search import DDGS

class SearchTool:

```
def search(self, query):

    try:

        print("Searching:", query)

        with DDGS() as ddgs:

            results = list(ddgs.text(query, max_results=5))

        answer = ""

        for i, result in enumerate(results, start=1):

            answer += f"{i}. {result['title']}\n"
            answer += f"{result['href']}\n\n"

        return answer

    except Exception as e:

        return f"Search Error: {e}"



================================================================================
FILE: .history\tools\search_tool_20260606002049.py
================================================================================

from duckduckgo_search import DDGS

class SearchTool:

```
  def search(self, query):

    try:

        print("Searching:", query)

        with DDGS() as ddgs:

            results = list(ddgs.text(query, max_results=5))

        answer = ""

        for i, result in enumerate(results, start=1):

            answer += f"{i}. {result['title']}\n"
            answer += f"{result['href']}\n\n"

        return answer

    except Exception as e:

        return f"Search Error: {e}"



================================================================================
FILE: .history\tools\search_tool_20260606002051.py
================================================================================

from duckduckgo_search import DDGS

class SearchTool:

```
    def search(self, query):

    try:

        print("Searching:", query)

        with DDGS() as ddgs:

            results = list(ddgs.text(query, max_results=5))

        answer = ""

        for i, result in enumerate(results, start=1):

            answer += f"{i}. {result['title']}\n"
            answer += f"{result['href']}\n\n"

        return answer

    except Exception as e:

        return f"Search Error: {e}"



================================================================================
FILE: .history\tools\search_tool_20260606002053.py
================================================================================

from duckduckgo_search import DDGS

class SearchTool:

```
def search(self, query):

    try:

        print("Searching:", query)

        with DDGS() as ddgs:

            results = list(ddgs.text(query, max_results=5))

        answer = ""

        for i, result in enumerate(results, start=1):

            answer += f"{i}. {result['title']}\n"
            answer += f"{result['href']}\n\n"

        return answer

    except Exception as e:

        return f"Search Error: {e}"
```


================================================================================
FILE: .history\tools\search_tool_20260606002142.py
================================================================================

from duckduckgo_search import DDGS


class SearchTool:

    def search(self, query):
        try:
            print("Searching:", query)
            with DDGS() as ddgs:
                results = list(ddgs.text(query, max_results=5))

            answer = ""
            for i, result in enumerate(results, start=1):
                answer += f"{i}. {result['title']}\n"
                answer += f"{result['href']}\n\n"

            return answer

        except Exception as e:
            return f"Search Error: {e}"


================================================================================
FILE: .history\tools\search_tool_20260606002322.py
================================================================================

from duckduckgo_search import DDGS


class SearchTool:

    def search(self, query):
        try:
            print("Searching:", query)
            with DDGS() as ddgs:
                results = list(ddgs.text(query, max_results=5))

            answer = ""
            for i, result in enumerate(results, start=1):
                answer += f"{i}. {result['title']}\n"
                answer += f"{result['href']}\n\n"

            return answer

        except Exception as e:
            return f"Search Error: {e}"


================================================================================
FILE: .history\tools\search_tool_20260606002325.py
================================================================================

from duckduckgo_search import DDGS


class SearchTool:

    def search(self, query):
        try:
            print("Searching:", query)
            with DDGS() as ddgs:
                results = list(ddgs.text(query, max_results=5))

            answer = ""
            for i, result in enumerate(results, start=1):
                answer += f"{i}. {result['title']}\n"
                answer += f"{result['href']}\n\n"

            return answer

        except Exception as e:
            return f"Search Error: {e}"
            


================================================================================
FILE: .history\tools\search_tool_20260606002540.py
================================================================================

from duckduckgo_search import DDGS


class SearchTool:

    def search(self, query):
        try:
            print("Searching:", query)
            with DDGS() as ddgs:
                results = list(ddgs.text(query, max_results=5))

            answer = ""
            for i, result in enumerate(results, start=1):
                answer += f"{i}. {result['title']}\n"
                answer += f"{result['href']}\n\n"

            return answer

        except Exception as e:
            return f"Search Error: {e}"
            


================================================================================
FILE: .history\tools\search_tool_20260606002650.py
================================================================================

from ddgs import DDGS

class SearchTool:

```
def search(self, query):

    try:

        print("Searching:", query)

        results = DDGS().text(query, max_results=5)

        answer = ""

        for i, result in enumerate(results, start=1):

            answer += f"{i}. {result.get('title', 'No Title')}\n"

            if 'href' in result:
                answer += f"{result['href']}\n\n"
            elif 'url' in result:
                answer += f"{result['url']}\n\n"

        return answer if answer else "No results found."

    except Exception as e:

        return f"Search Error: {e}"
```


================================================================================
FILE: .history\tools\search_tool_20260606002750.py
================================================================================

from ddgs import DDGS


class SearchTool:

    def search(self, query):
        try:
            print("Searching:", query)
            results = DDGS().text(query, max_results=5)

            answer = ""
            for i, result in enumerate(results, start=1):
                answer += f"{i}. {result.get('title', 'No Title')}\n"
                if 'href' in result:
                    answer += f"{result['href']}\n\n"
                elif 'url' in result:
                    answer += f"{result['url']}\n\n"

            return answer if answer else "No results found."

        except Exception as e:
            return f"Search Error: {e}"


================================================================================
FILE: .history\tools\search_tool_20260606002753.py
================================================================================

from ddgs import DDGS


class SearchTool:

    def search(self, query):
        try:
            print("Searching:", query)
            results = DDGS().text(query, max_results=5)

            answer = ""
            for i, result in enumerate(results, start=1):
                answer += f"{i}. {result.get('title', 'No Title')}\n"
                if 'href' in result:
                    answer += f"{result['href']}\n\n"
                elif 'url' in result:
                    answer += f"{result['url']}\n\n"

            return answer if answer else "No results found."

        except Exception as e:
            return f"Search Error: {e}"


================================================================================
FILE: agents\__init__.py
================================================================================



================================================================================
FILE: agents\router.py
================================================================================

class Router:

    def route(self, question):

        question = question.lower()

        if any(op in question for op in ["+", "-", "*", "/"]):
            return "calculator"

        if "search" in question:
            return "search"

        return "knowledge"

================================================================================
FILE: agents\student_agent.py
================================================================================

from agents.teacher_agent import TeacherAgent

class StudentAgent:

    def __init__(self):
        self.teacher = TeacherAgent()

    def ask_teacher(self, question):
        return self.teacher.answer(question)

================================================================================
FILE: agents\teacher_agent.py
================================================================================

from memory.memory import Memory
from tools.calculator_tool import CalculatorTool
from tools.search_tool import SearchTool
from agents.router import Router
from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class TeacherAgent:

    def __init__(self):
        self.memory = Memory()
        self.calculator = CalculatorTool()
        self.search_tool = SearchTool()
        self.router = Router()
        self.llm = NvidiaClient(NVIDIA_API_KEY)

    def answer(self, question):
        self.memory.save("student", question)

        route = self.router.route(question)

        if route == "calculator":
            result = self.calculator.calculate(question)
            response = f"Answer = {result}"
            self.memory.save("teacher", response)
            return response

        elif route == "search":
            result = self.search_tool.search(question)
            self.memory.save("teacher", result)
            return result

        history = self.memory.get_history()
        context = ""
        for item in history[-10:]:
            context += f"{item['role']}: {item['message']}\n"

        full_prompt = f"""
Previous Conversation:

{context}
Current Question:
{question}

Answer naturally and use the conversation context if relevant.
"""

        response = self.llm.ask(full_prompt)
        self.memory.save("teacher", response)
        return response


================================================================================
FILE: config.py
================================================================================

from dotenv import load_dotenv
import os

load_dotenv()

NVIDIA_API_KEY = os.getenv("NVIDIA_API_KEY")

================================================================================
FILE: export_project.py
================================================================================

#!/usr/bin/env python3
# ============================================================================
# PROJECT EXTRACTION UTILITY
# ============================================================================
# Purpose: Generate a comprehensive report of the entire Python codebase
# Output: Single text file containing:
#         1. Visual tree structure of project directories/files
#         2. Complete content of all Python files
#         3. Summary statistics
# Usage: python Project_extract.py
# Use Case: Code review, documentation, sharing codebase overview
# ============================================================================

"""
Extract all Python files with their content and project structure into a single text file.
"""

import os
import sys
from pathlib import Path


def get_project_structure(root_dir, prefix="", max_depth=10, current_depth=0, ignore_dirs=None):
    """
    Generate a text representation of the project structure.
    
    Creates a tree-style visualization of directories and files.
    Recursively traverses subdirectories while respecting depth limits.
    
    Args:
        root_dir: Starting directory path
        prefix: String prefix for tree indentation
        max_depth: Maximum recursion depth to prevent infinite loops
        current_depth: Current recursion level
        ignore_dirs: Set of directory names to skip
    
    Returns:
        String containing formatted directory tree
    """
    if ignore_dirs is None:
        ignore_dirs = {".git", "__pycache__", ".pytest_cache", "*.egg-info", ".venv", "venv", "env", ".csv"}
    
    if current_depth >= max_depth:
        return ""
    
    structure = ""
    try:
        items = sorted(os.listdir(root_dir))
    except PermissionError:
        return ""
    
    dirs = []
    files = []
    
    for item in items:
        # Skip hidden files/dirs and common ignore patterns
        if item.startswith("."):
            continue
        if item in ignore_dirs or any(item.endswith(x.replace("*", "")) for x in ignore_dirs if "*" in x):
            continue
        
        item_path = os.path.join(root_dir, item)
        if os.path.isdir(item_path):
            dirs.append(item)
        else:
            files.append(item)
    
    # Add files first
    for i, file in enumerate(files):
        is_last_file = (i == len(files) - 1) and len(dirs) == 0
        structure += f"{prefix}{'└── ' if is_last_file else '├── '}{file}\n"
    
    # Add directories
    for i, dir_name in enumerate(dirs):
        is_last = i == len(dirs) - 1
        structure += f"{prefix}{'└── ' if is_last else '├── '}{dir_name}/\n"
        
        dir_path = os.path.join(root_dir, dir_name)
        extension = "    " if is_last else "│   "
        structure += get_project_structure(dir_path, prefix + extension, max_depth, current_depth + 1, ignore_dirs)
    
    return structure


def extract_python_files(root_dir, output_file):
    """
    Extract all Python files and their content.
    
    Main extraction function that:
    1. Walks through directory tree to find all .py files
    2. Generates project structure visualization
    3. Writes formatted output with all file contents
    4. Provides summary statistics
    
    Args:
        root_dir: Root directory to scan
        output_file: Path to output text file
    """
    py_files = []
    
    # Collect all Python files
    for root, dirs, files in os.walk(root_dir):
        # Skip certain directories
        dirs[:] = [d for d in dirs if d not in {".git", "__pycache__", ".pytest_cache", ".venv", "venv", "env", ".csv"}]
        
        for file in files:
            if file.endswith(".py"):
                file_path = os.path.join(root, file)
                py_files.append(file_path)
    
    # Sort files for consistent output
    py_files.sort()
    
    # Write to output file
    with open(output_file, "w", encoding="utf-8") as out:
        # Header
        out.write("=" * 80 + "\n")
        out.write("PROJECT EXTRACTION REPORT\n")
        out.write("=" * 80 + "\n\n")
        
        # Project Structure
        out.write("PROJECT STRUCTURE:\n")
        out.write("-" * 80 + "\n\n")
        out.write(f"{os.path.basename(root_dir)}/\n")
        out.write(get_project_structure(root_dir))
        out.write("\n\n")
        
        # File Contents
        out.write("=" * 80 + "\n")
        out.write("FILE CONTENTS\n")
        out.write("=" * 80 + "\n\n")
        
        for file_path in py_files:
            # Calculate relative path
            rel_path = os.path.relpath(file_path, root_dir)
            
            # Write file header
            out.write(f"\n{'=' * 80}\n")
            out.write(f"FILE: {rel_path}\n")
            out.write(f"{'=' * 80}\n\n")
            
            try:
                with open(file_path, "r", encoding="utf-8") as f:
                    content = f.read()
                    out.write(content)
            except Exception as e:
                out.write(f"[ERROR] Could not read file: {str(e)}\n")
            
            out.write("\n")
        
        # Summary
        out.write("\n" + "=" * 80 + "\n")
        out.write(f"SUMMARY: {len(py_files)} Python files extracted\n")
        out.write("=" * 80 + "\n")
    
    print(f"✓ Extraction complete!")
    print(f"✓ Total Python files: {len(py_files)}")
    print(f"✓ Output saved to: {output_file}")
    print(f"✓ File size: {os.path.getsize(output_file) / 1024:.2f} KB")


if __name__ == "__main__":
    # Get project root (current directory)
    project_root = os.getcwd()
    
    # Output file
    output_filename = "project_extraction.txt"
    output_path = os.path.join(project_root, output_filename)
    
    print(f"Extracting Python files from: {project_root}")
    print(f"Output file: {output_path}\n")
    
    extract_python_files(project_root, output_path)


================================================================================
FILE: llm\__init__.py
================================================================================



================================================================================
FILE: llm\nvidia_client.py
================================================================================

from openai import OpenAI


class NvidiaClient:

    def __init__(self, api_key):

        self.client = OpenAI(
            base_url="https://integrate.api.nvidia.com/v1",
            api_key=api_key,
            timeout=30
        )

    def ask(self, question):

        print("Sending request to NVIDIA...")

        completion = self.client.chat.completions.create(
            model="meta/llama-3.1-8b-instruct",
            messages=[
                {
                    "role": "user",
                    "content": question
                }
            ],
            temperature=0.7,
            max_tokens=300
        )

        print("Response received!")

        return completion.choices[0].message.content

================================================================================
FILE: main.py
================================================================================

from agents.student_agent import StudentAgent

def main():

    print("=== Student Teacher Agent ===")

    student = StudentAgent()

    while True:

        question = input("\nYou: ")

        if question.lower() == "exit":
            print("Goodbye!")
            break

        response = student.ask_teacher(question)

        print(f"\nTeacher: {response}")

        print("\nMemory:")
       # print(student.teacher.memory.get_history())


if __name__ == "__main__":
    main()

================================================================================
FILE: memory\__init__.py
================================================================================



================================================================================
FILE: memory\memory.py
================================================================================

import json


class Memory:

    def __init__(self):

        self.file_path = "data/chat_history.json"

        self.chat_history = self.load_history()

    def load_history(self):

        try:

            with open(self.file_path, "r") as file:

                return json.load(file)

        except:

            return []

    def save(self, role, message):

        self.chat_history.append(
            {
                "role": role,
                "message": message
            }
        )

        self.save_history()

    def save_history(self):

        with open(self.file_path, "w") as file:

            json.dump(
                self.chat_history,
                file,
                indent=4
            )

    def get_history(self):

        return self.chat_history

================================================================================
FILE: rag\chunker.py
================================================================================

from langchain_text_splitters import RecursiveCharacterTextSplitter


def create_chunks(text):

    splitter = RecursiveCharacterTextSplitter(

        chunk_size=1000,
        chunk_overlap=200,

        separators=[
            "\n\n",
            "\n",
            ". ",
            " ",
            ""
        ]
    )

    chunks = splitter.split_text(text)

    return chunks

================================================================================
FILE: rag\embedding_model.py
================================================================================

from sentence_transformers import SentenceTransformer

# Load only once
_model = None


def get_model():

    global _model

    if _model is None:

        print("Loading embedding model...")

        _model = SentenceTransformer(
            "all-MiniLM-L6-v2"
        )

    return _model


def create_embeddings(chunks):

    model = get_model()

    embeddings = model.encode(
        chunks,
        show_progress_bar=False
    )

    return embeddings

================================================================================
FILE: rag\pdf_reader.py
================================================================================

from pypdf import PdfReader


def read_pdf(pdf_path):
    reader = PdfReader(pdf_path)
    text = ""
    for page in reader.pages:
        page_text = page.extract_text()
        if page_text:
            text += page_text + "\n"
    return text

================================================================================
FILE: rag\rag_chat.py
================================================================================

from rag.pdf_reader import read_pdf
from rag.chunker import create_chunks
from rag.embedding_model import create_embeddings
from rag.vector_store import VectorStore
from rag.retriever import Retriever

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY


class RAGChat:

    def __init__(self):

        print("Loading PDF...")

        text = read_pdf("sample.pdf")

        print("Creating Chunks...")

        self.chunks = create_chunks(text)

        print("Creating Embeddings...")

        embeddings = create_embeddings(
            self.chunks
        )

        print("Building FAISS Index...")

        store = VectorStore(
            embeddings
        )

        self.retriever = Retriever(
            self.chunks,
            store
        )

        self.llm = NvidiaClient(
            NVIDIA_API_KEY
        )

    def ask(self, question):

        docs = self.retriever.retrieve(
            question,
            k=5
        )

        context = "\n\n".join(docs)

        prompt = f"""
You are a PDF assistant.

IMPORTANT RULES:

1. Answer ONLY from the provided context.
2. Never use outside knowledge.
3. If the answer is not found in the context, reply exactly:
   I could not find that information in the PDF.
4. Do not guess.
5. Keep answers short and clear.
6. If possible, explain in simple language.

Context:
{context}

Question:
{question}

Answer:
"""

        answer = self.llm.ask(prompt)

        return answer

================================================================================
FILE: rag\retriever.py
================================================================================

from sentence_transformers import SentenceTransformer


class Retriever:

    def __init__(self, chunks, store):

        self.chunks = chunks
        self.store = store

        print("Loading embedding model...")

        self.model = SentenceTransformer(
            "sentence-transformers/all-MiniLM-L6-v2"
        )

    def retrieve(self, query, k=5):

        query_embedding = self.model.encode(
            query,
            show_progress_bar=False
        )

        indices, distances = self.store.search(
            query_embedding,
            k
        )

        results = []

        for idx in indices:

            if 0 <= idx < len(self.chunks):

                results.append(
                    self.chunks[idx]
                )

        return results

================================================================================
FILE: rag\test_chunks.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks

text = read_pdf("sample.pdf")

chunks = create_chunks(text)

print("Total Chunks:", len(chunks))

print("\nFirst Chunk:\n")
print(chunks[0])


================================================================================
FILE: rag\test_embeddings.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings

text = read_pdf("sample.pdf")

chunks = create_chunks(text)

embeddings = create_embeddings(chunks)

print("Total Chunks:", len(chunks))
print("Total Embeddings:", len(embeddings))

print("\nEmbedding Shape:")
print(embeddings.shape)

================================================================================
FILE: rag\test_faiss.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore

text = read_pdf("sample.pdf")

chunks = create_chunks(text)

embeddings = create_embeddings(chunks)

store = VectorStore(embeddings)

query = "What are Python functions?"

query_embedding = create_embeddings(
    [query]
)[0]

results = store.search(
    query_embedding,
    k=3
)

print("Best Chunks:\n")

for idx in results:

    print("=" * 50)

    print(chunks[idx][:500])

    print("\n")

================================================================================
FILE: rag\test_pdf.py
================================================================================

from pdf_reader import read_pdf

text = read_pdf("sample.pdf")

print(text[:1000])

================================================================================
FILE: rag\test_rag.py
================================================================================

import sys
import os

BASE_DIR = os.path.dirname(
    os.path.dirname(os.path.abspath(__file__))
)

sys.path.append(BASE_DIR)

from rag.rag_chat import RAGChat

bot = RAGChat()

while True:

    question = input("\nAsk: ")

    if question.lower() == "exit":
        break

    answer = bot.ask(question)

    print("\nAnswer:\n")

    print(answer)

================================================================================
FILE: rag\test_retriever.py
================================================================================

from pdf_reader import read_pdf
from chunker import create_chunks
from embedding_model import create_embeddings
from vector_store import VectorStore
from retriever import Retriever


text = read_pdf("sample.pdf")

chunks = create_chunks(text)

embeddings = create_embeddings(chunks)

store = VectorStore(embeddings)

retriever = Retriever(
    chunks,
    store
)

results = retriever.retrieve(
    "What are Python functions?"
)

for i, chunk in enumerate(results):

    print("\n")
    print("=" * 50)

    print(f"RESULT {i+1}")

    print("=" * 50)

    print(chunk[:500])

================================================================================
FILE: rag\vector_store.py
================================================================================

import faiss
import numpy as np


class VectorStore:

    def __init__(self, embeddings):

        self.index = faiss.IndexFlatL2(
            embeddings.shape[1]
        )

        self.index.add(
            np.array(embeddings).astype("float32")
        )

    def search(self, query_embedding, k=5):

        distances, indices = self.index.search(
            np.array([query_embedding]).astype("float32"),
            k
        )

        return indices[0], distances[0]

================================================================================
FILE: test_nvidia.py
================================================================================

from llm.nvidia_client import NvidiaClient
from config import NVIDIA_API_KEY

client = NvidiaClient(NVIDIA_API_KEY)

response = client.ask("What is AI?")

print(response)

================================================================================
FILE: test_nvidia_key.py
================================================================================

from config import NVIDIA_API_KEY

print(NVIDIA_API_KEY)

================================================================================
FILE: test_openai.py
================================================================================

from llm.openai_client import OpenAIClient
from config import OPENAI_API_KEY

client = OpenAIClient(OPENAI_API_KEY)

response = client.ask("What is AI?")

print(response)

================================================================================
FILE: tools\__init__.py
================================================================================



================================================================================
FILE: tools\calculator_tool.py
================================================================================

class CalculatorTool:

    def calculate(self, expression):

        try:
            result = eval(expression)
            return result

        except:
            return "Invalid Calculation"

================================================================================
FILE: tools\search_tool.py
================================================================================

from ddgs import DDGS


class SearchTool:

    def search(self, query):
        try:
            print("Searching:", query)
            results = DDGS().text(query, max_results=5)

            answer = ""
            for i, result in enumerate(results, start=1):
                answer += f"{i}. {result.get('title', 'No Title')}\n"
                if 'href' in result:
                    answer += f"{result['href']}\n\n"
                elif 'url' in result:
                    answer += f"{result['url']}\n\n"

            return answer if answer else "No results found."

        except Exception as e:
            return f"Search Error: {e}"


================================================================================
SUMMARY: 293 Python files extracted
================================================================================
